# Contract: Runtime Composition and Non-Read Relationships

[日本語](runtime-composition.ja.md)

**Contract version**: 2026-07-20

**Official-source review**: 2026-07-15

**Normative for**: every `strategyId`, every `relationship-only` `ruleId`, and shared
non-read rules used by the initial-release inspection model

The vendor contracts define `behaviorId` records and the inspection allowlist defines
read-authorizing `ruleId` records. This contract alone defines how documented runtime
inputs may compose and how non-read relationships are represented. A strategy or
relationship never grants filesystem read authority.

## Canonical evidence-assessment index

Every `strategyId` and every relationship-only or shared non-read `ruleId` owned by this
contract carries its own `documentationStatus` and `lifecycleQualifiers`. Unless listed
below, the canonical values are `documented` and `[]`. This is a closed subject-by-subject
mapping, not an inference from evidence presence. Empty qualifiers mean that no lifecycle
claim is made and never mean `stable`. Documentation-status values are closed to
`documented`, `partially-documented`, `unknown`, and `conflict`. These are maintenance
records; no response carries one (QR-005).

| Subject ID | `documentationStatus` | `lifecycleQualifiers` | Assessment basis |
|---|---|---|---|
| `copilot.vscode.instructions.layering` | `partially-documented` | `[experimental]` | Same-layer ordering is incomplete and nested `AGENTS.md` selection is experimental |
| `copilot.vscode.skills.selection` | `partially-documented` | `[]` | Cross-location duplicate precedence is incomplete |
| `copilot.vscode.agents.selection` | `partially-documented` | `[]` | Cross-scope duplicate precedence is incomplete |
| `copilot.cli.hooks.composition` | `conflict` | `[]` | The hooks reference and the configuration reference disagree about the two inline blocks: every source's entries run for one event, and the repository `hooks` key overrides the user one for the same key |
| `copilot.vscode.hooks.composition` | `documented` | `[preview]` | The upstream hook feature is preview |
| `copilot.vscode.mcp.selection` | `conflict` | `[]` | The 1.118 workspace-root `.mcp.json`/most-specific release assertion conflicts with the current guide's exhaustive location list; root schema and total location order remain unknown |
| `copilot.cli.instructions.layering` | `partially-documented` | `[]` | General precedence among non-identical applicable files is not established |
| `copilot.cloud.instructions.layering` | `partially-documented` | `[]` | Order among the combined repository files and the coexistence of the agent-instruction alternatives are not established |
| `copilot.cli.skills.selection` | `partially-documented` | `[]` | Command anchoring and ancestry are only partially documented |
| `copilot.cli.agents.selection` | `conflict` | `[]` | Official project-versus-User precedence assertions conflict |
| `copilot.cloud.skills.selection` | `partially-documented` | `[]` | Exact remote-skill collision behavior is incomplete |
| `copilot.cloud.mcp.selection` | `partially-documented` | `[]` | The three-level processing order and the later-overrides-earlier direction are fixed, but neither the override's unit nor any cross-level merge rule is established |
| `claude.rules.layering` | `partially-documented` | `[]` | Ancestor `paths` base and descendant rule discovery are unclear |
| `claude.settings.precedence` | `partially-documented` | `[]` | Two array keys are excepted from the merge and a security-sensitive tier does not follow the order in either direction |
| `claude.commands.selection` | `partially-documented` | `[]` | Complete ancestor/lazy traversal is not independently stated |
| `claude.skills.selection` | `partially-documented` | `[]` | Exact IDE surface availability remains conditional |
| `claude.agents.selection` | `partially-documented` | `[]` | Same-tree duplicate order is unspecified |
| `claude.mcp.selection` | `partially-documented` | `[]` | Exact project-root selection is only partially specified |
| `codex.agents.inheritance` | `partially-documented` | `[]` | Project traversal and child instruction inheritance are incomplete |
| `codex.skills.discovery` | `partially-documented` | `[]` | The cited section establishes retention but no precedence or ordering among the discovery scopes |
| `codex.rules.resolution` | `partially-documented` | `[experimental]` | Nested recursion is unspecified and the rules feature is experimental |

The fixed qualifier order is `preview`, `experimental`, `deprecated`; arrays are
duplicate-free. The typed registry expands the default and exception table to one record
per subject. Existing Documentation status and Required conditions/status cells below are
human rationale, not serialized scalar enums. A provenance publishes which rule admitted
the file, never how completely that rule is documented.

## Runtime composition is not Inspector source merging

A strategy consumes independently recognized artifacts and unavailable-source facts. It
produces selection, ordering, shadowing, combination, or unresolved-condition metadata;
it does not concatenate files into a new artifact, execute content, connect to a server,
install a plugin, or change a candidate's source boundary.

| Input domain | Vendor runtime model | Inspector model |
|---|---|---|
| Repository | A vendor can walk runtime roots, select applicable files, and combine Repository inputs. | Each accepted file retains its Repository candidate provenance. A strategy may relate candidates but never expands the allowlist. |
| User | A vendor can combine User settings, instructions, skills, agents, hooks, MCP, or state with Repository inputs. | The Repository Source and zero to three tool-specific Global Sources remain separate. After the one selector-free session-wide consent evaluates all three frozen tool entries, only the frozen Global instruction rules for admitted roots can read User files; all other User inputs remain unavailable facts. |
| Hosted, organization, enterprise, or managed | A hosted surface can combine service-side inputs with a repository checkout. | These inputs are represented by `shared.relationship.runtime` or `shared.excluded.managed-remote-state`; they are never local scan roots. |
| Plugin, hook, MCP, import, or arbitrary path | A vendor may activate, execute, connect to, or follow it after runtime checks. | Authored declarations and relationships are inert. A target can be read only when an independent static or bounded-derived rule admits that same target. |

If a required condition fact is unavailable, this contract records the composition as
`unknown`. If official sources disagree, it records `documentation-conflict`. Neither
defaults to an inferred winner.

These are maintenance records about the vendors, not a projection the product makes. No
registry record carries a condition key, no response carries one, and no surface states
what a product would do with a file: the Inspector reports the customization files it
found and where, and says nothing about that runtime (FR-009). Repository and
member Global results are therefore never merged into an “effective configuration”
either — there is no configuration to merge, only Sources whose boundaries stay separate.

A documented hosted or runtime input with no originating customization file is out of
scope: the product reports the customization files it found, and behavior no file
originates belongs to the vendor's own documentation.

## Required condition facts

Strategy rows use these fact names. Each fact is independently `satisfied`, `unsatisfied`,
`unknown`, or `documentation-conflict` where applicable.

| Fact | Meaning |
|---|---|
| `surface` | Exact product surface, including local versus hosted execution. |
| `engine-version` | CLI, extension-bundled engine, or host version whose feature set applies. |
| `runtime-cwd` | Product runtime working directory, which can differ from the Inspector's selected Repository root. |
| `workspace-root` | VS Code workspace folder used by the local surface. In a `.code-workspace` multi-root workspace each configured folder is a distinct candidate value, and the folder actually used stays `unknown` until documented facts resolve it. |
| `repository-root` | Git or hosted repository root used by the product. |
| `project-root` | Product-defined project root when it differs from a repository root. |
| `worked-path` | File or directory currently being read, edited, or processed. |
| `target-match` | Path used by `applyTo`, `paths`, nearest-file, or other applicability logic. |
| `scope-availability` | Which Repository, User, plugin, built-in, hosted, managed, or session inputs actually exist. |
| `feature-state` | Feature flags, invocation mode, and experiment state. |
| `trust` | Workspace/project trust and trust gates. |
| `approval` | MCP approval, hook review, and approval gates. |
| `enablement` | Whether a recognized customization or runtime input is enabled at the relevant scope. |
| `selection` | Skill, agent, server, style, plugin, profile, command, or setting key being resolved. |
| `settings-inputs` | User/project/local/profile/managed values, environment, CLI flags, and key-specific merge mode. |
| `plugin-state` | Catalog registration, availability, selected components, and component overrides. |
| `agent-context` | Agent kind, parent session, context mode, tool availability, memory scope, and nesting depth. |
| `event` | Hook event and event-specific input. |
| `content-limits` | Upstream instruction-content policy and configured fallback names. |
| `documentation-variant` | Official-source variant when upstream pages conflict or leave an edge unspecified. |
| `tool-availability` | Exact tools available to the current main or child agent context. |
| `installation` | Whether a plugin, extension, or component is actually installed for the surface. |
| `managed-policy` | Administrator, enterprise, or managed-policy inputs and gates. |
| `instruction-byte-budget` | The upstream-configured instruction byte budget and the order in which inputs consume it. |
| `external-runtime` | Hosted, connector, server-provided, or other runtime data unavailable from local files. |

## GitHub Copilot strategies

### VS Code

| Strategy ID | Tool / surface | Operation(s) | Runtime projection | Input behavior refs | Required condition facts | Documentation status | Official source refs |
|---|---|---|---|---|---|---|---|
| `copilot.vscode.instructions.layering` | GitHub Copilot / local VS Code | `filter`, `append`, `unknown-order` | Filter enabled and applicable personal, Repository, and organization instructions; combine all applicable inputs using the documented personal-before-Repository-before-organization layer order; preserve unknown ordering within one layer. Nested `AGENTS.md` selection remains model-dependent | `copilot.behavior.vscode.instructions.agents`; `copilot.behavior.vscode.instructions.claude`; `copilot.behavior.vscode.instructions.path`; `copilot.behavior.vscode.instructions.repository`; `copilot.behavior.vscode.user.claude`; `copilot.behavior.vscode.user.instructions` | `surface`, `engine-version`, `workspace-root`, `worked-path`, `target-match`, `scope-availability`, `feature-state`, `enablement`, `settings-inputs`, `managed-policy`, `external-runtime` | partially documented; same-layer order and experimental nested selection are not deterministic | `vscode.copilot.instructions`, `vscode.copilot.settings` |
| `copilot.vscode.skills.selection` | GitHub Copilot / local VS Code | `filter`, `select-first`, `unknown-order` | Discover metadata from enabled locations and parent layers, then progressively load a relevant skill; do not invent a duplicate-name winner | `copilot.behavior.vscode.skills`; `copilot.behavior.vscode.user.skills` | `surface`, `engine-version`, `workspace-root`, `worked-path`, `scope-availability`, `feature-state`, `enablement`, `selection`, `settings-inputs`, `external-runtime` | partially documented: cross-location duplicate precedence is unknown | `github.copilot.skills`, `vscode.copilot.settings`, `vscode.copilot.skills` |
| `copilot.vscode.agents.selection` | GitHub Copilot / local VS Code | `filter`, `select-first`, `unknown-order` | Select an invocable or inferred profile whose target includes VS Code; retain its body/model/tool facts and preserve unresolved same-name precedence across workspace, User, organization, and plugin sources | `copilot.behavior.vscode.agents`; `copilot.behavior.vscode.user.agents` | `surface`, `engine-version`, `workspace-root`, `scope-availability`, `feature-state`, `enablement`, `selection`, `plugin-state`, `external-runtime` | partially documented: cross-scope duplicate precedence is unknown | `github.copilot.custom-agents`, `vscode.copilot.custom-agents`, `vscode.copilot.settings` |
| `copilot.vscode.hooks.composition` | GitHub Copilot / local VS Code | `filter`, `select-first`, `append` | Resolve the event's workspace and User hooks with workspace precedence for the same event, then retain additional applicable agent and plugin hooks | `copilot.behavior.vscode.agents`; `copilot.behavior.vscode.hooks`; `copilot.behavior.vscode.plugins`; `copilot.behavior.vscode.user.hooks` | `surface`, `engine-version`, `workspace-root`, `scope-availability`, `feature-state`, `trust`, `approval`, `enablement`, `selection`, `plugin-state`, `event`, `managed-policy` | documented; preview and activation conditions remain required | `vscode.copilot.hooks` |
| `copilot.vscode.mcp.selection` | GitHub Copilot / local VS Code | `merge-map`, `replace`, `unknown-order` | For `.vscode/mcp.json`, project the documented VS Code `servers` input. For VS Code 1.118+ root `.mcp.json`, retain only path/surface availability until direct documentation establishes its schema. The release note's “most-specific” same-name rule does not define a total order across the root, `.vscode`, User, and plugin inputs, so preserve every unresolved winner as `documentation-conflict` or unknown condition rather than composing an inferred map. An agent profile is no input here: the custom-agents reference documents its `mcp-servers` field as not used in VS Code custom agents | `copilot.behavior.vscode.mcp`; `copilot.behavior.vscode.user.mcp` | `surface`, `engine-version`, `workspace-root`, `scope-availability`, `trust`, `approval`, `enablement`, `selection`, `settings-inputs`, `external-runtime` | conflict: current guide and 1.118 release locations disagree; root schema and total same-name order are unknown | `vscode.copilot.mcp`, `vscode.copilot.mcp.workspace-root-release` |
| `copilot.vscode.settings.precedence` | GitHub Copilot / local VS Code | `merge-map`, `replace` | Resolve VS Code setting scopes, with workspace values above User values and policy, remote, language, and profile scopes retained as inputs | `copilot.behavior.vscode.settings`; `copilot.behavior.vscode.user.settings` | `surface`, `engine-version`, `workspace-root`, `scope-availability`, `selection`, `settings-inputs`, `managed-policy` | documented | `vscode.settings` |
| `copilot.vscode.plugins.activation` | GitHub Copilot / local VS Code | `select-first`, `filter` | At an explicitly established root, apply manifest/catalog recognition order, then keep registration, recommendation, installation, enablement, availability, and component selection as separate activation states | `copilot.behavior.vscode.plugins`; `copilot.behavior.vscode.user.plugins` | `surface`, `engine-version`, `workspace-root`, `scope-availability`, `trust`, `approval`, `enablement`, `selection`, `plugin-state`, `installation`, `external-runtime` | documented; activation is runtime-state dependent | `github.copilot.plugins`, `vscode.copilot.plugins` |

### Copilot CLI

| Strategy ID | Tool / surface | Operation(s) | Runtime projection | Input behavior refs | Required condition facts | Documentation status | Official source refs |
|---|---|---|---|---|---|---|---|
| `copilot.cli.instructions.layering` | GitHub Copilot / CLI | `filter`, `deduplicate`, `append`, `unknown-order` | Collect applicable standard-location and User instructions, filter `applyTo` and disablement, remove duplicate copies of identical user-level root, repository-wide, and agent instruction files — path-specific files are outside the documented deduplication — and combine the remainder without inventing a general precedence | `copilot.behavior.cli.instructions.agents`; `copilot.behavior.cli.instructions.claude`; `copilot.behavior.cli.instructions.gemini`; `copilot.behavior.cli.instructions.path`; `copilot.behavior.cli.instructions.repository`; `copilot.behavior.cli.user.instructions.path`; `copilot.behavior.cli.user.instructions.root` | `surface`, `engine-version`, `runtime-cwd`, `repository-root`, `worked-path`, `target-match`, `scope-availability`, `feature-state`, `enablement`, `settings-inputs`, `managed-policy` | partially documented; order among non-identical applicable files is unspecified | `github.copilot.cli.instructions`, `github.copilot.instructions.support` |
| `copilot.cli.skills.selection` | GitHub Copilot / CLI | `select-first` | Resolve the first same-name skill in the documented project, inherited, personal, plugin, custom, built-in, and remote source order; a same-name skill outranks a legacy command | `copilot.behavior.cli.commands`; `copilot.behavior.cli.skills`; `copilot.behavior.cli.user.skills` | `surface`, `engine-version`, `runtime-cwd`, `repository-root`, `scope-availability`, `feature-state`, `enablement`, `selection`, `plugin-state`, `installation`, `external-runtime` | documented for skills; command anchor and ancestry are partially documented | `github.copilot.cli.reference` |
| `copilot.cli.agents.selection` | GitHub Copilot / CLI | `select-closest`, `select-first`, `unknown-order` | Within project layers, prefer the deepest ancestor and `.github/agents` over `.claude/agents`; keep project-versus-User selection as `documentation-conflict`; plugin agents remain the documented lowest source | `copilot.behavior.cli.agents`; `copilot.behavior.cli.user.agents` | `surface`, `engine-version`, `runtime-cwd`, `repository-root`, `scope-availability`, `enablement`, `selection`, `plugin-state`, `documentation-variant`, `managed-policy` | Canonical documentation status is `conflict`; runtime projection uses `documentation-conflict` for project versus User | `github.copilot.cli.configuration`, `github.copilot.cli.custom-agents`, `github.copilot.cli.plugins`, `github.copilot.cli.reference` |
| `copilot.cli.hooks.composition` | GitHub Copilot / CLI | `filter`, `append` | Compose every applicable policy, User, Repository, and plugin hook for the event; preserve the documented source append order and do not collapse same-event hooks into one winner | `copilot.behavior.cli.hooks`; `copilot.behavior.cli.plugins`; `copilot.behavior.cli.user.hooks` | `surface`, `engine-version`, `repository-root`, `scope-availability`, `feature-state`, `trust`, `approval`, `enablement`, `plugin-state`, `event`, `managed-policy` | conflict: the hooks reference runs every source's entries for one event while the configuration reference gives the repository `hooks` key a merge policy that overrides the user for the same key | `github.copilot.cli.configuration`, `github.copilot.hooks` |
| `copilot.cli.mcp.selection` | GitHub Copilot / CLI | `select-first`, `replace` | Resolve server sources in session-additional, plugin, workspace, then User order; among workspace files, definitions closer to the working directory take precedence, and `.mcp.json` takes precedence over `.github/mcp.json` in the same directory | `copilot.behavior.cli.mcp`; `copilot.behavior.cli.user.mcp` | `surface`, `engine-version`, `runtime-cwd`, `repository-root`, `scope-availability`, `trust`, `approval`, `enablement`, `selection`, `settings-inputs`, `plugin-state`, `external-runtime` | documented | `github.copilot.cli.reference`, `github.copilot.cli.mcp` |
| `copilot.cli.settings.precedence` | GitHub Copilot / CLI | `append`, `replace`, `merge-map`, `deduplicate`, `tighten-only` | Apply the documented defaults, managed, User, Repository, local, environment, and flag cascade using each key's own documented merge behavior — alternatives per key rather than sequential steps: replaced by the repository layer, merged so the repository overrides the user for the same key, a union the repository can add entries to and never remove from, or tighten-only, which the repository can enable and never disable. Two documented exceptions to the cascade: an MDM value of `disable` for `permissions.disableBypassPermissionsMode` always wins over a closer layer, and the repository's `model`, `effortLevel`, and `contextTier` overrides apply only when the working directory is trusted | `copilot.behavior.cli.settings`; `copilot.behavior.cli.user.settings` | `surface`, `engine-version`, `repository-root`, `scope-availability`, `trust`, `selection`, `settings-inputs`, `managed-policy` | documented | `github.copilot.cli.configuration` |
| `copilot.cli.plugins.activation` | GitHub Copilot / CLI | `select-first`, `filter` | Apply manifest/catalog recognition order only at an established root, then resolve registration, installation, enablement, and component-source state without treating authored metadata as active | `copilot.behavior.cli.plugins`; `copilot.behavior.cli.user.plugins` | `surface`, `engine-version`, `repository-root`, `scope-availability`, `trust`, `approval`, `enablement`, `selection`, `plugin-state`, `installation` | documented | `github.copilot.cli.plugins`, `github.copilot.plugins` |

### Cloud agent and hosted inputs

| Strategy ID | Tool / surface | Operation(s) | Runtime projection | Input behavior refs | Required condition facts | Documentation status | Official source refs |
|---|---|---|---|---|---|---|---|
| `copilot.cloud.instructions.layering` | GitHub Copilot / Cloud agent | `filter`, `select-closest`, `append`, `unknown-order` | Filter root, path-specific, nearest `AGENTS.md`, and root alternative instructions for the worked path; combine applicable Repository and organization layers in documented Repository-before-organization order, preserving the order among the combined repository files and the coexistence of the agent-instruction alternatives as unknown. Do not project hosted personal Chat instructions into Cloud agent | `copilot.behavior.cloud.instructions.agents`; `copilot.behavior.cloud.instructions.alternatives`; `copilot.behavior.cloud.instructions.path`; `copilot.behavior.cloud.instructions.repository`; `copilot.behavior.cloud.organization-instructions` | `surface`, `engine-version`, `repository-root`, `worked-path`, `target-match`, `scope-availability`, `feature-state`, `managed-policy`, `external-runtime` | partially documented; order among the combined repository files and the agent-instruction alternatives is not established | `github.copilot.cloud.instructions`, `github.copilot.instructions.support` |
| `copilot.cloud.agents.selection` | GitHub Copilot / Cloud agent | `select-first`, `deduplicate` | Select same-name agent profiles in Repository, organization, then enterprise order and deduplicate the documented filename identity | `copilot.behavior.cloud.agents`; `copilot.behavior.cloud.organization-agents` | `surface`, `engine-version`, `repository-root`, `scope-availability`, `selection`, `external-runtime` | documented | `github.copilot.custom-agents` |
| `copilot.cloud.skills.selection` | GitHub Copilot / Cloud services | `filter`, `select-first`, `unknown-order` | Progressively load relevant Repository and relayed remote skills; retain collision behavior as unknown where the Cloud sources do not specify it | `copilot.behavior.cloud.remote-skills`; `copilot.behavior.cloud.skills` | `surface`, `engine-version`, `repository-root`, `worked-path`, `scope-availability`, `feature-state`, `enablement`, `selection`, `external-runtime` | partially documented: exact remote collision behavior is incomplete | `github.copilot.cli.reference`, `github.copilot.skills` |
| `copilot.cloud.hooks.composition` | GitHub Copilot / Cloud agent | `filter`, `append` | Use applicable Repository hook files available in the ephemeral clone; do not import local User hooks by analogy | `copilot.behavior.cloud.hooks` | `surface`, `engine-version`, `repository-root`, `scope-availability`, `feature-state`, `trust`, `approval`, `enablement`, `event`, `external-runtime` | documented | `github.copilot.hooks` |
| `copilot.cloud.mcp.selection` | GitHub Copilot / Cloud agent | `replace` | Resolve out-of-box servers, then custom-agent servers, then Repository settings, each level able to override settings from the previous | `copilot.behavior.cloud.mcp` | `surface`, `engine-version`, `scope-availability`, `trust`, `approval`, `enablement`, `selection`, `settings-inputs`, `agent-context`, `external-runtime` | partially documented; the page fixes the processing order and later-level override but neither the override unit nor any cross-level merge rule, and inputs are hosted rather than local `.mcp.json` files | `github.copilot.custom-agents` |
| `copilot.cloud.plugins.activation` | GitHub Copilot / Cloud and hosted plugin state | `filter`, `select-first` | Keep authored recommendation, known marketplace, installation, hosted availability, enablement, and selected component state distinct before calling a plugin active | `copilot.behavior.cloud.plugins` | `surface`, `engine-version`, `repository-root`, `scope-availability`, `trust`, `approval`, `enablement`, `selection`, `plugin-state`, `installation`, `external-runtime` | documented; activation depends on unavailable hosted state | `github.copilot.plugins`, `vscode.copilot.plugins` |

<a id="claude-code"></a>

## Claude Code strategies

The shared strategy IDs below do not erase surface differences. `surface` and
`engine-version` distinguish CLI, VS Code, and JetBrains; a row marked CLI-full or
IDE-conditional cannot be projected onto another surface merely because the settings
hierarchy is shared.

| Strategy ID | Tool / surface | Operation(s) | Runtime projection | Input behavior refs | Required condition facts | Documentation status | Official source refs |
|---|---|---|---|---|---|---|---|
| `claude.instructions.layering` | Claude Code / shared CLI, VS Code, and JetBrains core | `append` | Compose User, ancestor, launch-directory, and lazily discovered descendant instructions broad-to-narrow; at one directory place regular before local. Preserve semantic conflicts as model-interpreted rather than declaring a hard setting-style winner | `claude.behavior.repo.instructions.ancestor`; `claude.behavior.repo.instructions.descendant`; `claude.behavior.repo.instructions.launch`; `claude.behavior.user.instructions` | `surface`, `engine-version`, `runtime-cwd`, `worked-path`, `scope-availability`, `feature-state`, `settings-inputs`, `managed-policy` | documented; conflicting natural-language instructions have no deterministic semantic winner | `anthropic.claude-code.memory.locations-load`, `anthropic.claude-code.sdk.setting-sources` |
| `claude.rules.layering` | Claude Code / shared core | `filter`, `append` | Add applicable User and project rule layers; apply unconditional rules with project instructions and activate a `paths` rule when a matching file is read | `claude.behavior.repo.rules`; `claude.behavior.user.rules` | `surface`, `engine-version`, `runtime-cwd`, `worked-path`, `target-match`, `scope-availability`, `settings-inputs`, `documentation-variant`, `managed-policy` | partially documented: ancestor `paths` base and descendant rule discovery are unclear | `anthropic.claude-code.memory.locations-load` |
| `claude.skills.selection` | Claude Code / CLI full, IDE subset | `retain-all`, `select-closest` | Within one root, keep every same-name skill available — a nested one under a directory-qualified command (Claude Code 2.1.178+, changelog § 2.1.178) — and invoke the variant matching the files being worked on when the unqualified name is used; the enterprise-over-User-over-project precedence is a rule between levels, plugin skills stay namespaced, and a same-name skill wins over a legacy command | `claude.behavior.repo.skills`; `claude.behavior.user.skills` | `surface`, `engine-version`, `runtime-cwd`, `repository-root`, `worked-path`, `scope-availability`, `feature-state`, `enablement`, `selection`, `plugin-state`, `managed-policy` | partially documented: nested-clash retention is version-anchored at 2.1.178+ (changelog § 2.1.178), while automatic working-context invocation carries no version of its own — the skills page states it without one, and the changelog entry that introduced nested loading states it together with the retention; exact IDE surface availability remains conditional | `anthropic.claude-code.changelog.nested-skill-discovery`, `anthropic.claude-code.ide.shared-differences`, `anthropic.claude-code.skills.locations-discovery` |
| `claude.commands.selection` | Claude Code / CLI full, IDE subset | `select-first` | Integrate recursive legacy commands into the skill command namespace; a same-name skill wins and subdirectories form namespaces | `claude.behavior.repo.commands`; `claude.behavior.repo.skills`; `claude.behavior.user.commands`; `claude.behavior.user.skills` | `surface`, `engine-version`, `runtime-cwd`, `scope-availability`, `feature-state`, `selection`, `documentation-variant` | partially documented: complete ancestor/lazy traversal is not stated independently | `anthropic.claude-code.changelog.legacy-command-nesting`, `anthropic.claude-code.skills.locations-discovery` |
| `claude.agents.selection` | Claude Code / subagent-capable runtime | `select-first`, `select-closest`, `unknown-order` | Resolve same-name agents in managed, session `--agents`, closest project layer, User, then plugin order; preserve a same-tree duplicate as unknown | `claude.behavior.repo.agents`; `claude.behavior.user.agents` | `surface`, `engine-version`, `runtime-cwd`, `repository-root`, `scope-availability`, `feature-state`, `enablement`, `selection`, `settings-inputs`, `plugin-state`, `documentation-variant`, `managed-policy` | documented across scopes; same-tree duplicate order is unknown | `anthropic.claude-code.subagents.scope-context` |
| `claude.agent-context.composition` | Claude Code / main, custom, and built-in agent contexts | `concatenate`, `filter`, `replace` | For a normal custom agent create fresh conversation context with documented instruction, rule, memory, git, and preloaded-skill inputs; for `context: fork` inherit the parent conversation. Apply built-in omissions, skill/tool conditions, selected memory scope, and the current nested-spawn depth limit | `claude.behavior.repo.agent-memory.local`; `claude.behavior.repo.agent-memory.project`; `claude.behavior.repo.agents`; `claude.behavior.repo.instructions.ancestor`; `claude.behavior.repo.instructions.descendant`; `claude.behavior.repo.instructions.launch`; `claude.behavior.repo.rules`; `claude.behavior.repo.skills`; `claude.behavior.user.agent-memory`; `claude.behavior.user.agents`; `claude.behavior.user.auto-memory` | `surface`, `engine-version`, `runtime-cwd`, `scope-availability`, `feature-state`, `enablement`, `selection`, `agent-context`, `content-limits`, `tool-availability`, `external-runtime` | documented; unavailable Global/runtime inputs remain explicit unknowns | `anthropic.claude-code.memory.locations-load`, `anthropic.claude-code.subagents.scope-context` |
| `claude.settings.precedence` | Claude Code / shared CLI, VS Code, and JetBrains core | `replace`, `merge-map`, `concatenate`, `deduplicate` | Resolve managed, CLI, exact-launch-`cwd` local, exact-launch-`cwd` project, then User settings; use documented scalar, array, object, and exceptional-key merge modes | `claude.behavior.repo.settings.local`; `claude.behavior.repo.settings.shared`; `claude.behavior.user.settings` | `surface`, `engine-version`, `runtime-cwd`, `scope-availability`, `trust`, `approval`, `selection`, `settings-inputs`, `managed-policy` | `partially-documented` | `anthropic.claude-code.large-codebases.start-directory`, `anthropic.claude-code.sdk.setting-sources`, `anthropic.claude-code.settings.scopes-precedence` |
| `claude.hooks.additive` | Claude Code / hook-capable CLI and IDE runtime | `filter`, `append`, `select-first` | Execute every applicable matching hook of every active source, a closer settings level adding to the broader ones rather than replacing them; retain trust, managed policy, `disableAllHooks`, and plugin enablement as the filter that decides which sources are active; resolve a tool call the vendor's own way, where an explicit deny returned by a hook takes precedence. Registration lifetime — a subagent's while it runs, a skill's from its invocation — stays a condition rather than a composition | `claude.behavior.repo.agents`; `claude.behavior.repo.hooks-contained`; `claude.behavior.repo.plugin`; `claude.behavior.repo.settings.local`; `claude.behavior.repo.settings.shared`; `claude.behavior.repo.skills`; `claude.behavior.user.plugins`; `claude.behavior.user.settings` | `surface`, `engine-version`, `scope-availability`, `feature-state`, `trust`, `approval`, `enablement`, `selection`, `settings-inputs`, `plugin-state`, `event`, `managed-policy` | documented; managed hook gates and feature availability remain conditions | `anthropic.claude-code.hooks.locations-resolution`, `anthropic.claude-code.plugins.components-scopes`, `anthropic.claude-code.settings.scopes-precedence` |
| `claude.mcp.selection` | Claude Code / CLI full, VS Code partial, other IDE conditional | `select-first`, `replace`, `filter` | Select an entire duplicate server entry in local, project, User, plugin, then connector order — the three scopes match duplicates by name, while plugins and connectors match by endpoint (the same URL or command as a server above). A subagent inherits the selected parent tools by default, then applies tool filters; inline agent servers live only for that agent | `claude.behavior.repo.agents`; `claude.behavior.repo.mcp`; `claude.behavior.repo.plugin`; `claude.behavior.user.mcp-state`; `claude.behavior.user.plugins` | `surface`, `engine-version`, `runtime-cwd`, `project-root`, `scope-availability`, `trust`, `approval`, `enablement`, `selection`, `settings-inputs`, `plugin-state`, `agent-context`, `tool-availability`, `managed-policy`, `external-runtime` | documented; exact project-root selection remains partially specified | `anthropic.claude-code.ide.shared-differences`, `anthropic.claude-code.mcp.scopes-precedence`, `anthropic.claude-code.subagents.scope-context` |
| `claude.output-style.selection` | Claude Code / CLI documented, IDE conditional | `select-closest`, `replace` | Resolve a same-name custom style to the project layer closest to the working directory, among the nested project directories alone — the User, project, and managed-policy levels the page lists carry no documented order between them — then apply the style selected by settings/session state, which a plugin style marked `force-for-plugin` overrides while its plugin is enabled, first such plugin loaded winning | `claude.behavior.repo.output-style`; `claude.behavior.repo.plugin`; `claude.behavior.user.output-style`; `claude.behavior.user.plugins` | `surface`, `engine-version`, `runtime-cwd`, `repository-root`, `scope-availability`, `feature-state`, `selection`, `settings-inputs`, `plugin-state`, `managed-policy` | documented; surface availability is conditional, and no order between the three levels is documented | `anthropic.claude-code.ide.shared-differences`, `anthropic.claude-code.output-styles.locations` |
| `claude.plugins.activation` | Claude Code / CLI plugin management and shared IDE configuration | `filter`, `select-first`, `replace`, `append` | Establish a plugin root through registration, installation, explicit CLI input, or exact-`cwd` skills-directory plugin discovery; then apply enablement scope, trust, manifest defaults/overrides, and component availability. Never treat an arbitrary matching manifest/catalog as active | `claude.behavior.repo.marketplace`; `claude.behavior.repo.plugin`; `claude.behavior.repo.settings`; `claude.behavior.repo.skills-directory-plugin`; `claude.behavior.user.plugins`; `claude.behavior.user.settings` | `surface`, `engine-version`, `runtime-cwd`, `scope-availability`, `feature-state`, `trust`, `approval`, `enablement`, `selection`, `settings-inputs`, `plugin-state`, `installation`, `managed-policy`, `external-runtime` | documented; activation requires external runtime state | `anthropic.claude-code.ide.shared-differences`, `anthropic.claude-code.marketplaces.catalog-sources`, `anthropic.claude-code.plugins.components-scopes`, `anthropic.claude-code.settings.scopes-precedence` |

## OpenAI Codex strategies

“Local clients” below means Codex CLI, the Codex IDE extension, and the ChatGPT desktop
Codex host. Hosted ChatGPT Work does not consume these local files.

| Strategy ID | Tool / surface | Operation(s) | Runtime projection | Input behavior refs | Required condition facts | Documentation status | Official source refs |
|---|---|---|---|---|---|---|---|
| `codex.instructions.layering` | OpenAI Codex / local clients | `select-first`, `concatenate`, `filter` | Select the first non-empty User fallback, then at each project-root-to-`cwd` directory select the first non-empty `AGENTS.override.md`, `AGENTS.md`, or configured fallback in documented filename order; concatenate broad-to-narrow until the upstream project-document byte budget is exhausted | `codex.behavior.repo.instructions`; `codex.behavior.user.instructions` | `surface`, `engine-version`, `runtime-cwd`, `project-root`, `scope-availability`, `selection`, `settings-inputs`, `content-limits`, `managed-policy`, `instruction-byte-budget` | documented; excluded higher-scope settings can leave fallback names and budget unknown | `openai.codex.agents-md` |
| `codex.skills.discovery` | OpenAI Codex / local clients | `retain-all`, `unknown-order` | Discover skills from active Repository, User, admin, and system scopes; same-name skills are not merged and both remain available in skill selectors. The cited section establishes no precedence or ordering among the scopes, so no selection operation is claimed | `codex.behavior.repo.skills`; `codex.behavior.user.skills` | `surface`, `engine-version`, `runtime-cwd`, `repository-root`, `scope-availability`, `feature-state`, `enablement`, `selection`, `managed-policy` | partially documented | `openai.codex.skills` |
| `codex.agents.inheritance` | OpenAI Codex / local spawned sessions | `select-first`, `merge-map`, `replace` | Select a custom or built-in agent configuration, a custom agent whose name matches a built-in one taking precedence over it, then overlay the child file on the parent session for documented inheritable fields, and reapply live parent sandbox and approval overrides. Do not infer child `AGENTS.md` inheritance, and do not infer an order between the personal and project custom-agent scopes, which the page does not establish | `codex.behavior.repo.agents`; `codex.behavior.user.agents` | `surface`, `engine-version`, `project-root`, `scope-availability`, `trust`, `approval`, `selection`, `settings-inputs`, `agent-context`, `documentation-variant`, `tool-availability`, `managed-policy` | partially documented: project traversal and child instruction inheritance are incomplete | `openai.codex.config-basic`, `openai.codex.subagents` |
| `codex.config.precedence` | OpenAI Codex / local clients | `merge-map`, `replace`, `select-closest` | Resolve User/profile/CLI and every trusted project config layer from root to `cwd`; use the closest applicable value for the same key and resolve relative paths from the containing `.codex` directory | `codex.behavior.repo.config`; `codex.behavior.user.config` | `surface`, `engine-version`, `runtime-cwd`, `project-root`, `scope-availability`, `trust`, `approval`, `selection`, `settings-inputs`, `managed-policy` | documented | `openai.codex.config-basic` |
| `codex.hooks.additive` | OpenAI Codex / local hook-capable clients | `filter`, `append` | Execute all matching User, project, and plugin hooks; retain both file and inline hooks at one layer and the documented warning rather than selecting one | `codex.behavior.plugin.manifest`; `codex.behavior.repo.hooks`; `codex.behavior.user.hooks` | `surface`, `engine-version`, `runtime-cwd`, `project-root`, `scope-availability`, `feature-state`, `trust`, `approval`, `enablement`, `plugin-state`, `event`, `managed-policy` | documented | `openai.codex.config-basic`, `openai.codex.hooks` |
| `codex.rules.resolution` | OpenAI Codex / local clients | `filter`, `select-first` | Evaluate direct `.rules` files from active User and trusted project layers and combine matching decisions with the documented restrictive result; do not infer nested-directory recursion | `codex.behavior.repo.rules`; `codex.behavior.user.rules` | `surface`, `engine-version`, `runtime-cwd`, `project-root`, `scope-availability`, `feature-state`, `trust`, `approval`, `enablement`, `selection`, `documentation-variant`, `managed-policy` | partially documented; experimental and nested recursion is unspecified | `openai.codex.rules` |
| `codex.mcp.configuration` | OpenAI Codex / local clients | `merge-map`, `replace` | Resolve `[mcp_servers.*]` through active User/profile/CLI/project config precedence; retain trust, enablement, and server availability separately and never connect during inspection | `codex.behavior.repo.mcp`; `codex.behavior.user.config` | `surface`, `engine-version`, `runtime-cwd`, `project-root`, `scope-availability`, `trust`, `approval`, `enablement`, `selection`, `settings-inputs`, `managed-policy`, `external-runtime` | documented | `openai.codex.config-basic`, `openai.codex.mcp` |
| `codex.plugins.activation` | OpenAI Codex / ChatGPT desktop and plugin-management CLI; plugin-capable local clients | `filter`, `select-first` | Discover exact repository marketplaces or a personal marketplace, establish a plugin root through a selected marketplace/installation, require its manifest, then retain installation, enablement, trust, and component state separately. Do not project local activation to hosted ChatGPT Work | `codex.behavior.plugin.manifest`; `codex.behavior.repo.marketplace`; `codex.behavior.user.plugins` | `surface`, `engine-version`, `repository-root`, `scope-availability`, `feature-state`, `trust`, `approval`, `enablement`, `selection`, `plugin-state`, `installation`, `external-runtime` | documented; activation remains external-state dependent | `openai.codex.plugins` |

## Normative relationship-only registry

Every rule in this section has class `relationship-only` and **MUST NOT authorize a
read**. A relationship is extracted only from an independently accepted origin candidate.
It retains the origin candidate provenance and `matchedPath`; a relative target is based
on that origin. A target independently admitted by a static or bounded-derived rule may
be linked to the relationship, but its read authority and provenance come solely from
that other rule. Relationships never seed a scan, a derivation, a network request, a
plugin install, hook execution, or MCP connection.

| Rule ID | Origin | Behavior refs | Recorded target or runtime edge | Required conditions / status | Policy refs | Evidence |
|---|---|---|---|---|---|---|
| `copilot.relationship.prompt-reference` | Accepted VS Code prompt | `copilot.behavior.vscode.prompts` | Markdown link or `#file` reference | Record only; never open the target through this edge | FR-005, FR-007, FR-019, FR-020, FR-022, FR-024, FR-033, QR-001, QR-005 | `vscode.copilot.prompts`, `vscode.copilot.settings` |
| `copilot.relationship.settings` | Accepted supported Copilot settings | `copilot.behavior.cli.plugins`; `copilot.behavior.cli.settings`; `copilot.behavior.vscode.plugins`; `copilot.behavior.vscode.settings` | `extraKnownMarketplaces`, named `enabledPlugins`, and contained hook/boolean facts | Record only; values never create a scan root or candidate | FR-005, FR-007, FR-019, FR-020, FR-022, FR-024, QR-001, QR-005 | `github.copilot.cli.configuration`, `github.copilot.cli.plugins`, `github.copilot.plugins`, `vscode.copilot.plugins`, `vscode.copilot.settings`, `vscode.settings` |
| `copilot.relationship.component` | Accepted skill, hook, plugin, or marketplace declaration | `copilot.behavior.cli.hooks`; `copilot.behavior.cli.plugins`; `copilot.behavior.cli.skills`; `copilot.behavior.cloud.hooks`; `copilot.behavior.cloud.plugins`; `copilot.behavior.vscode.hooks`; `copilot.behavior.vscode.plugins`; `copilot.behavior.vscode.skills` | Skill resource, hook command, or arbitrary plugin agent/skill/hook/MCP/LSP component path | Record only; arbitrary component files and scripts remain unread | FR-005, FR-007, FR-019, FR-020, FR-021, FR-022, FR-024, FR-029, QR-001, QR-005 | `github.copilot.cli.configuration`, `github.copilot.cli.plugins`, `github.copilot.cli.reference`, `github.copilot.hooks`, `github.copilot.plugins`, `github.copilot.skills`, `vscode.copilot.custom-agents`, `vscode.copilot.customization`, `vscode.copilot.hooks`, `vscode.copilot.plugins`, `vscode.copilot.settings`, `vscode.copilot.skills` |
| `copilot.relationship.agent-context` | Accepted custom-agent profile | `copilot.behavior.cli.agents`; `copilot.behavior.cloud.agents`; `copilot.behavior.vscode.agents` | Body, surface, tool, model, invocation, handoff, link, hook, instruction-context, skill-preload, and surface-specific MCP edges | VS Code, CLI, and Cloud remain separate. Unknown Cloud/CLI context order and skill preload stay unknown | FR-005, FR-007, FR-008, FR-009, FR-019, FR-020, FR-021, FR-022, FR-029, FR-037, QR-001, QR-005 | `github.copilot.cli.configuration`, `github.copilot.cli.custom-agents`, `github.copilot.cli.plugins`, `github.copilot.cli.reference`, `github.copilot.custom-agents`, `vscode.copilot.custom-agents`, `vscode.copilot.settings` |
| `claude.relationship.component` | Accepted skill, hook, plugin, or marketplace declaration | `claude.behavior.repo.hooks-contained`; `claude.behavior.repo.marketplace`; `claude.behavior.repo.plugin`; `claude.behavior.repo.skills` | Skill resource, hook command, remote plugin source, or default/custom plugin component path | Record only; retain each component type's documented base and add/replace semantics | FR-005, FR-007, FR-019, FR-020, FR-021, FR-022, FR-024, FR-029, QR-001, QR-005 | `anthropic.claude-code.hooks.locations-resolution`, `anthropic.claude-code.ide.shared-differences`, `anthropic.claude-code.large-codebases.start-directory`, `anthropic.claude-code.marketplaces.catalog-sources`, `anthropic.claude-code.plugins.components-scopes`, `anthropic.claude-code.skills.locations-discovery` |
| `claude.relationship.agent-reference` | Accepted settings, skill, command, or subagent | `claude.behavior.repo.agent-memory.local`; `claude.behavior.repo.agent-memory.project`; `claude.behavior.repo.agents`; `claude.behavior.repo.commands`; `claude.behavior.repo.settings`; `claude.behavior.repo.skills`; `claude.behavior.user.agent-memory` | Selected main agent, preload, named MCP server/tool, agent reference, or selected User `<claude-config-dir>/agent-memory/<name>/`, project `<project-root>/.claude/agent-memory/<name>/`, or local `<project-root>/.claude/agent-memory-local/<name>/` scope plus the 200-line / 25-KB startup-prefix limit | Resolve only an independently admitted target; excluded memory remains a source fact | FR-005, FR-007, FR-009, FR-019, FR-020, FR-021, FR-022, FR-024, FR-029, QR-001, QR-005 | `anthropic.claude-code.changelog.legacy-command-nesting`, `anthropic.claude-code.large-codebases.start-directory`, `anthropic.claude-code.sdk.setting-sources`, `anthropic.claude-code.settings.scopes-precedence`, `anthropic.claude-code.skills.locations-discovery`, `anthropic.claude-code.subagents.scope-context` |
| `claude.relationship.agent-context` | Accepted custom subagent, skill, or command | `claude.behavior.repo.agents`; `claude.behavior.repo.commands`; `claude.behavior.repo.skills` | Fresh-versus-fork context, instruction/rule/memory inputs, conditional Skill-tool discovery, built-in omissions, and nested-spawn facts | A named preload supplies full content only when the skill exists, is enabled, and permits model invocation. Preserve unavailable Global/runtime inputs; current nested spawn is supported to the documented limit | FR-005, FR-007, FR-008, FR-009, FR-019, FR-020, FR-022, FR-029, QR-001, QR-005 | `anthropic.claude-code.changelog.legacy-command-nesting`, `anthropic.claude-code.large-codebases.start-directory`, `anthropic.claude-code.skills.locations-discovery`, `anthropic.claude-code.subagents.scope-context` |
| `claude.relationship.agent-mcp` | Accepted custom subagent | `claude.behavior.repo.agents`; `claude.behavior.repo.mcp`; `claude.behavior.user.mcp-state` | Default parent MCP-tool inheritance, inline versus named-parent server origin, and tool filters | Record only; never connect. Strict, bare, and managed restrictions can remain unavailable facts | FR-005, FR-007, FR-009, FR-019, FR-020, FR-021, FR-022, FR-029, QR-001, QR-005 | `anthropic.claude-code.directory.file-reference`, `anthropic.claude-code.ide.shared-differences`, `anthropic.claude-code.mcp.scopes-precedence`, `anthropic.claude-code.subagents.scope-context` |
| `codex.relationship.config-path` | Accepted Codex config | `codex.behavior.repo.agents`; `codex.behavior.repo.config`; `codex.behavior.repo.skills` | `agents.<name>.config_file`, `model_instructions_file`, `experimental_compact_prompt_file`, or `skills.config[].path` | Record only; these arbitrary targets remain unread in the initial release | FR-005, FR-007, FR-009, FR-019, FR-020, FR-022, FR-024, FR-029, QR-001, QR-005 | `openai.codex.config-basic`, `openai.codex.skills`, `openai.codex.subagents` |
| `codex.relationship.component` | Accepted skill, hook, plugin, or marketplace declaration | `codex.behavior.plugin.manifest`; `codex.behavior.repo.hooks`; `codex.behavior.repo.marketplace`; `codex.behavior.repo.skills` | Plugin skill/MCP/app/hook path, skill resource/asset, hook command, remote source, or conditional default `hooks/hooks.json` | Record only. Synthesize the default hook relation only when the manifest has no explicit `hooks` field; an explicit field replaces that default | FR-005, FR-007, FR-019, FR-020, FR-021, FR-022, FR-024, FR-029, QR-001, QR-005 | `openai.codex.config-basic`, `openai.codex.hooks`, `openai.codex.plugins`, `openai.codex.skills` |
| `codex.relationship.agent-context` | Accepted custom-agent TOML | `codex.behavior.repo.agents`; `codex.behavior.repo.config`; `codex.behavior.repo.mcp`; `codex.behavior.repo.skills`; `codex.behavior.user.config`; `codex.behavior.user.skills` | Parent config/skill/MCP inheritance edges for omitted fields, sandbox/approval reapplication, local-versus-hosted fact, and unknown child `AGENTS.md` inheritance | Record only; resolve only independently accepted parent declarations | FR-005, FR-007, FR-008, FR-009, FR-019, FR-020, FR-021, FR-022, FR-024, FR-029, FR-035, QR-001, QR-005 | `openai.codex.config-basic`, `openai.codex.mcp`, `openai.codex.skills`, `openai.codex.subagents` |
| `shared.relationship.runtime` | Any accepted recognition needing unavailable runtime data | `claude.behavior.repo.mcp`; `claude.behavior.user.mcp-state`; `claude.behavior.user.plugins`; `claude.behavior.user.settings`; `codex.behavior.repo.mcp`; `codex.behavior.user.config`; `codex.behavior.user.plugins`; `copilot.behavior.cloud.mcp`; `copilot.behavior.cloud.organization-agents`; `copilot.behavior.cloud.organization-instructions`; `copilot.behavior.cloud.plugins`; `copilot.behavior.cloud.remote-skills` | MCP-server-provided instructions, hosted settings, organization policy, installed-service state, and other runtime-only inputs | Source fact only; never connect, fetch, authenticate, or create a local candidate | FR-005, FR-007, FR-009, FR-014, FR-018, FR-019, FR-021, FR-022, FR-031, QR-001, QR-005 | `anthropic.claude-code.directory.file-reference`, `anthropic.claude-code.ide.shared-differences`, `anthropic.claude-code.mcp.scopes-precedence`, `anthropic.claude-code.plugins.components-scopes`, `anthropic.claude-code.settings.scopes-precedence`, `github.copilot.cli.reference`, `github.copilot.cloud.instructions`, `github.copilot.custom-agents`, `github.copilot.instructions.support`, `github.copilot.plugins`, `github.copilot.skills`, `openai.codex.config-basic`, `openai.codex.mcp`, `openai.codex.plugins`, `vscode.copilot.plugins` |

An authored relationship displays its exact validated source slice without masking,
redaction, reveal state, or environment-variable substitution. A separate validated semantic/
structural path form may determine relationship status, but never replaces that displayed
literal. A registry-defined documented default such as the conditional Codex
`hooks/hooks.json` relation has no authored target at all: its DTO uses
`targetOrigin: documented-default` and `authoredTarget: null`, and the UI labels its registry-defined
path as a documented default. Deduplication uses the closed origin identity plus target
identity; it never discards distinct authored occurrences or candidate provenance.

## Shared non-read exclusions

These shared rules are also normative here. They are `excluded`, not relationships, and
never authorize a read. Vendor-specific excluded rules remain normative in their vendor
contracts and are not duplicated here.

| Rule ID | Excluded input | Behavior refs | Required retained fact | Policy refs | Evidence |
|---|---|---|---|---|---|
| `shared.excluded.managed-remote-state` | Managed, organization, hosted, remote, credential, log, cache, session, runtime-state, plugin-installation, and service-side files or values | `claude.behavior.user.mcp-state`; `claude.behavior.user.plugins`; `codex.behavior.user.plugins`; `copilot.behavior.cloud.mcp`; `copilot.behavior.cloud.organization-agents`; `copilot.behavior.cloud.organization-instructions`; `copilot.behavior.cloud.plugins`; `copilot.behavior.cloud.remote-skills` | Record only the source category and its possible effect on a strategy; never create a local path | FR-009, FR-013, FR-014, FR-018, FR-019, FR-021, FR-022, FR-031, QR-001, QR-003, QR-005 | `anthropic.claude-code.directory.file-reference`, `anthropic.claude-code.mcp.scopes-precedence`, `anthropic.claude-code.plugins.components-scopes`, `anthropic.claude-code.settings.scopes-precedence`, `github.copilot.cli.reference`, `github.copilot.cloud.instructions`, `github.copilot.custom-agents`, `github.copilot.instructions.support`, `github.copilot.plugins`, `github.copilot.skills`, `openai.codex.config-basic`, `openai.codex.mcp`, `openai.codex.plugins`, `vscode.copilot.plugins` |

## Registry completeness

This contract defines exactly 39 strategy IDs referenced by the three vendor contracts:
20 GitHub Copilot strategies, 11 Claude Code strategies, and 8 OpenAI Codex strategies.
Rows explicitly marked with no strategy in a vendor contract—such as explicit prompt
invocation or excluded experimental surfaces—do not acquire an invented strategy here.
It also defines all 14 relationship-only IDs from the staged allowlist/vendor contracts
and the one shared non-read exclusion. Any future reference to a new ID requires an English
and Japanese definition in this contract in the same change.
