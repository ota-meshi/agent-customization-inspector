# 機能仕様: Agentカスタマイズの調査

[English](spec.md)

**Feature Branch**: `dev`

**Created**: 2026-07-15

**Status**: Ready for Implementation

**Input**: ユーザー説明: 「提供されたローカルのプロダクト説明からAgent Customization Inspectorの初期プロダクトを定義し、後で削除する一時的な入力へのリンクは残さない。」

## Clarifications

### Session 2026-07-15

- Q: Repository sourceのrootを何によって決定するか？ → A: 選択済みRepository rootとする。`--root`を省略した場合は1回の`process.cwd()` captureが返すexact process working directory、指定した場合はその1つの`--root`値を使用する。値がabsoluteならそのまま保持し、relativeならactive platformの`node:path.resolve`でcaptureに対してlexicalにresolveする。Root selection自体はfilesystem/network I/Oを0件とし、`package.json.bin`はbootstrap readなしでpackaged CLIへ直接mapする。RejectされたselectionはInvocationの`cwd`、`--root`、Repository rootからderiveした、またはそれらを対象とするI/O、およびDNS、SMB、outbound-network callを0件とする。初期リリースでは別のrepository pickerもancestor rootの探索も行わない。
- Q: 最初のプロダクト範囲をどの用語で表すか？ → A: 仕様全体で「初期リリース」を使用する。
- Q: 発見したAgentカスタマイズ用ファイルを仕様で何と呼ぶか？ → A: 仕様全体で「カスタマイズファイル」を使用する。
- Q: 調査してよいfilesystem pathの限定集合を何と呼ぶか？ → A: 仕様全体で「調査対象パス一覧」を使用する。
- Q: Vendor lookup表とその根拠をどのように構成するか？ → A: Vendor lookup behaviorをInspector matcherおよびruntime compositionから分離し、製品ごとの文書、RepositoryとUser/Globalの別表、GitHub CopilotのVS Code/CLI/Cloud別表、ならびに保守する全行のstable official-source参照を使用する。

### Session 2026-07-16

- Q: 初期リリースにどのruntime実装制約を適用するか？ → A: 実行可能なapplication codeをすべてJavaScript/TypeScriptで実装する。CLI、local host、調査対象sourceのI/OはNode.jsの公開JavaScript API上で動作し、browserには生成済みJavaScriptとdeclarativeなHTML/CSS assetを渡す。Strict JSON manifest、documentation、license fileは有効なpackage dataとする。Rust、Node-APIその他のnative addon、platform別のprebuilt native binary、package lifecycleでのcompile、package lifecycleまたはruntimeでのartifact downloadは使用しない。
- Q: そのNode.js-only制約で、filesystem raceについてどの保証が可能か？ → A: 調査対象sourceのI/Oを1つのNode.js moduleへ集約し、Node.js公開APIが示すlinkとboundary failureを拒否し、root、ancestor、candidate path、open handle、read後のidentity、canonical location、metadata snapshotを比較し、不一致の検出時は候補byteをすべて破棄する。Node.jsが公開しplatformがenforceする場合は`O_NOFOLLOW`をfinal-componentの多層防御として使用する。これらの非原子的check間でsource rootまたはancestor、あるいは有効な`O_NOFOLLOW`がない場合のfinal path componentをraceさせる敵対的なlocal processはthreat modelから除外する。Node.js公開APIはsame-device mountやreparse behaviorをすべて公開することもできない。これらの残存riskとNode.jsまたはoperating systemによる解消pathは文書化し続ける。

### Session 2026-07-17

- Q: User-global rootをSourceとしてどのように表すか？ → A: memberごとに、admitされたGlobal rootを独立したGlobal Sourceとして表す。Codexは`CODEX_HOME`、Claudeは`CLAUDE_CONFIG_DIR`、Copilotは`COPILOT_HOME`、共有agent homeは導出した`.agents` path（FR-045）をrootとし、sessionは0から4つのGlobal Sourceを持つ。1つのSourceは正確に1つのrootを持ち、そのroot内の異なる種別のカスタマイズファイルはそれぞれ独立して表示する。
- Q: カスタマイズファイル内のリテラルcredentialと環境変数参照をどのように表示するか？ → A: リテラルな差分を確認できるよう、source text、表示対象の宣言済みmetadata値、authored relationship target、comparison contentはcredential maskingやreveal workflowを使わず、記述されたまま表示する。調査対象content内の環境変数参照はリテラルtextとして扱い、process上の値を解決または置換しない。文書化されたtool-home環境変数はGlobal rootを特定するためだけに使用する。ファイルは直接表示する — 警告・acknowledgement・注意書きはその前にも隣にも置かない（FR-027）。operational diagnosticとlogにはsource valueを複製しない。
- Q: 明示的な再scanが致命的に失敗した場合、以前のinventoryをどう扱うか？ → A: 最後に正常commitされたsnapshotを表示したまま残し、再scan失敗によりstaleであることを示し、実行可能なfailure diagnosticを表示する。部分結果を含む失敗scanの未commit結果はすべて破棄し、後続の再scanが正常commitされた場合にだけ保持中のsnapshotを置換する。
- Q: Repository sourceとGlobal sourceに一貫して適用するpath用語は何か？ → A: カスタマイズファイルを所有するSource rootからのpathを「Source-relative path」と呼ぶ。Repository Sourceでは選択済みRepository rootからの相対path、各Global Sourceではそのtoolについてadmitされたhome rootからの相対pathとする。「repository-relative path」はRepository Sourceだけを説明する場合に限って使用する。
- Q: SC-006では何をcritical usability issueとして数えるか？ → A: 禁止された支援なしでprimary workflowを完了できなくする問題、または意図しない実行、調査対象sourceの変更、FR-022で定義した禁止対象のdirect product-issued outbound requestまたはMCP connection、別machineへの調査content露出などのunsafe behaviorを起こす問題とする。FR-022が認可するexactな2つのinternal loopback HTTP classはoutboundでもMCP connectionでもなく、このautomatic eventには含めない。記録済みpre-mounted/mapped sourceのOS-mediated trafficは、このautomatic connection eventではなくFR-022のplatform/environment limitationとして扱う。

### Session 2026-07-18


### Session 2026-07-19

直前のreviewer回答にある「reuse」はhuman、collector process/component-run identity、case assignmentのreuseを指す。Literal reviewer slot labelとsanitized terminal-equipment surfaceはdrain/resetしてlater caseへfresh mappingしてよい。

- Q: Child process禁止はproductのbrowser起動helperも禁止するか？ → A: いいえ。FR-001に基づくstartup時のbrowser起動は、productがchild processを開始する2つのsurfaceの一方であり — もう一方はreader自身が明示的に要求するopen-in-editorで、同じ規定のもとで追加された（FR-022） — こちらが渡す引数はすべてinspectionが存在する前に閉じている。 *(2026-08-22修正: 明示的なopen要求が2つ目のsurfaceである。1つしか存在しなかった時点の答えが「唯一」と述べていた。)*macOSでは、固定のprocess一覧probe（`ps cax`）と、OSの`osascript` automation hostを通じたproduct-authoredな固定のtab再利用scriptをspawnしてよく、これらが受け取るのはその固定script、closedなloopback URL、固定のChromium系application一覧のmemberだけである。それ以外のすべての場合は、維持されている`open` packageを通じて固定のOS browser起動helperをspawnすることで、cross-platformなhelper解決はそのpackageのpolicyに留まり、渡す引数はclosedなloopback URLの正確に1つである。spawnされるどのprocessもinspection由来のcontent/path、authored value、user-supplied commandを受け取らない。各childはlaunch environmentを変更なしで継承する — productはどの環境変数にもinspection由来の値を書き込まないため、environmentを通じてinspection由来の何かがchildへ届くことはなく、environmentをfilterする自作launcherは、何も守らないまま維持されたpackageのplatform policyを再実装することになる。`xdg-open`が`$BROWSER`を参照するように、platform helperがuser自身のenvironment設定を尊重するのは、user自身のshellにあるuser preferenceの適用であって、inspection由来のinputではない。自動起動を無効にした場合、非対応の場合、または失敗した場合もinspectionを利用可能にする。Customization fileのdiscovery、read、parse、display、comparison、relationship処理はchild processを開始してはならない。 _(amended 2026-08-19: macOSのtab再利用がprobeとscriptのprocessを追加したため、回答は単一のhelperではなくclosedなspawn集合を記載する（T1098）。)_
- Q: FR-007でrelevantまたはknownとするdeclared metadataとrelationshipはどれか？ → A: Presentation上の各supported customization file typeは、exactな`(tool, kind)`とadmission済みsource formの組とする。維持管理するsupported-customization文書で、各rowのclosedなrelationship setとexact source-form extractor applicabilityを列挙する。Rowはdeclared fieldを列挙しない。Skillの宣言はfileが書いたkeyで公開され、authored keyの集合は閉じていないからである。Initial releaseは両gateを満たすauthored relationshipだけを表示し、未記載のrelationshipを推論したりsource form間でpromoteしたりしない _(2026-08-02 修正: authored keyの集合は閉じていないため、relationshipだけに絞った。)_。
- Q: Originating customization fileを持たない、文書化済みruntimeまたはhosted inputをどう表現するか？ → A: 表現しない。製品は見つけたカスタマイズファイルを報告する。Vendorがfile以外のbehaviorについて何を文書化しているかはそのvendorの文書であり、それを説明するsurfaceはスコープ外とする _(2026-08-02 修正: それを表示するsurfaceが存在しないため、表現しないことにした。)_
- Q: User storyのpriorityはimplementation orderをどのように制約するか？ → A: Priorityは相対的なuser valueを表し、implementationは元のfamily-vertical delivery orderを維持する。Shared setupとblocking foundationの後、各familyでUS1 discoveryとUS2の完全で不活性なdetailを完了してからUS3 comparisonを行い、次のfamilyへ進む。正確な順序は、SKILL（Skill Metadataを含む）→ Instructions → MCP → Rules → Commands → Copilot Prompts → Custom Agents → Configuration/Settings → Output Styles → Plugins → Hooksとする。その後、Repository-wide Inventory、Detail、Comparison Acceptanceをこの順で完了し、Global inspection（US4、P3）を実施する。Cross-cutting verificationとrelease evidenceは最後に行う。 _(2026-08-24修正: catalogはplugin名をそのsourceへ解決するものであり、独立したkindではなくplugin kindのcarrierであるため、Marketplaces familyは順序から外れた。)_
- Q: Global enablementをcommitしたとき、Repository resultがunchangedであるとは何を意味するか？ → A: Repository Sourceはstableな`sourceId`を維持し、この操作ではRepositoryを再scanしないため、カスタマイズファイルのmembership、Source-relative path、readableまたはdiagnostic state、recognition、relationship、Repository-scoped diagnostic、authored source textはsemantic上変化しない。InitialまたはretryのGlobal Source正常commitはGlobal sequenceのgenerationだけを進め、そのsequenceの以前のgenerationに属する`FileDetail`、comparison selection/view、editor-model stateだけを無効化する。Repository sequenceのviewは無効化されない（FR-030）。これらGlobal sequenceのuser-interface lifecycleの変化はRepository resultのsemanticな変化ではない。すべてのcandidateがrejectされたattemptはGlobal Source commitを生成せず、このruleの対象外とする。
- Q: 各Diagnosticはどのlocation contextを持つか？ → A: 保持する各Diagnosticは正確に1つのscopeを持つ。File-scoped Diagnosticは、fileがそのSourceに属し、pathがそのSource内での当該fileのpathである、coherentな`sourceId`と`sourceRelativePath`のpairを必須とする。Source-scoped Diagnosticは`sourceId`だけを必須とし、`sourceRelativePath`を禁止する。Path-lessなscopeは存在しない: productが生成するDiagnosticはすべてSourceに属する。Scopeは、Diagnosticがcommit済みgenerationまたはsession lifecycleのどちらに属するかとは独立してlocationを表す。
- Q: 調査対象sourceのmutationには何を数えるか？ → A: Write、truncate、create、rename、delete、link、modeまたはownershipの変更、time、extended attribute、ACLの設定、またはmutation可能なflagでのopenを要求するproduct起因requestを数える。Inspectorはこれらを一切発行しない。Readだけを契機にOSが行うaccess-time更新はproduct control外であり、product起因mutationとして数えない。Testでは別に記録し、Inspectorから要求せず、Inspectorがsourceを変更した証拠にも使用しない。
- Q: Inspectorをsemantic analyzerにせず、どのstructural interpretationを許可するか？ → A: 構文だけのparsing、認識したkindが公開する宣言についてparserが解決した値の読み取り、確定済みdocumented catalogに対する分類、文書化済みorder、scope、condition、selection、reference relationshipの投影を許可する。Inventory、detail、comparison、Global control、diagnostic、API response、CLI output、documentationの全surfaceで、natural-languageの意味を解釈またはrank付けし、正しさ、有効性、compliance、qualityを判定し、remediation adviceを提供してはならない。
- Q: Sensitive-content acknowledgementはAPI authorization factorか？ → A: いいえ。認証なしsession hostのloopback限定bindがaccess-control boundaryである。Acknowledgement gateは存在しない（2026-07-28）。loopback限定のsessionでユーザー自身のfileを表示する前に立っていたため何も守らず、一方ですべてのfileを読むのに2回の操作を要求していた。Detailまたはcomparisonのsurfaceはfileを記述されたとおりそのまま表示する。事前に問わず、contentに何が含まれ得るかについての注意書きも掲げない。それは読み手自身のrepositoryについて読み手に説明するために画面を費やすことになる。残るのは取り扱いのほうである。Authored contentはbrowser memory内だけに保持し、1つのfileまたは1つのcomparisonに対する明示的なrequestでのみ到達でき、client-data purgeはdocument-liveness failureまたは同等のterminal reset後にinventory、detail、comparison、editor model、memory内metadataをclearする中央のfull-session client resetである。Global-disable actionはrequest送信前にこのpurgeを実行し、より大きいGlobal content epochまたはnon-null disable fenceの観測時にもrender前に繰り返さなければならない（MUST）。Route close、通常のfileまたはSourceのremove、generation changeは対象modelだけをdisposeでき、それ自体はclient-data purgeではない。
- Q: Inspectorはfile sizeまたはitem件数のvalidation limitを定義するか？ → A: いいえ。Fileとcollectionの容量はNode.js、parser、OS、filesystem、browser、実行環境によって決まる。Inspectorは製品固有のsizeまたはcount上限によってcustomization contentを拒否または分類しない。1 fileに閉じたfailureはそのfileのDiagnosticとして表面化する（FR-028）。それ以外のthrown exceptionまたはrejected operationは対象publication attemptをabortする — fileとscan layerは再分類・retry・recoveryのために原因を検査せず、唯一のclosedな原因checkはresource-exhaustion errno集合（`EMFILE`、`ENFILE`、`ENOMEM`）であり、これはread failureをper-file outcomeではなくこのabortへ送るためのものである。そのattemptからitem、Source、recognitionまたはderived result、scan-result record、success response、generationを一切commitせず、partial generationを決して許可せず、以前のcommit済みsnapshotがあればそれだけを利用可能に保つ。
- Q: Scanがpartial generationを公開してよいのはいつか？ → A: 完全なtraversalとgeneration assemblyの後、1つ以上の公開fileがFR-028の対象となる決定的でthrowしないentry-local outcomeをdataとして返し、影響を受けないfileが完全に残る場合に限る。公開fileとは、admit済みcandidateか、companion censusがその隣に列挙したfileである。列挙はadmitではないが読み取り経路は同じである。読み取りの失敗は両者で同じに数える。bytesがbinaryであるcompanionは、assetの通常の公開事実であり — それがtextであることを期待したものは何もない — Diagnosticを持たず、何もpartialにしない。一方、admit済みcandidateのbinary contentは、ruleがそのfileをtext customizationとしてadmitしたため、diagnostic-onlyのままである（2026-07-29）。Readableな`utf-8-replaced` textなどのcomplete outcomeは、そのpartial conditionから明示的に除外する。単一ファイル境界でのread、decode、parse、extractionの失敗は、原因を調べるのではなく失敗が起きた場所によって分類され、そのファイルの契約済みentry-local outcomeになる（FR-024/FR-028とClosedなScan Publication Outcomes表）。回復可能なenvironment/resource failureは、あるファイルのread中に現れた場合でもこれに当たらない。それはそのファイルについての事実ではなく実行環境についての事実であり、そのファイルをunreadableと報告することは、マシンの状態をユーザーのコンテンツの性質として報告することになる。その単一ファイル境界の外で発生したexceptionまたはrejected operation—coordination、assembly、serialization、transport、またはauthority喪失によるもの—はentry outcomeへ変換してはならず、partial publicationを許可しない。回復可能なenvironment/resource failureは、publication attempt全体をabortし、何も公開しない（Constitution § Quality and Safety Standards）。
- Q: Readしたbyteをどうdecodeするか？ → A: NUL byteを1つでも含むfileはbinaryに分類する。admit済みcandidateではその分類はdiagnostic-onlyであり、censusが列挙したcompanionではbinaryのbytesはそのfileの通常の公開事実である。それ以外はUTF-8 replacement semanticsで1回decodeし、先頭BOMが1つあれば記録して除去する。不正byte sequenceが`U+FFFD`へ置換された場合は`utf-8-replaced`を記録し、その文字化けしたdecoded stringをそのままdisplay、parse、extraction、comparisonへ渡す。別encodingを検出またはretryしない。
- Q: 通常のhome外にあるabsoluteなGlobal rootは、その場所だけを理由に失敗するか？ → A: いいえ。Absoluteな設定済みroot（active platformの`node:path.isAbsolute`）は、通常のhome外でもconsent後のadmission対象となる。設定が存在しない場合は文書化済みdefaultを選択する。Empty、relative、invalid、またはアクセス不能なrootは下記のclosed outcomeに従い、fallback authorityを作らない。
- Q: SC-003、SC-004、SC-005、SC-007のrelease-evidence denominatorをどのようにfreezeするか？ → A: Check-inしたversion付きrelease-evidence fixture manifestが、stable case ID、required class、fixtureまたはbuilder reference、expected outcome、content digestをこれらのcriterionへ割り当てる。Manifest versionとdigestがrelease candidateに使う正確なdenominatorを識別し、case欠落、未実行case、digest不一致、required classの空集合は該当criterionをfailureにする。Caseのremove/reclassify、required-class定義の変更、expected outcomeの変更は、denominatorを黙って弱めるのではなく、明示的なmanifest versionのincrementとreviewを必要とする。Fixture byteだけを変更する場合は、代わりに参照fixture digestとcanonical manifest digestの両方を変更する。どちらの変更も新しい直接比較不能なmeasurement setを開始する。

### Session 2026-07-20

- Q: Generation 0でRepository Sourceが存在する前にfilesystem admissionが必要か？ → A: いいえ。Session bootstrapは、`process.cwd()`から起動時working directoryを取得し、その文字列または有効な`--root`値からlexical path operationだけでRepository rootを選択し、filesystem I/Oを一切行わず同期的にRepository Sourceを正確に1つ作成しなければならない（MUST）。Escaped root labelとstable `sourceId`はauthorityを持たないsession identityにすぎず、最初のscanは保持したraw selected rootをそのままreadする — 別途のadmission layerは存在しない。決定的なscan failure — 存在しない、またはdirectoryとしてreadできないrootを含む — では同じSourceをfailed scan statusで保持し、source-scopedな`root-unreadable` Diagnosticを保持し（FR-002）、customization fileまたはgenerationを公開しない。Thrownまたはrejectedなautomatic-startup operationはprocess top levelまで伝播し、processまたはsessionが終了してよい。
- Q: ユーザーは起動時working directory以外のRepository rootをどう選択できるか？ → A: CLIは`--root <path>`を受け付けなければならない（MUST）。省略時は1回の`process.cwd()` captureから得たexact stringを選択済みRepository rootとする。Absolute optionはそのまま保持し、relative optionはactive platformの`node:path.resolve`でcapture済みinvocation directoryに対してlexicalにresolveする。`--root`を複数回指定した場合は引数parserのlast value（決定的なlast-wins）を採用し、別途rejectしない。引数の反復ポリシーは引数parserが所有する。明示的なempty valueは固定actionableかつsource-value-freeなstartup errorを出し、session作成またはbrowser起動より前に終了しなければならない（MUST）。Valueの欠落は同じboundaryで引数parserのtyped validationによりrejectしなければならず（MUST）、productはparser所有のcheckを重複実装してはならない（MUST NOT）。Root selection自体はfilesystem/network I/Oを0件とし、rejectされたlaunchは起動時`cwd`、`--root`、selected rootからderiveしたI/OとDNS、SMB、outbound-network callを0件としなければならない（MUST）。選択済みrootのreadabilityは初回scanだけが確定し、rootが存在しないかdirectoryとしてreadできない場合はsource-scopedな`root-unreadable` Diagnosticでそのscanが失敗する。
- Q: 1回のGlobal調査consentでどのtoolを有効にするか？ → A: Consentはsession-wideであり、toolを個別選択しない。Consent前はproposed rootに対するfilesystem I/Oを一切行わない。1回の明示的なGlobal opt-in後、1つのtransactionがfrozen previewの4 entryすべてを試行し、structuralに存在してadmissionに成功したrootをすべて自動的に含めなければならない（MUST）。ユーザーはmemberを個別に選択または解除できない。Lexicalにinvalidなroot、正確なstructural `lstat` absence、または決定的なlink/type/boundary rejectionからSourceを作成せず、admit済みsubsetのcommitを妨げない。Admit済みSourceはすべて1つのatomic generationとして一緒に公開する。1 fileに閉じないthrownまたはrejected operationは、失敗したrequestの通常のerrorとしてそのtransactionをabortし、subsetを一切commitしない。Rootを1件もadmitできず、operationのthrowまたはrejectもない場合は、文書化済みall-rejected outcomeを使用する。
- Q: Global root inputをplatform間で一貫してどうcaptureするか？ → A: New unconsented previewを作るrequestごとに、`COPILOT_HOME`、`CLAUDE_CONFIG_DIR`、`CODEX_HOME`をこの固定順で正確に1回ずつreadする。Capture値が`undefined`の場合だけabsentとし、empty stringはpresent overrideとする。そのrequestでimport済み`node:os.homedir()`を正確に1回callし、各absent valueについてactive platformの`node:path.join`で`.copilot`、`.claude`、`.codex`をlexicalにappendし、environment設定を持たない共有agent homeについては常に同じ方法で`.agents`をappendする。`HOME`、`USERPROFILE`その他のplatform variableを別途選択しない。このcaptureはfilesystem operationもexistence checkも行わない。Environment capture、`homedir()`、join、retention、escape、preview serialization中のthrow/rejectionは、そのinvocationをsession RPC boundaryで通常のreal errorとしてfailさせ、previewもauthorityも作らない。正常作成したpreviewは4つのexact stringをfreezeし、active consentが使う間は再captureしない。
- Q: Unicodeまたはescape ambiguityなしにroot stringをどうclassify、display、bindするか？ → A: Global stateは次の順でassignする。Present environment stringがlength zeroなら`present-empty`、U+0000またはunpaired UTF-16 surrogateを含めば`invalid`、それ以外でactive-platform `node:path.isAbsolute`がfalseなら`relative`、残るabsolute spellingは`eligible`とする。Spellingのsemanticsはplatform自身のpath処理が所有する。Repository/Global root labelはnormalizationせずexact UTF-16 code unitをiterateし、ASCII letter、digit、`.`, `/`, `:`, `_`, `-`だけをcopyし、その他の全code unitを各surrogate halfとbackslashも含め4桁uppercase hexの`\uXXXX`にする。そのASCII resultをHTMLでなくtextとしてrenderし、I/O用にdecodeしない。Previewはserverが保持するopaque `previewId`で識別する唯一のrecordであり、admitted root配下で何をreadするかはdigestなしの`allowlistVersion`/`traversalPlanVersion` pairがbindする。pairがdigestを持たないのは、serverが自身の保持するstateをcryptographicに再検証しないためである。
- Q: Runtimeのfile-read errorは誰が処理するか？ → A: 1 fileに閉じたfailureはそのfileのper-file Diagnosticになる（FR-024/FR-028）。それ以外のrejectされたNode.js operationは、file、parser、recognition、scan layerが分類・retry・recoveryのためにcatchせず、triggerを所有するexecution boundaryまで伝播する。Session API所有のoperationでは、failed requestがreal errorを通常どおり報告し — request accept前なら直接、accept済みjobならFR-030に従いterminal errorとして保持 — processと以前のcommit済みsnapshotを利用可能に保つ。Request ownerを持たないautomatic startup operationではprocess top levelまで伝播し、productはprocess survivalを保証しない。どちらも失敗attemptのitem、result body、generationをcommitしない。
- Q: 期待されるabsenceとruntime-ownedなfile-read errorをどのように両立するか？ → A: Contractで宣言したexact target（Codexのoverride/fallback対を含むGlobal exact-file rule）のprobeだけが、missing entry（existence probeの`ENOENT`/`ENOTDIR`）をclosedな`absent` outcomeとして扱ってよい（MAY）。この限定的な変換はmessageを調べ、別の原因を推論し、他のcodeまたはoperationへ適用してはならない（MUST NOT）。発見後のread failure — broken symbolic linkを含む — はそのfileの`file-unreadable` Diagnosticである（FR-024）。`entry-disappeared`やrace-detectionのtaxonomyは存在しない。
- Q: NULを含まないbyteがvalid UTF-8でない場合はどうするか？ → A: 文字化けのまま処理する。UTF-8 replacement semanticsで1回decodeし、生成された全`U+FFFD`を`sourceText`に維持し、encodingを`utf-8-replaced`とlabel付けして、通常のparse、extraction、display、comparisonを続行する。このdecode outcomeだけではpartialにしない。Source charsetを推測せず、2つ目のdecoderを試さない。NULを含むadmit済みcandidateは`binary`、diagnostic-only、file-confinedな`partial` outcomeのままとする。censusが列挙したcompanionのbinary bytesは、そのfileの通常の公開事実である。
- Q: Unicode normalization collisionと複数のhard-link pathをどう表すか？ → A: Enumerated pathでは`Dirent.name`が返した正確なraw entry-name segmentを保持し、parent enumerationを意図的に行わないtargeted fixed pathではimmutable registryのexact target spellingを保持する。そのprovenance固有spellingがfilesystem operandであると同時に、`/`でjoinしてpublicなmatchingとdisplayに使うSource-relative Pathでもある: 何も正規化して消さず、filesystemは1つの名前につき1つのentryしか保持しないため公開されるpathはすべて曖昧さを持たず、presentationは保存値を変えずに制御文字をescapeする。同じ物理fileへのhard linkであるallowlisted path同士は、通常の独立したfileである。Physical-identity grouping、primary-path選択、alias listは存在せず、各pathがそれぞれのCustomization Fileを公開する（FR-024）。Symbolic linkは透過的にreadし、Sourceをまたいでidentityをmergeしない。
- Q: Codex overrideのfallback selectionでemptyとは正確に何か？ → A: 安全にverifyしてreadできたnon-binaryな`AGENTS.override.md`について、任意の先頭UTF-8 BOMを1つ記録して除去した後のdecoded stringが`String.prototype.trim().length === 0`である場合に限りemptyとする。Whitespace-only textはemptyである。安全にreadした`utf-8-replaced` stringにも同じtestを適用するため、保持した`U+FFFD`が1つでもあればnon-emptyになる。Missingなinitial target（existence probeの`ENOENT`/`ENOTDIR`）だけをabsentとしてfallbackを許可する。Unreadableまたはbinaryなoverride — broken linkやprobe後にfailするreadを含む — はそのfile Diagnostic（`file-unreadable`または`file-content-binary`）でbranchを終了し、fallbackしない。
- Q: Current MCP guideがまだ省略しているVS Code 1.118以降のworkspace root `.mcp.json`をどう扱うか？ → A: CLI recognitionの対象でもある同一physical fileへ、2つ目のcompatibleなCopilot/MCP provenanceとしてexact root fileをadmitする。1.118 release noteが確立するのはVS Code pathとmost-specific same-name ruleだけで、current guideは`.vscode/mcp.json`をworkspaceの`servers` fileとして文書化し続け、root file schemaまたはtotal orderを確立しない。`documentationStatus: conflict`を保持し、VS Code所有root extractor fieldを追加せず、direct official documentationが解消するまでselectionをunknownのままにする。

### Session 2026-07-21

- Q: Active-consent Global retryは何を対象とし、どのstateを置換できるか？ → A: 同じfrozen preview、consent record、fixed ordered tuple `[copilot, claude, codex, agents]`を再利用する。Matching previewを検証済み、`globalEnableInProgress`がnull、`pendingTools`がempty、`retryableTools`がnonemptyの場合だけretryを提示する。Serverはnon-pending unpublished `admitted` controlと`retryDisposition: same-preview`の`rejected` controlからなるfixed-order `retryableTools` set全体をderiveし、published、pending、lexicalな`new-preview-required` controlを除外する。UI/APIはtargetを追加、omit、reorderできない。Tentative validation/admission中に新たにvisibleとなるのはauthority-freeな`globalEnableInProgress`だけで、既存Repository/Global Source、control、batch/diagnostic field、`pendingTools`、`retryableTools`、prior committed snapshotはexact pre-operation projectionを維持する。Retry対象がすべてdeterministically rejectedならcontrolをatomicに更新して`active-no-job`を返し、`scanRequestId`、scan job、Source、generationを作らず、既存Sourceを置換しない。1つ以上をadmitした場合はatomic queued acceptanceで正確に1つの`GlobalBatchScan`と1つの`scanRequestId`を作り、`pendingTools`をmatching `batchStatus`と同じadmitted subsetへ変更し、既存Sourceを維持して新規admit済みSourceを1つのatomic generationで一緒にpublishする。Thrownまたはrejected operationは失敗したrequestの通常のerrorとして報告し、正確なpre-operation stateを復元する。

### Session 2026-07-22

- Q: 調査対象のカスタマイズファイルは、productが防御すべき敵か？ → A: いいえ。このツールはユーザーが既に信頼しているworkspaceで実行され、AIエージェントが読み込むものを単純に表示する。敵対的file modelに由来する要件（TOCTOU checkpointの再検証、race taxonomy、hard-linkのread-once grouping、structural-`lstat` carve-out doctrine）は、通常のtraversalとper-file diagnosticへ置き換えられた。過去のclarification entryは履歴として残す。維持する義務: 調査対象contentは決して実行しない、loopback sessionを他originから保護する、表示contentはinertにrenderする。
- Q: Symlinkされたカスタマイズファイルはreadするのか、報告だけか？ → A: 他のfileと同じように透過的にreadする。Agentはcustomization fileをloadするときsymbolic linkを解決するので、忠実なinspectionはlink先のcontentを表示しなければならない。Targetがmissingまたはunreadableなlinkは通常のper-file diagnosticになり、recursiveなtraversalはreal pathで訪問済みdirectoryを追跡してlink cycleがscanの終了を妨げないようにする。暫定のsymlink非追跡ruleとその`file-symlink` diagnosticは削除された。
- Q: Local hostは何で実装し、sessionはどう保護するか？ → A: Productはeslint/config-inspectorと同じ基盤であるdevframe local-tool frameworkを、static asset配信とlocal session API channelのために採用し、devframeの認証は無効にする。保護はloopback bindingのみとする。Hostはloopback interfaceにbindし、sessionを起動元machineの外へ決して公開しない。Sessionごとのtokenとproduct所有のOriginチェック、手書きのHTTP routerは削除する。認証なしloopback hostの残存露出（他のlocal process、DNS rebinding）は文書化したlimitationとする。憲章v3.0.0が改定後の義務を記録する。
- Q: Operational logの内容規制とgeneric error envelopeはまだ必要か？ → A: いいえ。FR-040とFR-041は削除された。Productにtelemetryは存在せず（FR-022がoutbound通信を禁止済み）、terminalとUIの出力を読むのは調査対象fileを所有する本人であり、認証なしのsession APIは既にfileの完全な内容を返す。したがってerrorの原因を隠しても何も守られず、failureをdebug不能にするだけだった。Errorは通常どおり報告し（実際のmessage、通常の伝播）、closedなerror entity、sanitizer、envelopeはいずれも存在しない。Failureのsemanticsは既存の所有者に残る: per-file分離はFR-028、atomic commitとstale snapshotのruleはFR-030とscan-publication表。憲章v4.0.0がこの削除を記録する。
- Q: RepositoryとGlobalの調査は1本のgeneration sequenceを共有するか？ → A: いいえ。両者のlifecycleは独立している—Repository Sourceは常に存在し、Global sourceはenableからdisableまでしか存在しない—ため、それぞれが独自のatomic generation sequenceを保持する。Repository sequenceはbootstrap generation 0から、Global sequenceはenable commitが作成しdisableが破棄する（disableは何もcommitしない）。Commitは自分のsequenceのviewだけを無効化する。従来のcarry-forward rule—Global commitがRepository inventoryをcopyして変えずに引き継ぐ—は削除された。Source間comparisonは影響を受けない。比較は常に各sourceの最後のcommit済みstate同士で行われる。

### Session 2026-07-23

- Q: Local session hostはどのhost名でbindし表示するか？ → A: 固定のhost名`localhost`とする。Hostはこれを通じてloopback interfaceへbindし（platformのresolverがIPv4 `127.0.0.1`またはIPv6 `::1`として解決する）、表示および自動オープンするURLは、数値addressよりuserが認識しやすい`http://localhost:<port>/`とする。どのconfigurationやflagも`0.0.0.0`、LAN address、Unix socketをbindしない。Bind・表示URL・発行済みinternal-loopback authorityの従来のexact `127.0.0.1`表記はこれに置き換える。

### Session 2026-07-29

- Q: PublicなSource-relative Pathはraw entry nameをnormalizeするか？ → A: いいえ。公開されるpathはexactなraw entry nameを`/`でjoinしたもの — 同じtreeを読むagentが操作する綴りそのもの — であり、presentationは保存値を変えずに制御文字をescapeする。導出されるNFC display綴りは存在せず、normalization-collisionという概念も存在しない: filesystemは1つの名前につき1つのentryしか保持しないため公開されるpathはすべて曖昧さを持たず、見た目が同じにrenderされ得る2つのraw綴りは、別々に公開される2つの実在fileである。`path-normalization-collision` Diagnosticとpathlessな`session` scopeもこれとともに削除される。NFC導出が役立っていたのは、normalization-preservingなfilesystemが残したNFD綴りentryのrenderingだけであり、この製品が忠実に見せるために存在するidentityそのものを書き換えたうえ、必要になったrejection機構は実在するfileをinventoryから隠していた。

### Session 2026-08-08

- Q: Skill inventory rowを名付けるものは何か？ → A: 認識した各tool自身の文書がそのfileを呼び出す名前であり、rowの単位は「1つのtoolが解決した1つのinvocation name」となる。CodexとCopilotはauthoredなfrontmatter `name`を呼び出す。Claude Codeはfrontmatterの宣言に依らずskill directoryからcommandを導き、nestedなskillには`.claude`を保持するdirectoryのroot相対`/`-joined pathと`:`を前置するため、`name: ship`を宣言する`apps/web/.claude/skills/deploy/SKILL.md`は、Claude Code rowでは`apps/web:deploy`、Copilot rowでは`ship`である。認識toolごとに異なる名前で呼び出される1つのfileは、それぞれの名前のrowに定義として現れる。名前を宣言しない — または空で宣言する — fileはそのskill directory名で呼び出される。これはもともとClaude Codeのcommand名の出所でもあり、すべてのrowが名前を持ち、同名directoryに置かれたそうした2つのfileは1つのrowを共有する。
- Q: NestedなClaude Code skillの名前はいつqualifyされるか？ → A: 常に。prefixは選択されたroot相対とする。Vendorはsession working directory相対のprefixで名前衝突時にqualifyするが、Inspectorはsessionを観測せず、unqualifiedな名前が空いているかはInspectorが読まないuser・enterprise層に依存するため、root相対のqualified綴りだけがstaticなinventoryが保証できる唯一の安定した名前である（skills page § How a skill gets its command name）。
- Q: Skill detailの見出しを名付けるものは何か？ → A: そのskill自身のdirectoryであり、その下に、各認識製品が呼び出す名前を並べる。pageはrouteが指すfileを示す — detail URLは`/skills/detail/<source>/<Source相対パス>`というfile自身のidentityで、各path segmentはpercent-encodeされる — ため、どのdocumentがpageに出るかはlinkのidentityであって、pageが適用するpreferenceではない。tool segmentは持たない: 1つの`SKILL.md`を読む2つの製品は同じbyteを読むため、製品ごとのaddressは1つのdocumentに名前だけが異なる2つのURLを与えることになる。代わりにpageは、各製品がそのskillを呼び出す名前を述べる。このidentityは再スキャンとserver起動を跨いで安定である — それはURLのpath半分のことで、Source-relative Pathがwire上のfileのidentityであり — 世代ごとのfile IDは存在しない — detail requestはそれを現在のcommit済みsnapshotに対して解決する（FR-030）ため、bookmarkされたlinkのpathは、再スキャンを跨ぎ、同じrootを選択する起動を跨いで、同じfileを名指し続ける — pathはSource相対であり、別のrootを選択する起動（FR-001）はそのrootのscanに対して解決する。Bookmark全体が解決されるかはoriginの問題である: originはdevframeのport選択に属し、希望port — 起動が`--port`で述べた場合はその値、述べない場合はdevframe自身のdefault — が空いていれば再利用され、塞がっているときだけ別portへ移る（quickstart.md）ため、portの移動が変えるのはbookmarkの指す先であって、そのpathが名指すfileではない。 *(2026-08-25修正: 起動が希望portを述べられるようになったため、再利用されるのはdefaultだけではなくその希望である。)*現在のscanが保持しないpathは、近くの何かへ解決されるのではなくdead linkとして報告される。

### Session 2026-08-14

- Q: 比較サーフェスはcustomization kind間で共有されるか？ → A: 共有されない。比較サーフェスはkind固有である。比較を持つ各kindが同じ形のサーフェスを1つずつ出荷しており、ここではskill比較を通じて記述する — 一つのskill名のcopy同士を対応するファイルごとに比較し、その名前のinventory rowまたはdetail画面のlinkから入り、比較画面そのものの上で切り替える。片方のcopyだけが持つファイルもこの比較の一部であり、switcherに提示され、存在する側の完全な内容を明示された不在に対して表示する — であり、そのroute・page・composable・componentはskillスコープである: `/skills/compare/<family>`、`src/app/pages/skills/compare/[family].vue`、`src/app/composables/skill-comparison.ts`、`src/app/components/skill-comparison/`。他のkindはこのサーフェスを共有しない — 例えばMCP比較は同名のfile copyではなくcarrier file内のdeclarationを比較する — ため、各kindは同じ`compare`先導のaddress形`/<kind>/compare/<family>`とper-side Source query parameter（contracts/http-api.md § Host requirements #5）の下で自身の比較route・composable・componentを所有し、kind間で共有されるmoduleは存在しない。URLはmodel自身の座標で比較を名指す — `/skills/compare/<family>?name=<row名>&leftSource=<selector>&left=<entry path>&rightSource=<selector>&right=<entry path>&file=<copy相対パス>`、Source familyはpath segmentで、rowはそれがkeyされるinvocation nameで、copyはそのSourceとentry fileのSource-relative Path（FR-030）で指名し、`file`はentry同士のとき省略される。rowを2つのpathから導出せず名指すのは、2つのfileが複数のrowに同居しうるためである — ため、modelが表現しないペアは比較されず報告され、typed recognition rowは常にサーフェス自身のkindのものである。

### Session 2026-08-18

- Q: Instructions一覧のrowは何を列挙するか？ → A: 1つの適用範囲と、それを担当するinstruction fileである。範囲はRepository root相対のglobで、file自身のpathから導出する: Repository rootは`**`を、`packages/api/`のfileは`packages/api/**`を導出し、認識した製品がそのfile名を置くためのdirectoryは末尾から取り除く — Claude Codeは`CLAUDE.md`を`.claude`に置くため、`.claude/CLAUDE.md`は隣の`CLAUDE.md`と同じくrootの`**`を導出する一方、`.claude/CLAUDE.local.md`は自身のdirectoryを保って`.claude/**`を導出する。ページが`.claude`の代替配置を挙げているのは`CLAUDE.md`だけだからである。そうしたdirectoryが何を意味するかはその製品自身の事実であるため、各製品が自身のruleについて答える。Rowは導出したglobそのものでkeyされ、文字列一致でgroupingする: globのparse、綴りの正規化、2つの範囲が重なるかの判定は行わないため、2つの範囲が同じrowになるのは綴りが一致するときだけであり、導出側の綴りはfileごとでなく製品が固定する。導出した範囲はglobがsyntaxとして読む文字をescapeするため、`packages/[api]` directoryは`packages/\[api\]/**`を公開し、文字classではなくそのdirectory自身を表す。自身の範囲を宣言するfile — Copilotの`applyTo` — はpathでなくその宣言値でkeyされ、その宣言値はparserが解決したとおりに公開する — 値自身のquoteとescapeは、すべての宣言値と同じ読み方で一度だけ解決される — 一方でこの製品はescapeも正規化も加えない: 解決済みの値こそが著者自身のpatternであり、escapeすればその名前のdirectory literalに変えてしまうからである。rowをkeyできるものを何も宣言しないそうしたfile — 宣言なし、authoredな空値、rowとして綴れないlistやmapping、あるいは宣言をまったく読めなかったfile — は既知の範囲を持たない: そのvendorはこのfile名の適用可否を宣言だけから読むため、pathから読み取った範囲は、vendorが何も与えないfileに最大の担当範囲を述べることになる。そうしたfileは「既知の範囲がない」と述べる1つのrowを共有し、すべての範囲付きrowの後にsortされ、理由がparse失敗であるときは各file自身のdiagnosticsがそれを述べる。範囲はfileが担当する対象を述べるのであって、sessionがloadしたものではない: どの製品がいつそのfileを読むかは、このtoolが観測しないruntimeのままである（FR-009）。 _(2026-08-22修正: 宣言による範囲の箇所がCopilotの`applyTo`以外の宣言も挙げていた。Claude ruleは行の単位がfile自身である独立kindであり、そこからは何も読み出さないため、その宣言がinstructions rowをkeyすることはない。)_
- Q: Walkはinstalled dependency treeへ入るか？ → A: 入らない。`node_modules`という名前のdirectoryには、Repository rootでも任意の深さでも決して入らない。既に除外している`.git`、`.hg`、`.svn`内部と並ぶ: `node_modules` directoryはpackage managerがinstallしたpackageを保持するため、その中のcustomization fileはそれをshipしたpackageのものであって、検査対象のrepositoryでauthorされたものではなくmanifestとlockfileから再現される。製品はruntimeでそのfileを読み得る — Claude Codeはfileをreadしたsubdirectoryの`CLAUDE.md`を発見する — ため、この除外はagentがloadできるものの記述ではなく、この製品がinventoryする対象を狭めるものである。`node_modules`はentry名だけで除外し、それ以外では除外しない。解決済みreal pathでも除外するVCS内部とは異なる: object storeへ到達するのはwalkの経路によらず誤りである一方、repositoryが自身のpathに置いたdirectoryは、そのlinkが何へ解決されようとrepositoryのものであり、authorされた場所にあるsymbolic linkはその場所の条件でinventoryする（FR-024）。名前はこの4つだけであり、除外の判断にignore fileは読まない。Ignore fileはそのtoolingが何をskipするかを述べるものであって別の問いであり、かつ変化する。他のecosystemのinstalled-dependency directoryは、それを報告する事例とともに追加する。

### Session 2026-08-21

- Q: どのrecognitionが、それをadmitしたruleの依拠するproduct surfaceを述べるか → A: すべてのrecognitionである。この言明は、tool名だけでは答えられない問いに答える: vendorがそうしたfileをどこから読むとdocumentしているか、である。GitHub Copilotのeditor、CLI、cloudの各surfaceは同じ名前のfileを異なる場所から読むため、rootの`.mcp.json`に対する1つのCopilot recognitionは2つのsurfaceを名指し、同じproductの`.vscode/mcp.json`は1つを名指す。catalogに1つしかsurfaceを持たないproductもこれを述べる。集合はfile同士を区別するためではなく、fileを読むものを絞り込むからである: Codexのlocal clientsはCLI、IDE拡張、desktop hostであり、local fileを読まないhosted serviceを除外する。変化するrecognitionにだけ限定すると、その不在がsurface 1つのproductなのか何も述べないkindなのか曖昧になり、条件を持たない表示規則にvendorごとの条件を持ち込むことになる。

### Session 2026-08-23

- Q: Skill rowは、すべてのtoolが共有するauthoredな`name`ではなく、各toolが呼び出す名前でkeyすべきか？ → A: すべきである。Rowはその名前で読み手が呼び出せるものを列挙するものだから、自身が抱えるtoolの一つが応じない名前を見出しに持つrowは、その読み手が到達できないものを名指すことになる — `name: voyage`を宣言する`.claude/skills/lander/SKILL.md`はCopilotの`voyage`でありClaude Codeの`lander`であり、両方にまたがる1つのrowは、どちらかの製品が持たないcommandを述べることになる。したがって名前とinvocation nameは1つの事実であり、名前がpathと宣言からどう導かれるかはそのvendor自身のcontractであるため、prompt/command fileと同じく、admitしたruleが答える。Tool横断の比較が失われるわけではない: 両方の製品が読むfileはそれぞれの名前のrowの定義となり、比較surfaceはentry fileでcopyを組み合わせる。
- Q: Codexの`.codex/config.toml`は、自身の`settings/config` rowの下で完全なsourceを表示するか？ → A: する。detailがfileのbyteを示すかどうかは、そこへ辿り着いたrowの主題から従うのであって、file自体からではない: 主題がfile内の1つの宣言であるrowはその宣言だけを公開する。fileを示せば、1つの宣言についての画面に他のすべての宣言と設定が載ることになるからである。一方、主題がfileであるrowはauthorが書いたdocumentを示す。1つの物理fileは両方の形のrowを持ちうる — config carrierは宣言した各serverのMCP rowと、file自身である1つの`settings/config` rowを持ち、`project_doc_fallback_filenames` entryが名指した`.mcp.json`はinstruction fileでもある — ため、同じfileが宣言rowの下では宣言だけを、file rowの下では完全なsourceをserveする。Configuration fileのcomment、authoredな綴り、section順は読者が自分のfileと突き合わせる対象であり、parserが解決した宣言の一覧はそのいずれも運ばない。

### Session 2026-08-27

- Q: User-globalの調査はどこまで及ぶか？ → A: サポート対象toolが読む文書化済みuser-levelカスタマイズfileすべてに、製品がpublishするすべてのkindで、4つのconsent済みmember root — Copilot、Claude、Codexのtool homeと、共有agent home `~/.agents` — の配下で及ぶ。共有agent homeはCodexとCopilotがpersonal skillとpersonal plugin marketplaceの場所として文書化しており、これを移動させる設定は存在しない。toolが読むのにこの製品が見せられないuser-level fileはAIへの見えない入力であり、それはこの製品が埋めるために存在する空白そのものであるため、instruction限定だったscopeを文書化された全体へ広げた（FR-015からFR-018、FR-045）。除外のまま残るのは、ユーザーがtoolに読ませるために書いたカスタマイズではないもの — vendor管理のruntime state、credential、cache、session data、生成されたmemory、install済みplugin copy — と、closed kind setがpublishできない文書化済みsurfaceであり、それぞれFR-018が名指す。
- Q: Vendorが別toolのhomeを読むと文書化している — VS Code Copilotは`~/.claude/CLAUDE.md`、`~/.claude/rules/`、`~/.claude/skills/`を読む。これはClaude memberのfileにCopilot recognitionを生むか？ → A: 生まない。Memberのruleは、自身のconsent済みroot配下にある自身のtoolの文書化済みsurfaceである。したがってhome横断のreadは、Inspector recognitionを持たない記録済みvendor behaviorに留まり、file自体はhome member自身のrowとして既に見える。共有agent homeは例外ではなくこの規則の通常の適用である: それ自体が1つのconsent済みrootであり、そのpathを文書が名指すtool — CodexとCopilot — がそのruleのprojectするrecognitionである。

### Session 2026-08-28

- Q: comparisonがRepositoryのfileとconsent済みhomeのfileを組むことはあるか？ → A: ない。comparisonはどのkindでも1つのSource familyの内側に留まる。repositoryとconsent済みhomeは別の種類の場所だからである: rowの各family blockが自身のcomparison entryを持ち、そのfamilyのmemberどうしを組む — 1つのfamilyはconsent済みの2つのhomeを持ちえて、そのfileどうしはなお比較できる — そして各blockがmemberを1つずつしか持たないrowはentryを持たない。したがってすべてのkindのcomparison routeは自身の`compare` segmentの下にfamilyを従える`/<kind>/compare/<family>`であり — detailの2番目のsegmentはSource selectorで、`compare`は決してselectorになり得ないため、どのSource相対pathもこれと衝突できない — 各sideはなおqueryで自身のSourceを名乗る（FR-030; contracts/http-api.md § Host requirements #5）。

### Session 2026-09-01

- Q: Scan timingとinteraction latencyのsuccess criterionはこのreleaseに含めるか？ → A: 含めない。測定には、runの前にprocessor型番・image revision・memory・storageを記録した凍結host 1台が要る。同じ数値を他所で取れば、それはそのマシンの説明であってこのproductの説明ではないからである。そうしたhostは指定されていないため、意味を成さない場所で測るのではなくcriterionを撤回する。Performance suiteは100,000 entry fixtureに対する非gatingのsmoke passとして残る。閾値を主張せずにharnessの健全性を保つのがその役目である。
- Q: SC-001とSC-006のセッションは誰が実施するか？ → A: 20件の独立した自律agentセッションであり、各セッションにはproduct自身のguidanceと定型taskプロンプトだけを与える。これが測るのは、そのguidanceでfileに到達でき、productがそのfileについて述べることを言えるかである。human-subjectのusability studyではなく、そこから得たevidenceはparticipant cohortではなくagent駆動runとして記録する。SC-006のsafetyの側 — 意図しないexecution、被検査sourceのmutation、禁止対象のoutbound requestやMCP connection、被検査contentの露出 — は変更なく、自動runはそれをmoderated runと同等以上に観測する。
- Q: SC-008は支援技術に対する手動実行を要求するか？ → A: 要求しない。閉じたmanual matrixは3つのoperating systemと3つのscreen readerで38,880のkeyed cellを要し、このreleaseで利用できるどのrunもそれを生めない。SC-008が主張するのは3つの認証browserにおける自動Level A/AA checkと、keyboardのみの4 workflowである。Applicability matrixはどのcriterionが適用され、なぜそうかの記録として残り、manual checkを名指すrowはそのcheckを合格ではなく未実行として記録する。
- Q: セッションがagent駆動になった今、SC-001/SC-006のstudy機構には何が残るか？ → A: Task材料以外は何も残らない。Sealed-capture kit — 固定launch line、study-input distributionとそのdigest freeze、request ledger、browser proxyとnavigation grant、reviewer process、inherited-IPCのsupervisorとevidence seal — は、moderatedな人間studyを監査可能にするために存在した。そのstudyは行われないため、kit、そのprotocol contract、suite、package command、およびproduct側のreadiness probeは、背後にrunを持たない機構として残すのではなく削除する。残るのは評価が読むもの、すなわちguidance、4つの定型task prompt、response form、ground truth、scoring rubricである。

## ユーザーシナリオとテスト _(必須)_

### ユーザーストーリー1 - リポジトリのカスタマイズを発見する（優先度: P1）

開発者は意図するRepository rootへ移動するか`--root <path>`で指定してから`npx`経由でInspectorを起動し、GitHub Copilot、Claude Code、OpenAI Codexが認識するカスタマイズファイルの一覧をブラウザで確認する。選択済みRepository rootは常に独立したRepository Sourceとして表す。

**この優先度の理由**: Agentを実行せずに関連ファイルを見つけることが、プロダクトとして価値を持つ最小単位であり、後続workflowの前提でもある。

**独立テスト**: サポート対象、対象外、ネストしたファイル、複数ツールに認識されるファイルを含むfixtureリポジトリを起動時`cwd`として1回、別directoryからrelativeな`--root`でそのfixtureを選択して1回Inspectorを起動する。両runが同じRepository rootを選択し、調査対象パス一覧に含まれるすべてのサポート対象カスタマイズファイルが一覧に含まれ、無関係なファイルが除外され、Repository Source、カスタマイズファイル種別、Source-relative path、認識ツールが正しく示されることを確認する。

**受け入れシナリオ**:

1. **前提** `--root`を省略し、起動時`cwd`に3ツールすべてのサポート対象カスタマイズファイルがある、**操作** ユーザーが調査を開始する、**結果** ブラウザにはそのdirectoryが1つのRepository Sourceとして、toolおよびカスタマイズファイル種別で絞り込める一覧とともに表示される。
2. **前提** ユーザーが別directoryから起動し、relativeまたはabsoluteな`--root`を指定する、**操作** ユーザーが調査を開始する、**結果** 選択済みrootが1つのRepository Sourceとして表示され、process working directoryは変更されない。
3. **前提** `--root`が明示的なempty valueを持つ、またはvalueが欠落している、**操作** startupがcommand lineをparseする、**結果** 明示的なempty valueは固定actionableかつsource-value-freeなerrorを出し、valueの欠落は引数parserのtyped validationによりrejectされ、どちらもsession作成またはbrowser起動より前に終了する。
4. **前提** 1つの物理`AGENTS.md`がCopilotとCodexの両方に認識される、**操作** 一覧を表示する、**結果** 1つのカスタマイズファイルに2つの異なるtool recognitionが付いた状態で表示される。
5. **前提** Repositoryの調査対象パス一覧に含まれないファイルがある、**操作** リポジトリをスキャンする、**結果** それらのファイルはカスタマイズファイルとして解釈も表示もされない。
6. **前提** サポート対象カスタマイズファイルがない、**操作** スキャンが完了する、**結果** エラーではなく、サポート範囲を説明する正常な空状態が表示される。

---

### ユーザーストーリー2 - カスタマイズファイルを有効化せずに調査する（優先度: P1）

開発者はカスタマイズファイルを開き、そのsource text、関連metadata、source boundary、tool recognition、文書化されたscopeまたは関係を確認する。Inspectorは不確実性を明示し、カスタマイズファイルを実行も評価もしない。

**この優先度の理由**: Agentが読み込むものをまったく有効化せずに正確に表示することがこのプロダクトの価値であり、忠実で実行しない調査は追加機能ではなく中核機能である。

**独立テスト**: 実行可能なhook command、script付きskill、MCP server定義、import、不正なdata、リテラルcredential、環境変数参照、boundary外linkを含むfixtureを、filesystem書き込み、child process、network activityを監視し、sentinel環境変数値を与えながら調査する。自動browser起動を無効にするか、許可された固定browser起動helperが完了した後にchild process監視を開始する。調査対象contentが不活性のままであり、リテラル値と参照がenvironment置換なしで記述されたまま表示され、sentinel値が表示contentへ混入せず、diagnosticが出ても影響を受けないカスタマイズファイルを引き続き利用できることを確認する。

**受け入れシナリオ**:

1. **前提** command、hook、plugin、skill、workflow、extension、MCP serverのいずれかを宣言するカスタマイズファイルがある、**操作** ユーザーが開く、**結果** Inspectorは宣言を表示するが、起動、接続、指示の評価を行わない。
2. **前提** リテラルcredentialと環境変数参照を含むサポート対象設定がある、**操作** 表示または比較する、**結果** 両方をmaskせず記述されたまま表示し、環境変数参照を解決せず、reveal操作も必要としない。
3. **前提** 優先順位または実効動作が未知のruntime surface、version、trust decision、working directory、flag、environmentに依存する、**操作** カスタマイズファイルを調査する、**結果** Inspectorは見つけたものとその場所だけを述べ、勝者も実効設定も主張しない。
4. **前提** 完全なtraversal後、1つのfileだけがunreadableである、admit済みcandidateがbinaryである、またはrecognitionがparse failureであり、他のすべてのfileが完全である、**操作** scanがresultをassembleする、**結果** Inspectorはそのfileの実行可能なdiagnosticと完全な非影響fileを含むpartial generationをpublishする。NULを含まないinvalid UTF-8だけの場合は代わりにreadableな`utf-8-replaced` resultを生成し、scanをpartialにしない。**代わりに前提** attemptが単一fileの外で失敗する、**結果** 何もcommitせず、失敗を通常のerrorとして報告し、prior committed snapshotがあれば維持し、validityまたはlintのverdictを出さない。

---

### ユーザーストーリー3 - カスタマイズを比較する（優先度: P2）

開発者は一つのskill名の2つのcopyを対応するファイルごとに並べて比較し、source textとrecognition metadataを読み合わせて、Agentに解釈させずに重複と差分を理解する。一つのskill名のcopy同士の中では、片方のcopyだけが持つfileも同じ形で比較する: 存在する側の完全な内容を、もう片方のcopyの明示された不在に対して表示する。Credentialの差を隠さないよう、記述された値は表示したままにする。決定的なdiagnostic-only itemはinventoryに残すが、comparisonの選択対象にはしない。Thrownまたはrejectedなreadからは、そのようなitemを作成しない。

**この優先度の理由**: 比較によって、ファイル一覧は移行やトラブルシューティングに実用的な道具となり、同時にsemanticな判断をしない範囲を維持できる。

**独立テスト**: 2つのcopyが異なるtool recognitionを持つskill名の比較を開き、sourceとmetadataが並んで表示されること、正しさの評価や変更提案をせずにリテラルな差分とrecognitionの差を示すことを確認する。決定的なdiagnostic-only Repository fixtureをcomparisonに選択できず、rejected readからitemを作成しないことも確認する。このtestはGlobal調査より前に完了するRepository workだけを必要とする。

**受け入れシナリオ**:

1. **前提** 2つのcopyがreadableなfileを持つskill名がある、**操作** ユーザーが比較する、**結果** contentベースのmaskingを行わず、両方の完全なsource viewと、Source-relative path、source、file type、tool-recognition metadataを同時に確認できる。
2. **前提** 同じカスタマイズファイルに複数のtool recognitionがある、**操作** 別のカスタマイズファイルと比較する、**結果** 各recognitionを物理ファイルと区別したまま確認できる。
3. **前提** 2ファイルに競合する自然言語指示がある、**操作** 比較する、**結果** どちらがsemantic上正しいか、または有効かを断定せず、リテラルな差分だけを示す。
4. **前提** 発見済みのカスタマイズファイルが決定的なdiagnostic-only outcomeを持つ、**操作** ユーザーがcomparisonの選択肢を確認する、**結果** そのitemはdiagnostic reviewに利用可能なままだが、comparison inputとして選択できない。Readがthrowまたはrejectしたfileはattemptがcommitされないため存在しない。
5. **前提** あるskill名のcopyの一つが、別のcopyが何も保持しないcopy相対pathにreadableなfileを持つ、**操作** ユーザーが比較をそのfileへ切り替える、**結果** 存在する側の完全な内容がもう片方の明示された不在に対して表示され、空のfileの捏造も、どちらのcopyが正しいかの断定もない。

---

### ユーザーストーリー4 - User-global調査へopt-inする（優先度: P3）

開発者は、サポート対象toolの文書化されたuser-levelカスタマイズfileを調査するため、session-wideなconsentを1回明示的に与える。Productは4つのmember root — Copilot、Claude、Codexのtool homeと共有agent home — すべてを試行し、存在してadmissionに成功したrootをすべて自動的に含め、memberごとのselectorを提供しない。Repository Sourceとそのviewは、Globalのlifecycle変化に影響されない。Global調査は独自のgeneration sequenceを持つため、Global sourceの有効化・再scan・無効化がRepository stateを無効化することはない。

**この優先度の理由**: User-levelのカスタマイズはリポジトリファイルだけでは説明できない挙動の理解に役立ち、toolが読むのにこの製品が見せられないuser-level fileは、AIへの見えない入力 — この製品が埋めるために存在する空白そのもの — である。それでもhome構成の調査はprivacy riskを高めるため、調査は任意のままとし、文書化されたカスタマイズfileに厳密に限定する: その横にあるconfiguration state、credential、cache、session data、生成物は決して読まない。

**独立テスト**: 3つのmember rootが存在し、1つがmissingな状態で起動し、opt-in前に一切probeまたはreadされないことを確認する。Consent UIがall-members actionを1つだけ提供し、memberごとのselectorを持たないことを確認する。Global調査を有効にし、productが4つのfrozen preview entryすべてを試行し、missing rootをfallbackなしでrejectし、memberのcontract済みselectorが名指すファイルだけを、それぞれ正確に1つのrootを持つ3つの独立して識別されたadmit済みGlobal Sourceの下でatomicに公開することを確認する。2つのconsented homeが持つ同じrowの読み取り可能なfileどうしを比較し、両方が独立して識別されたowning SourceとSource-relative Pathにboundされたまま、ユーザーストーリー3で要求するliteralかつnon-semanticなbehaviorを維持することを確認する — 比較は1つのSource family内に留まるため、Repository fileがそのpairの片側として提示されることはない（§ Clarifications Session 2026-08-28）。正常なadmitted-subset commitがRepositoryのviewとstateへ一切触れずにGlobal generationを作成することを確認する。その後でGlobal調査を無効にし、Globalの全結果がsessionから除去されることを確認する。

**受け入れシナリオ**:

1. **前提** Global調査を有効にしていない、**操作** Inspectorを起動する、**結果** user-globalの調査対象パスにあるファイルを読み取りも表示もしない。
2. **前提** ユーザーが完全な4-member previewを確認して1回明示的にopt-inした、**操作** Repository Sourceを再scanせずGlobal調査を実行する、**結果** productはtool selectorを受け付けずpreview済みtool rootすべてを試行し、missingまたはunreadableなrootを除外し、admit済みtool rootすべてを正確に1つのrootへboundされた別々のGlobal Sourceとしてatomicに公開する。予期しないfailureは代わりにtransaction全体をabortする。正常commitはGlobal generationを作成し、自分のsequenceのviewだけを無効化する。Repositoryのstateとviewは影響を受けない。
3. **前提** allowlist済みmember pathの近くにcredential、log、runtime state、cache、その他対象外ファイルがある、**操作** Global調査を実行する、**結果** それらの隣接ファイルを読み取らない。
4. **前提** ユーザーがGlobal調査を無効にする、**操作** disable barrierが正常完了してauthoritative snapshotからviewを更新する、**結果** すべてのGlobal sourceとGlobalのカスタマイズファイルをactive sessionから除去する。
5. **前提** Non-no-op disable barrierがaccept済みである、**操作** drainまたはcloseが失敗する、**結果** processはliveなまま、全inspection dataはfenceされclientからpurge済みのまま、retry/join controlを利用でき、cleanupをconfirmできない場合はrestartをfallbackとして示す。以前のGlobal contentを復元しない。
6. **前提** consent済みの2つのhomeが1つのrowの読み取り可能なfileを持つ、**操作** ユーザーが両者を比較する、**結果** rootを統合せず、semanticな判定や変更提案を行わず、各fileを独立して識別されたowning SourceとSource-relative Pathの下に表示する — そしてRepositoryのfileがそのpairの片側として提示されることはない。comparisonは1つのSource familyの内側に留まるからである（§ Clarifications Session 2026-08-28）。
7. **前提** Active Global consentにenable operation/pending workがなくmatching previewを検証済みで、FR-013が定義するserver-derived `retryableTools` setがnonemptyである、**操作** ユーザーがGlobal retryを実行する、**結果** serverはselectorを受け付けず同じfrozen previewに対してFR-013でderiveしたfixed-order projection全体を正確にretryする。Tentativeな間は全既存Sourceとexact pre-operation control/snapshot projectionを維持し、全対象を再びrejectした場合はrequest/generationなしの`active-no-job`を返すか、accepted batchのadmitted subsetだけをpendingとしてatomicに公開し、新規admit済みSourceすべてを正確に1つのrequest、batch、generationで一緒にpublishする。

#### ClosedなGlobal Root Admission Outcome

| Configured-rootのinputまたはphase                           | Closed outcome                                                                              |
| ----------------------------------------------------------- | ------------------------------------------------------------------------------------------- |
| 設定が存在しない                                            | 文書化済みdefault stringを導出して下記rowでclassifyする。Consent前はI/Oを行わない           |
| 設定を持たないmember（共有agent home）                      | 常に導出した文書化済みdefault stringであり、下記rowでclassifyする。Consent前はI/Oを行わない |
| Capture済みenvironment設定のlengthが0                       | `present-empty`。Root、Source、scan jobなしでinvalid preview entryを保持する                |
| StringがU+0000またはunpaired UTF-16 surrogateを含む         | `invalid`。Authorityを保持しない                                                            |
| Active-platform `node:path.isAbsolute`がfalse               | `relative`。Resolveもfallbackもしない                                                       |
| それ以外                                                    | `eligible`。Exact stringをpreviewへfreezeし、consentまでread authorityを持たない            |
| 送信されたconsentがcurrent frozen previewと一致しない       | Requestをrejectし、authority、root、Source、scan jobを作らない                              |
| Consent済みrootがmissing、またはreadableなdirectoryではない | 他memberのcommitを妨げずに、そのmemberをabsentまたはfailedとして記録する                    |
| 1つ以上のconsent済みrootがreadableなdirectory               | 各rootをadmitし、そのGlobal Sourceを単一のatomic batch generationでcommitする               |

#### Byte Decode Outcome

| Byteのcondition                               | Encoding state   | Sourceおよびcomparison outcome                                                                                                                                                                                             |
| --------------------------------------------- | ---------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| NUL byteが1つ以上                             | `binary`         | source textもcomparison eligibilityも公開しない。admit済みcandidateではdiagnostic-onlyとし、それ以外はpublish可能なgenerationを`partial`にする。censusが列挙したcompanionではassetの通常の事実であり、Diagnosticを持たない |
| NULなし、全byteがvalid UTF-8                  | `utf-8`          | 先頭BOMが1つあれば記録して除去し、完全にdecodeしたsourceを公開してcomparisonを許可する                                                                                                                                     |
| NULなし、invalid UTF-8 byte sequenceが1つ以上 | `utf-8-replaced` | Replacement semanticsで1回decodeし、先頭BOMが1つあれば記録して除去し、挿入された全`U+FFFD`を含む文字化けした結果を維持して通常のparse、extraction、source display、comparisonを続行する。別encodingを検出またはretryしない |

### 境界事例

- 選択済みRepository rootをreadできない、startup後に利用できなくなる、またはユーザーが調査を意図したrootではない。`process.cwd()`で取得する前に起動時`cwd`自体が利用不能になる場合もある。
- 調査対象パス一覧に一致するfileがsymbolic linkである（他のfileと同じようにtargetを透過的にreadする。壊れたlinkはper-file diagnosticになる）、または発見から読み取りまでの間に消える（per-file diagnostic）。
- サポート対象filenameが、不正なtext encoding、不正なfrontmatterやconfiguration、極端に長い行、binary contentを含む、または実行環境がfile処理を継続できないと報告する。
- 1つのカスタマイズファイルに複数の認識済みfile typeまたは認識ツールがある。
- カスタマイズファイルがabsolute path、`..` traversal、environment variableの文字列、またはimport chainを介して別ファイルを参照する。参照はauthoredなtextのままで、targetを開かない。
- 設定されたtool homeが存在しない、空、relative、アクセス不能、またはユーザーの通常のhome外にある。
- Global override fileが存在するが空であり、文書化されたfallback fileが適用され得る。
- ブラウザを開いている間に、機密内容が新たに追加される場合を含めてファイルが変化する。
- 明示的な再scanが部分結果を生成した後で致命的に失敗する。部分結果を破棄し、最後に正常commitされたsnapshotをstale markerとfailure diagnostic付きで表示したまま残す。
- Browser sessionがrefreshされる、または起動元とは別のhostから開かれる。
- カスタマイズファイルにリテラルcredential、またはInspector process上で値が設定済みの環境変数への参照がある。リテラルsourceはmaskせず表示し、参照は解決も置換もしない。
- Inspectorがmutation可能なfilesystem requestを発行していなくても、readを契機にOSがaccess timeを更新する。そのOS side effectは別に記録し、product起因mutationのassertionをfailureにしない。
- Admission済みoperationがI/O、decode、parse、assembly、transport中に予期せず失敗する。1つのfileに限定されるfailureはそのfileのdiagnosticになり（FR-028）、それ以外はattemptをfailさせて失敗を通常のerrorとして報告し、最後にcommitされたsnapshotを表示したまま残す。

#### ClosedなScan Publication Outcome

| Terminal condition                                                                                                                                                                                                                                        | Public status                       | Commitおよびresponse outcome                                                                                                                                                        |
| --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Traversalが完了し、readableな`utf-8-replaced` resultを含めて全fileが完全なresultを持つ                                                                                                                                                                    | `complete`                          | 1つのcomplete generationをatomicにcommitする                                                                                                                                        |
| Traversalが完了し、1つ以上のfileがfile-confined outcome（unreadable、admit済みcandidateのbinary content、parse failure — censusが列挙したcompanionのbinary bytesはその通常の事実であり、何もconfineしない。FR-025）だけを持ち、影響を受けない全fileが完全 | `partial`                           | 影響fileのdiagnosticと完全な非影響resultを持つ1つのgenerationをatomicにcommitする                                                                                                   |
| Attemptがcommit前に失敗する                                                                                                                                                                                                                               | その`scanRequestId`について`failed` | Attemptから何もcommitせず、失敗を通常のerrorとして報告し、prior committed snapshotがあれば維持する。明示rescanの場合に限り、維持したsnapshotをそのSourceについてstaleとしてmarkする |
| Disable、shutdown、supersessionがpublication authorityを取り消す                                                                                                                                                                                          | 後続のsuccess statusなし            | 遅延resultをすべて破棄し、取り消されたrequestから何もcommitしない                                                                                                                   |
| Atomic commit後にresponse transportが失敗                                                                                                                                                                                                                 | Commit済みstatusを変更しない        | Clientはsession APIからcommit済みgenerationを再取得する                                                                                                                             |

## 要件 _(必須)_

### 機能要件

- **FR-001**: ユーザーは`npx`経由でプロダクトを起動し、生成されたローカル調査sessionをブラウザで開けなければならない（MUST）。CLIは`process.cwd()`を正確に1回captureし、`--root <path>`を受け付けなければならない（MUST）。Absolute optionはそのまま保持し、relative optionはcapture済み起動directoryに対してresolveし、その結果を選択済みRepository rootとする。`--root`を複数回指定した場合は引数parserのlast value（決定的なlast-wins）を選択rootとし、別途rejectしない。反復引数のポリシーは引数parserが所有し、Gunshiは反復scalar optionを最後の値へ解決する。明示的なempty valueはsession作成またはbrowser起動より前に固定actionableかつsource-value-freeなstartup errorを生成しなければならない（MUST）。Valueの欠落は同じboundaryで引数parserのtyped validationによりrejectしなければならず（MUST）、productはparser所有のcheckを重複実装してはならない（MUST NOT）。CLIは`process.chdir()`を呼んではならない（MUST NOT）。CLIは省略可能な`--port <number>`も受け付けなければならない（MUST）。これはbindされるportそのものではなく、希望するlocal portを指定する: host frameworkがこれを解決し、そのportが空いていればそれを保ち、塞がっていれば別portへ移り、値が0の場合は空きportを自動で選ぶ。Optionを省略した場合、productは希望を述べず、framework自身のdefaultが適用される。Productは独自のdefaultで代替してはならず（MUST NOT）、値を再検査してもならない（MUST NOT）。どのportをbindできるかは、productが知りうる範囲ではなく、そのマシンで空いているものに対して決まるからである。選択済みrootはrepository rootである。Runtime working directoryからrepository rootへ上るvendor lookupはそこで終わるため、inspectorは`$CWD`と`$REPO_ROOT`を1つの選択済みrootとして扱い、全Repository matcherは層を探索せずそこにanchorする。初期リリースは別のrepository pathをpromptしたり、別rootを求めてancestor directoryを探索したり、repository markerをprobeしたりしてはならない（MUST NOT）。呼び出し側がsubdirectoryを指定した場合、そのdirectoryをrootとしてinspectするのであって、productが暗黙に範囲を広げてはならない（MUST NOT）。Certification baselineとは、release candidateの自動browser/accessibility certification suiteの実行対象として、そのrelease candidateのrelease evidenceに記録された、有限のversion付きexact browser buildの集合である。これはsupported user browserのexhaustiveな一覧ではなく再現可能なcertification集合であり、pin済みcertified browserはその集合の1 memberである。macOSでは、自動起動はまずsession URLを既に開いているtabを、起動中のChromium系browserで再利用してよく（MAY）、そのbrowserはlocalのprocess一覧を読んで固定のapplication一覧から選ぶ。この再利用attemptはbest-effortな起動の一部であり、helper呼び出しの成功と同様、compatibilityの証拠ではない。この固定一覧に基づく再利用の選択を超えて、Productはresolved handlerのbrowser family/versionをselect、probe、verifyしてはならず（MUST NOT）、自動起動はbest-effortにとどまる。Resolved browserがcertification baseline外かどうかは、記録済み環境を読む者が判定するのであって、product発行のinspectionで判定してはならない。Browserの自動起動がdisabled、unsupported、failure、unavailable/unidentifiable、またはcertification baseline外へ解決された場合、productはpin済みcertified-browser fallback用の利用可能なprinted local addressを提供しなければならない（MUST）。
- **FR-002**: すべての調査には、FR-001の選択済みRepository rootをrootとし、session bootstrap時に不透明でstableな`sourceId`とescaped root labelを付けて作成したRepository Sourceを正確に1つ含めなければならない（MUST）。選択済みrootが存在しない、またはdirectoryとしてreadできない場合、sessionを利用可能に保ったまま、scanを実行可能なdiagnosticとともにfailさせなければならず（MUST）、そのattemptのpartial inventoryをpublishしてはならない（MUST NOT）。
- **FR-003**: Inspectorは、文書化された調査対象パス一覧に含まれるpathだけからrepositoryのカスタマイズファイルを発見しなければならず（MUST）、リポジトリ内の全ファイルを無差別に解釈してはならない（MUST NOT）。Allowlistは選択されたRepository rootにanchorされる。Productが報告するのはそのrootのカスタマイズであり、特定のruntime working directoryが生む集合ではない。Vendor自身のlookupがそのworking directoryに依存する場合、その依存はvendorの維持管理behavior recordに記録し — recognitionへprojectすることはなく — allowlistを他のdirectoryへ広げてはならない（MUST NOT）。Vendorがfileではなくdirectoryとして定義するcustomization — skillとそのscript・reference・asset — は全体として読む。そのkindとして認識したcustomizationに、そのcandidate自身のdirectory内で付随するfileは、読み取り可能かつ表示可能でなければならない（MUST）。entry pointだけを見せて同梱物を伏せることは、そのcustomizationを見せていないからである。この拡張が従うのは認識である: 自身のbyteを読めなかったcandidateはそのfileに限定されたfinding（FR-028）であり、directoryを見せるべきcustomizationが存在しない。読み取り集合の拡張は正確に2種類だけである（contracts/inspection-path-allowlist.md § Bounded companion census）: 上記のcompanion census（admit済みcandidate自身のdirectoryに限られる）と、plugin-root census（validatedなcatalog entryのlocal sourceが名指すSource-relative directoryに限られる — catalogがadmit済みのcandidateであり、そのentryが名指すrootこそが宣言されたpluginである）。付随fileまたはplugin-rootのfileがそれによってcandidateになってはならない（MUST NOT）。rule、recognition、kind、自身のinventory rowのいずれも獲得しない。
- **FR-004**: 初期リリースは、GitHub Copilot、Claude Code、OpenAI Codexについて、「初期リリースでサポートするカスタマイズファイル」に記載したrepositoryのカスタマイズファイル種別を認識しなければならない（MUST）。
- **FR-005**: 1つのファイルを複数の物理ファイルとして重複させずに、複数のtool、kind、scope、relationshipを表せるよう、物理ファイルとtool-specific recognitionを分離して表現しなければならない（MUST）。
- **FR-006**: ユーザーはsource、tool、カスタマイズファイル種別、Source-relative pathで一覧を閲覧および絞り込みできなければならない（MUST）。
- **FR-007**: 読み取り可能で認識された各カスタマイズファイルについて、source、Source-relative path、file type、認識ツール、完全なsource text、既知のrelationshipを表示しなければならない（MUST）。Censusだけが列挙したfileは、そのdirectoryを持つカスタマイズのdetail — skill自身のdirectory、またはcatalog entryが名指したplugin root — を通じて自身のsource・path・read結果 — readがtextを返した場合はその完全なsource text（FR-025） — とともに表示される。表示すべきfile type・認識tool・relationshipは持たない。それをカスタマイズとしてadmitしたものが無いからである（FR-003）— ruleが独立にadmitするcensus列挙path、nestedな`SKILL.md`は、自身のrecognitionとrowを保つ。例外は、主題がfile自身ではなくfile内の1つの宣言であるrowのdetailである: MCP carrierのrowはそこに宣言された各serverであり、hook carrierのrowはそこに宣言された各lifecycle eventであり、plugin catalogのrowはそのentryが解決する各pluginであるため、そのdetailは1つの宣言から始まり、fileが書いたkeyで、fileの順序どおりに、各値をauthoredのまま公開し、source textをまったく持たない。file自体を表示すれば、そのfileが運ぶ他のすべての宣言と設定が、1つの宣言を主題とする画面に載ることになるからである。それでもcarrier自身のfileとしての事実 — source、path、encoding、size、read結果 — は公開する。Inspectorが読んだfileはInspectorが列挙するfileだからである（FR-003）。どのdetailがあるfileに答えるかは、fileではなく、そこへ辿り着いたrowが決める。rowの主題こそがそのdetailの対象だからである: 1つの物理fileは両方の形のrowを持ちうる — `.codex/config.toml`は宣言した各serverのMCP rowと、file自身である1つの`settings/config` rowを持ち、Codexの`project_doc_fallback_filenames`が名指しした`.mcp.json`はinstruction fileでもある — ため、同じfileが宣言rowの下では宣言だけを、file rowの下では完全なsourceを公開し、どちらも自身の主題が求めるものを伏せない。file自身がカスタマイズであるものは上の規則のままである: instruction file、skill、settingsまたはconfiguration file、censusが列挙したcompanionはいずれも完全なsourceを表示する。Detail surfaceは、それを運ぶfileではなくカスタマイズ自身から始めなければならない（MUST）: skillであればそのskill自身のdirectoryであり、その下に、各認識製品が呼び出す名前を並べる。pageはrouteが指すfileを示す — detail URLは`/skills/detail/<source>/<Source相対パス>`というfile自身のidentityで、各path segmentはpercent-encodeされる — ため、どのdocumentがpageに出るかはlinkのidentityであって、pageが適用するpreferenceではない。tool segmentは持たない: 1つの`SKILL.md`を読む2つの製品は同じbyteを読むため、製品ごとのaddressは1つのdocumentに名前だけが異なる2つのURLを与えることになる。このidentityは再スキャンとserver起動を跨いで安定であり、同じrootを選択する起動である限り、bookmarkされたlinkのpathは同じfileを名指し続ける: SourceとSource-relative Pathの組がfileの唯一のidentityであり — SourceはURLのselectorとして運ばれ、世代ごとのfile IDは存在しない — detail requestはpathを現在のcommit済みsnapshotに対して解決し、別のrootを選択する起動（FR-001）はそのrootのscanに対して解決する（FR-030）。originはdevframeのport選択に属し、希望portが塞がっているときだけ移る（quickstart.md）ため、portの移動だけがbookmarkの指す先を変える。現在のscanが保持しないpathは、近くの何かへ解決されるのではなくdead linkとして報告される。次にfrontmatterが宣言する全key（read-only viewerを通じて1つのYAML documentとして提示する — vendorがskillに対して文書化しているkeyを、Claude Code自身のfrontmatter referenceが公開する順で先頭に置き、それ以外のkeyはfileが書いた順のまま — 2つのskillを比べる読み手が`allowed-tools`を毎回別の位置で探すことにならないようにし、公開された順を採ることで一覧を好みではなくpageに対して照合可能に保つ。blockそのものの言語なので、読み手は自分のfileと変換なしに比較し、変換なしにcopyできる）、次にそのfrontmatter blockを取り除いた指示を置き、directoryの各fileはその傍らから個別に開ける形にする。完全なsource textはそれらとともに提供しなければならず（MUST）、そのsurfaceが既に示している値を、読み取った元のsourceの傍らに再公開してはならない（MUST NOT）。1つの画面が1つの事実を2つの綴りで運ぶことになるからである。表示する値はいずれも、それをkeyする維持管理上のidentifierではなく、そのidentifierが名指す言明として表示しなければならない（MUST）: rule ID、behaviorまたはstrategyのID、matcher lookup base、closedなstatus値は、registry recordまたはwire vocabularyを解決するcontract tokenであり、ユーザーに見えるテキストとして現れてはならない（MUST NOT）。Presentation上のsupported customization file typeは、exactな`(tool, kind)` recognitionと、そのadmit済みsource formの組み合わせとする。維持管理するsupported-customization文書は、closedな`(tool, kind)` presentation allowlistと各rowの適用対象となるsupported source formを列挙しなければならない（MUST）。Rowはdeclared fieldを列挙してはならない（MUST NOT）。Skillの宣言はfileが書いたkeyで公開され、authored keyの集合は閉じていないからである。Relationshipは、そのkindがそのrowに記載され、かつadmit済みsource formのexact extractorがauthored occurrenceを認識する場合だけeligibleとする。1つのsource formについてrelationship kindを列挙しても、別source formでそれをpromoteまたはinferしてはならない（MUST NOT）。Initial releaseはallowlistが記載していないrelationshipを推論してはならない（MUST NOT）。Inventory rowは、認識した各tool自身の文書がそのskillを呼び出す名前で名付けなければならない（MUST）。自らが列挙するものを名指せない一覧はinventoryではなく、また自身が抱えるtoolの一つが応じない名前を見出しに持つrowは、読み手が呼び出せないものを名指すことになるからである: CodexとCopilotはauthoredなfrontmatter `name`を呼び出す — fileが宣言しないか空で宣言する場合はskill directory名。名前付きdirectoryであることがskillであり、これによりすべてのrowが名前を持つ。extractionが失敗したfileも同じdirectory由来の暫定的なidentityへフォールバックする。directoryはpath自身の事実であり、失敗したparseから読み出せるものではないからである（FR-028）。一方Claude Codeは、frontmatterの宣言に依らずskill directoryを呼び出し、authoredな`name`は表示labelだけとして扱い（skills page § How a skill gets its command name）、nestedなskillのcommandには`.claude`を保持するdirectoryのroot相対`/`-joined pathと`:`を前置するため、`name: ship`を宣言する`apps/web/.claude/skills/deploy/SKILL.md`は、Claude Codeのrowでは`apps/web:deploy`、Copilotのrowでは`ship`である。Rowがkeyされる名前とそのtoolが呼び出す名前は1つの事実であり、名前がpathと宣言からどう導かれるかはそのvendor自身のcontractであるため、admitしたruleが解決する。1つのskillの定義をtool横断で比較することは、entry fileでcopyを組み合わせるcomparison surfaceを通じて引き続き可能である。InspectorはすべてのnestedなClaude Code recognitionにprefixを付けなければならず（MUST）、prefixは選択されたroot相対とする: vendorはsession working directory相対のprefixで名前衝突時にqualifyするが、Inspectorはsessionを観測せず、unqualifiedな名前が空いているかはInspectorが読まないuser・enterprise層に依存するため、root相対のqualified綴りだけがstaticなinventoryが保証できる唯一の安定した名前である。Inventory rowの単位は、物理fileではなく認識されたkindが使う単位でなければならない（MUST）: skillは1つのtoolが解決した1つのinvocation name、MCP rowは宣言されたserver名1つで、その名前を解決する全宣言 — `(carrier, tool)`につき1つで、同じ名前を宣言する第2のcarrierはその名前のrowに合流する — をrow内に列挙するもの、instructionsは1つの適用範囲とそれが担当するfileの列挙に、既知の範囲を持たないfileの1 rowを加えたもの — detailはfileで指す — 、custom-agent rowはagent name 1つとそれを定義する全fileの列挙（`(file, tool)`につき1 definition）に、nameを公開しないfileの1 rowを加えたもの、configuration fileはfile自身とする。1つの名前に解決される複数のfileは、それぞれを定義として列挙する1 rowとして公開しなければならない（MUST）。定義は1つのtoolによる1つのfileのrecognition — ToolRecognitionの単位、`(file, tool)`につき1つ — であるため、2つのtoolが1つの名前に解決するfileはそのrowの2つの定義であり、toolごとに異なる名前へ解決されるfileは各名前のrowで定義され、複数serverを宣言する1 carrierは宣言された名前ごとに1 rowを公開しなければならず（MUST）、複数のlifecycle eventを宣言する1 carrierも宣言されたeventごとに1 rowを公開しなければならない（MUST）。名前を宣言せず同名のskill directoryに置かれた2つのfileは1つの名前に解決され、1つの名前に解決される他のfileと同様にそのrowを共有しなければならない（MUST）。複数の定義をgroupingしたrowは、そのうち2つ以上を認識するtoolについて、その名前をどう解決するかを述べなければならず（MUST）、それらを順序づけてはならない（MUST NOT）。記録された記述はtoolごとに異なり、どれも完全には文書化されていないからである。extractionが失敗した定義は、authoredな名前を呼び出すtoolのこの数に入らない: そのtoolが解決する名前は不明であり、フォールバック先のdirectory名のrowは衝突の証拠ではなく暫定的なgroupingだからである（FR-028）。1つしか認識しないtoolはそこで衝突に直面していないため、引用してはならない（MUST NOT）。そのruleはrowが問うていない問いに答えることになるからである。衝突は、引用するruleが答えるものでなければならない（MUST）: Claude Codeの文書化されたruleは、skill directoryに由来するunqualifiedなcommandの衝突に答えるものであるため、その記述は、Claude定義のskill directory名を同一generationの別のClaude認識skillと共有するすべてのrowに付く。異なる名前のdirectoryの下でauthoredな名前だけを共有する2つのfileは衝突の無い2つのcommandであり、そこにruleは引用されない。Rowの名前 — 1つのtoolが解決したinvocation nameで、そのtoolがfileの宣言したものを呼び出す場合にだけauthoredである — はrowが運ぶ唯一の名前値であり、定義は自身の名前を公開しない。Rowの傍らの第2の複製は、事実とそこから導出した値の二重公開になるからである。それ以外のあらゆるdeclared値と完全なauthored sourceにはFR-027のdetailおよびcomparison surfaceを通じてのみ到達できなければならない（MUST）。inventoryやsessionのresponseからは到達できない。Toolがauthoredな名前を呼び出す場合、path segmentがそれを上書きしてはならない（MUST NOT）: CodexとCopilotについて、skill directoryがrowを名付けるのはfileが名前を宣言しないか空で宣言する場合だけである。Claude Codeはこれに当たらない — そのcommandはvendor自身の文書によるdirectoryであってフォールバックではない — 。root相対prefixはそのnested rowをqualifyする。
- **FR-008**: 1 directoryごとのoverrideやfallbackを含め、決定的なdiscovery orderとscope ruleが文書化されている場合は説明し、その基礎となる物理ファイルも表示し続けなければならない（MUST）。
- **FR-009**: カスタマイズを、製品が読み込んだ・選択した・有効化した・信頼したものとして提示してはならない（MUST NOT）。Discoveryはruntime version、product surface、working directory、trust、flag、environment、organization policy、文書化されていない競合解決に依存するが、Inspectorはそのいずれも観測しない。したがって、見つけたものとその場所を述べ、そのruntimeについては何も述べない — verdictも、読まなかった入力のfileごとの説明もである。vendor自身のdocumentationから引く2つの言明が例外であり、どちらもsessionではなくdocumentationを記述する: 複数のfileが争う1つの名前についてvendorが文書化していること（それらのfileを示すinventory row上に置く、FR-007）と、各recognitionの隣に置く、それをadmitしたruleが依拠するdocumented behaviorのproduct surface — vendorがそうしたfileをどこから読むとdocumentしているかであって、いずれかのsurfaceがそれをloadしたかでは決してない。これはproductが持つsurfaceの数にかかわらず、すべてのrecognitionの隣に述べる: surfaceの集合は要素が1つでもfileを読むものを絞り込んでおり — Codexのlocal clientsは、local fileを読まないhosted serviceを除外する — 変化する場所にだけ述べれば、surfaceが1つのproductと、何も述べないkindとを読者が区別できなくなる。
- **FR-011**: ユーザーは、contentベースのmaskingを行わない完全なsource textとrecognition metadataを含め、一つのskill名の2つのcopyを、対応するファイルごとに並べて比較できなければならない（MUST）。比較はmodel自身の座標 — 2つのcopyのentry fileのidentityとcopy相対の比較対象ファイル — で名指され、modelが表現しないペア（別名同士、同じcopyを2回、別kindのfile）は比較されず報告される。Typed recognition metadataはkind固有である（§ Clarifications）: 各kindの比較サーフェスはそのkindが所有するrecognitionをrenderする。entryが複数recognitionを持つ場合でも、同じcopyをcomparisonの両inputへ受け付けてはならない（MUST NOT）。比較は、readableな1つのfileを、別のcopyが何も保持しない対応するpathと組にもできなければならず（MUST）、存在する側の完全な内容を明示された不在に対して表示する — 存在の差は比較の一部であり、空のfileを捏造することは決してない。
- **FR-012**: 比較はリテラルかつ記述的なものに限り、いずれのカスタマイズファイルもvalidate、lint、semantic rank、synchronize、convert、format、または自動修正提案してはならない（MUST NOT）。parserが解決した宣言をside 1つにつき1つのcanonical documentへserializeすること（FR-007、FR-011）はparseの提示であり、fileのconvertやformatではない: 比較対象いずれのfile自身のbytesも書き換えず、整形し直さず、修正して返さず、source比較はそれらをリテラルに表示する。
- **FR-013**: Global調査は新規sessionごとに無効でなければならず（MUST）、完全な4-member scopeとfrozen root previewを説明した後に、session-wideなユーザー操作を1回明示的に必要としなければならない（MUST）。その1回の操作は、説明の後にconsent surfaceで読み手が行うconfirmationか、launch commandの`--inspect-personal-setup` optionのいずれかである。後者はsessionが存在する前に同じ判断を述べるものであり、option自身のdescriptionがscopeを担い、それが確認するpreviewはsessionが同じmember設定からcaptureするものと同一であり、そのconfirmationが何を対象としたか、何がreadされたかはconsent surfaceが述べる。UIとAPIはtoolごとのselectorを受け付けてはならない（MUST NOT）。その操作より前にproposed-root filesystem I/Oを行ってはならない（MUST NOT）。New unconsented previewごとに`COPILOT_HOME`、`CLAUDE_CONFIG_DIR`、`CODEX_HOME`をこの順で正確に1回ずつcaptureし、`undefined`だけをabsentとして、import済み`node:os.homedir()`をpreviewごとに正確に1回callしなければならない（MUST）。共有agent homeは常にそこからderiveされる。Absent defaultはactive platformの`node:path.join`と固定suffix `.copilot`、`.claude`、`.codex`でderiveし、共有agent homeを同じjoinと固定suffix `.agents`でderiveしなければならず（MUST）、`HOME`、`USERPROFILE`その他のhome sourceを独自選択してはならない（MUST NOT）。結果の4 exact stringをfreezeしなければならない（MUST）。Closed ordered lexical-state algorithm（`present-empty`、U+0000またはunpaired surrogateなら`invalid`、`node:path.isAbsolute`により`relative`、それ以外は`eligible`）でclassifyし、全Repository/Global root labelをdata model定義のexact injective UTF-16-code-unit escapingでencodeしなければならない（MUST）。Normalization、raw stringのUTF-8 replacement、presentation-to-path decode、HTML renderingを使ってはならない（MUST NOT）。Previewはserverが保持するopaque `previewId`で識別する唯一のrecordであり、`allowlistVersion`/`traversalPlanVersion`によってshipped allowlistへbindされる。Capture/construction/retention/escape/serializationのthrow/rejectionはrequestを通常のerrorとしてfailさせ、previewもauthorityも作ってはならない（MUST NOT）。Consent後、coordinatorは4つのfrozen preview entryすべてについてadmissionを試行し、clientにeligible memberを省略させてはならない（MUST NOT）。Active-consent retryは同じfrozen preview、consent record、fixed ordered 4-member tupleを再利用しなければならず（MUST）、matching `previewId`を確認済み、`globalEnableInProgress`がnull、`pendingTools`がempty、`retryableTools`がnonemptyの場合だけ提示しなければならない（MUST）。Serverはnon-pending unpublished `admitted` controlと`retryDisposition: same-preview`の`rejected` controlからfixed-order `retryableTools` set全体をderiveしなければならず（MUST）、published、pending、lexicalな`new-preview-required` controlを除外し、UI/APIがretry targetを追加、omit、reorderできないようにしなければならない（MUST）。
- **FR-014**: Initial Global-enable operationは、完全な4-member previewを1つのtransactionとして処理し、Copilot、Claude、Codex、共有agent homeについてそれぞれ最大1つ、0から4つの独立して識別されたmember Global Sourceを1つのgenerationでatomicに作成しなければならない（MUST）。Preview済みrootがmissingまたはunreadableなmemberはSourceを作成せず、他memberのcommitを妨げてはならない（MUST NOT）。Active-consent retryは同じfrozen previewとconsent recordを再利用し、すべての既存Sourceとcommitted snapshotを維持し、新規admit済みSourceを1つのatomic generationでpublishしなければならない（MUST）。各Global Sourceはmemberについて正確に1つのrootへboundしなければならず（MUST）、Global customization fileをRepository Sourceまたは別memberのGlobal Sourceへmergeしてはならない（MUST NOT）。
- **FR-015**: CopilotのGlobal memberは、capture済み`COPILOT_HOME`配下、またはその設定がabsentなら`node:path.join(capturedHomedir, '.copilot')`配下の、次のpathだけを調査しなければならない（MUST）: `copilot-instructions.md`。`instructions/`以下の任意の深さにある名前が`/\.instructions\.md$/u`にmatchするfile。skill名を直下1階層とする`skills/<skill-name>/SKILL.md`。`agents/`直下の名前が`/\.agent\.md$/u`にmatchするfile。`hooks/`直下の名前が`/\.json$/u`にmatchするfile。`settings.json`。`mcp-config.json`。
- **FR-016**: ClaudeのGlobal memberは、capture済み`CLAUDE_CONFIG_DIR`配下、またはその設定がabsentなら`node:path.join(capturedHomedir, '.claude')`配下の、次のpathだけを調査しなければならない（MUST）: `CLAUDE.md`。`rules/`直下の名前が`/\.md$/u`にmatchするfile。skill名を直下1階層とする`skills/<skill-name>/SKILL.md`。`commands/`以下の任意の深さにある名前が`/\.md$/u`にmatchするfile。`agents/`以下の任意の深さにある名前が`/\.md$/u`にmatchするfile。`settings.json`。`output-styles/`直下の名前が`/\.md$/u`にmatchするfile。
- **FR-017**: CodexのGlobal memberは、capture済み`CODEX_HOME`配下、またはその設定がabsentなら`node:path.join(capturedHomedir, '.codex')`配下の、次のpathだけを調査しなければならない（MUST）: 文書化されたinstruction fallback — まず`AGENTS.override.md`を対象とし、FR-035の定義どおりoverrideがabsentまたはemptyの場合に限り`AGENTS.md`へfallbackする。`config.toml`。`hooks.json`。`agents/`直下の名前が`/\.toml$/u`にmatchするfile。`rules/`直下の名前が`/\.rules$/u`にmatchするfile。`prompts/`直下の名前が`/\.md$/u`にmatchするfile。
- **FR-018**: Global調査から、consent済みmember root配下の他のすべてのpathと、FR-015からFR-017およびFR-045にないdirectoryを除外しなければならない（MUST）: 各vendorのmanaged/runtime state（Copilotでは文書化されたautomatically managed file — application state、保存済みpermission、sessionとcommand historyのstate、session store、log、install済みplugin、plugin data、IDE state、MCPのOAuth/secret storage。Claudeではinstall済みplugin tree、agent memory、auto memory、keybindingsとthemeのfile、configuration directory外の別user state file。Codexではmemories、credential、session、install済みplugin copy）、closed kind setがpublishしない文書化されたuser surface（CopilotのLSP configurationとextensions、Claudeのworkflow script）、hostまたはorganizationの設定、credential、log、cache、session data、managed policy、およびすべてのVS Code profile所有file。
- **FR-019**: Inspectorはユーザーが既に信頼しているworkspaceで実行される。その目的はAIエージェントが読み込むものを表示することであり、調査対象customization fileを敵としてモデル化しない。Readとtraversalは通常のfilesystem operationを使用し、operation間の反復的なidentity再検証、race-detection taxonomy、physical-identityによるread-once groupingのような敵対的入力向けの機構をproductへ追加してはならない（MUST NOT）。
- **FR-020**: Skill、command、hook、plugin、workflow、extension、script、handler、prompt、agent、rule、その他の調査対象contentを実行してはならない（MUST NOT）。
- **FR-021**: 調査対象contentに記載されたMCP serverを起動、接続、probe、またはrequest送信してはならない（MUST NOT）。
- **FR-022**: カスタマイズファイルの発見と表示によって、product発行のoutbound network request、dynamic code evaluation、またはcustomization content由来のchild-process実行を引き起こしてはならない（MUST NOT）。Local loopback hostは固定のhost名`localhost`（platformのresolverがIPv4 `127.0.0.1`またはIPv6 `::1`として解決する）を通じてloopback interfaceだけにbindし、packaged UI assetとlocal session API channelだけを配信し、調査contentから選択されたdestinationを使用したり、調査contentを別machineへ送信したりしてはならない（MUST NOT）。Detail surfaceは、表示しているfileをreader自身のmachine上のapplicationで開くことを併せて提示し、hostはreaderの明示的な要求に応じてそれを実行する: clientにはSource rootがdisplay escapingとしてしか渡らないため、pageは他のrequestと同じSource-relative Pathを送るだけで、自らpathを保持しない。Hostは何かをlaunchする前に、そのpathをcurrentのcommit済みgenerationへ解決しなければならない（MUST）。これによりlaunchが受け取り得る絶対pathはsessionが公開したものだけになり、保持していないpathはdetail requestと同じstaleness outcomeを取る。提示するapplicationは、そのmachineでhostがlaunchできるものと正確に一致しなければならない（MUST） — portをbindする前にlauncherを解決した各editor、hostがterminal windowを開けるならreader自身のterminal editor、そしてfile種別に対する登録済みhandlerとそのhandlerをfile自身のdirectoryへ適用したもの — 。したがってどのsurfaceも、何も起きないapplicationを提示せず、該当editorを持たないmachineにはそれを提示しないだけで、editorについて説明することもない。spawnされるprocessはその解決済みpath以外のinspection由来の値を受け取ってはならず（MUST NOT）、shellのmetacharacterを含むauthoredなfile名は1つのliteralなargumentとしてapplicationへ届かなければならない（MUST）。すべてのlaunchは、本product自身がcommand lineへ組み立てることのない1つのargumentとしてpathを渡す。それでも2つのlaunchはshellに到達するが、いずれも本productが組み立てたtextによってではなく、維持されているcomponent自身のquotingによってである。1つはplatformのhandler launcherで、維持されているlauncher packageがWindowsではPowerShell、Linuxでは`xdg-open`を通じて実行し、その1つのargumentをpackage自身がescapeする。もう1つはterminal editorである: terminalはcommand行を実行することでprogramを載せるため、pathは固定のautomation scriptへargumentとして渡し、そのcommand行への挿入はautomation host自身のPOSIX shell用quotingが行わなければならない（MUST）。このproductが組み立てたtextを連結してはならない（MUST NOT）。また、そのhostへの待ちには上限を設けなければならず（MUST）、許可を与えていないmachineではrequestを保持せずに失敗する。Machineがそのfileをどう扱うかはmachineの領分である: productはlaunchを要求したことだけを報告し、何が応答したかは報告しない。登録済みhandler自体が開いたものを実行するprogramであり得る点も、machine自身のfile browserからそのfileを開いた場合と同じである。FR-001に基づくstartup時のbrowser起動はproductのもう1つのchild-process surfaceであり — macOSでは固定のprocess一覧probeと、OSの`osascript` automation hostで実行する固定のtab再利用script、それ以外では固定のOS browser起動helper — そこでspawnされるものはinspection由来のcontentもpathも一切受け取らない。自動起動がdisabled、unsupported、または失敗した場合もinspectionを利用可能に保たなければならない（MUST）。
- **FR-023**: 調査対象sourceに対し、productが制御するmutation requestを一切発行してはならない（MUST NOT）。Product起因mutationには、write、truncate、create、rename、delete、link、modeまたはownershipの変更、file time、extended attribute、ACLの設定、またはmutation可能なflagでのopenを要求することを含む。Inspectorはaccess-time更新を要求してはならない（MUST NOT）。Readだけを契機にOSが行うaccess-time変更はproduct control外であり、product起因mutationとして数えたりInspectorがsourceを変更した証拠に使ったりせず、別に記録しなければならない（MUST）。
- **FR-024**: Traversalとreadはsymbolic linkを透過的にfollowする。Inspectorは同じpathをreadするagentが見るものを表示するからである。Targetがmissingまたはunreadableなlinkはそのfileのper-file diagnosticを生成し、recursiveなtraversalはreal pathで訪問済みdirectoryを追跡して、link cycleがscanの終了を妨げないようにする。Hard linkは通常のfileであり、physical-identityによるgroupingを必要としない。PublicなSource-relative Pathは、そのfileのexactなraw entry nameを`/`でjoinしたもの — filesystem operationが使うのと同じ綴り — であり、presentationは保存値を変えずに制御文字をescapeする。Entry nameはNode.jsがそのentryに対して返すstringである（`fs`は文書化された既定としてnameをUTF-8でdecodeする）。したがってvalid UTF-8でないplatform上の名前はreplacement-decodeされて届き、その返されたstringがこの製品のraw nameである。そのstringを通じてplatformが再解決できない名前は、影響を受けるoperationの通常のfailureとして表面化する。
- **FR-025**: Inspectorは、credential detection、contentベースのmasking、redaction、reveal stepなしで、読み取り可能なカスタマイズファイルのsource textを表示しなければならない（MUST）。表示する宣言名は、そのfieldについてparserが解決した値をmaskせずに用いなければならず（MUST）、すべてのauthored valueを書かれたとおりに運ぶ完全なauthored sourceを利用可能なまま保たなければならない（MUST）。これによりcredential間の差を含む差分を確認できる。publishする値自体を短縮するsurfaceがあってはならない。NUL byteが1つでもあればそのitemを`binary`に分類し、source textもcomparison eligibilityも公開してはならない（MUST）。そのように分類されたadmit済みcandidateはdiagnostic-onlyのままとし、それ以外はpublish可能なgenerationを`partial`にしなければならない（MUST）。そのように分類されたcensus掲載companionはassetの通常の事実であり、Diagnosticを持たせても、generationをpartialにしてもならない（MUST NOT）。それをtext customizationとしてadmitしたものが何もないからである。NULを含まないすべてのfileはUTF-8 replacement semanticsで正確に1回decodeしなければならない（MUST）。Decodeが1つ以上の`U+FFFD`を挿入した場合、encoding stateは`utf-8-replaced`としなければならず（MUST）、その結果のexactな文字化けstringが`sourceText`となり、それ自体でscanをpartialにせず、通常のparse、extraction、display、comparisonを続行しなければならない（MUST）。先頭のUTF-8 BOMが1つある場合は別に記録して`sourceText`から除去しなければならない（MUST）。Inspectorは別encodingの推測またはretry、replacement characterの除去または隠蔽、contentのsamplingまたはtruncation、product定義のbyte、行、item上限の導入をしてはならない（MUST NOT）。
- **FR-026**: 調査対象content内の環境変数参照はリテラルtextのままとし、Inspectorが参照先のprocess environment値を読み取り、解決、置換する契機にしてはならない（MUST NOT）。この制限は、FR-015からFR-017およびFR-045が明示的に文書化したtool-home環境変数またはcapture済みhome directory自体をGlobal member rootの特定だけに使用することを妨げない。
- **FR-027**: Detailまたはcomparisonのsurfaceは、読み取り可能なfileをcredential detection、contentベースのmasking、redaction、reveal stepなしに記述されたとおりそのまま表示しなければならない（MUST）。Monacoのtext modelはdocumentごとに1つのend-of-line sequenceを保持するため（research.md § 7）、行末が混在するfileをそれ上に構築したviewerが多数派の行末でrenderしcopyしてよい（MAY）。これはその特定のviewer componentの性質であり、本要件からの逸脱ではない — 本要件が拘束する正確な`sourceText`はwireが運びcomparisonが消費する値であり、行の内容と行数は影響を受けない。authored contentに何が含まれ得るかについての注意書きを掲げてはならず（MUST NOT）、contentを確認stepの背後にgateしてもならない（MUST NOT）。Inspectorはloopback-boundなsession上でユーザー自身のfileを表示するため、どちらも何も守らない。確認はすべてのfileを読むのに2回の操作を要求し、常設の注意書きは読み手自身のrepositoryについて読み手に説明するために画面を費やす。Authored contentはbrowser memory内だけに保持し、永続化・cache・logのいずれも行わず（MUST NOT）、1つのfileまたは1つのcomparisonに対する明示的なrequestでのみ到達できなければならない（MUST）。inventoryやsessionのresponseがそれを運んではならない。例外はinventory row自身のidentityである。rowはそれによって列挙され、それなしには列挙できない: skillの解決済みの名前、pathから導出するのではなくfile自身が宣言した適用範囲、宣言されたMCP server名、hook rowが冠する宣言されたlifecycle event、prompt/command rowがgroup化されるinvocation name、custom-agent rowがgroup化されるagent name、plugin rowが冠するplugin名、そしてoutput-style rowがgroup化されるstyle名がこれにあたる。Row identityをrowを識別する範囲を超えて広げてはならず（MUST NOT）、他の宣言値がそれに伴って運ばれることはない。そのauthoredな部分は、scan時の一度のparseが解決した値であり、この製品はmasking・escape・正規化のいずれも加えない。代わりにproductが供給する部分 — skillについてはfileが宣言しないときのdirectory fallbackとnestedなClaude Code recognitionが持つroot相対のprefix、output styleについては同じruleでのfile名fallback、commandについてはその名前全体で、commandを読むどのproductもpathから導出する — は、responseが既に公開しているpathから、FR-007の命名ruleに従って構成される。これらのidentityのうち4つは供給されるのではなく完全にauthoredである: pluginのそれは、自身の宣言が書いた名前をcatalogの名前で修飾した、そのvendorがaddressに使う形であり、どの部分もpathから来ない。hookのそれは、carrier自身のmapが書いたevent keyであり、vendorが文書化する三層のconfiguration形式がkeyとする階層そのものである。VS Code prompt fileのそれは、そのproductがfrontmatterの`name`を読み手が入力するものとして文書化しているため。custom agentのそれは、宣言された`name`でagentを識別するproduct — OpenAI CodexとClaude Code — のもとで、両者ともその`name`をagentのidentityとし、filenameを一致させることはlookupではなくconventionだと文書化しているためであり、これらのrowをpathで名指せばproductが持たないagentを報告することになる。GitHub Copilotは逆方向の例外である: `name`をoptionalなdisplay nameとして文書化し、profileをconfiguration file自身の名から`.md`または`.agent.md`を除いたもので識別するため、Copilotのrowはpathから名付けられ、宣言された`name`で名指せばCopilotがその名でdeduplicateしないagentを報告することになる。したがってどの事実が答えるかはkindではなくadmitしたruleに属し、2つのproductが認識する1つのfileは、両者の答えが異なる限り2つのrowに定義を持つ。またcustom-agent rowは、真に不在になり得る唯一のidentityでもあり、それは宣言された`name`で識別するproductのもとでのみ起こる: 宣言しないfile、scalar以外を宣言するfile、宣言をまったく読み取れなかったfileは、nameがnullである唯一のrowを共有し、それは名前ではなくauthoredな値を一切運ばない。Client-data purgeは、document-liveness failureまたは同等のterminal reset後にinventory、detail、comparison、editor model、memory内metadataをclearする中央のfull-session client resetでなければならない（MUST）。Global-disable actionはrequest送信前にこのpurgeを実行し、より大きい`globalContentEpoch`またはnon-null `globalDisableInProgress`の観測後はrender前に繰り返さなければならない（MUST）。Route close、通常のfileまたはSourceのremove、generation changeでは、client-data purgeを構成せずに対象scopeのmodelをdisposeしてよい（MAY）。本要件のいずれもaccess-control factorではない。session APIへはloopback-boundなlocal hostを通じてのみ到達でき、それが境界のすべてである。初期リリースはcredential maskingまたはreveal workflowを提供してはならない（MUST NOT）。
- **FR-028**: 1つのfileに限定される問題—unreadableなfile、binary content、またはparser/extractorのfailure—が、他のfileの発見や表示を妨げてはならない（MUST NOT）。影響itemには、ユーザーが問題を解決できるだけのsource-relative pathとSource contextを残さなければならない（MUST）。Parser/extractorのfailureでは、（あればreplacement characterを含む）完全でreadableなsourceを表示可能かつcomparison-eligibleのまま維持し、影響recognitionのderived metadataまたはrelationshipだけを省略しなければならない（MUST）。ただし、すべてのrowが宣言rowであるfile — standalone hook file、MCP carrier、plugin catalog — は例外である。そのdetailは宣言を公開しfile自身のbyteは公開しないため（FR-007）、failureではDiagnosticとfile自身のfactを公開し、読者はfile opener（FR-022）でtextに到達し、そのfileのsourceを保持するsurfaceが無いためcomparisonの対象にもならない。
- **FR-029**: Inspectorはfile size、fileまたはitemの件数、parser構造、requestまたはresponse size、work queue、scan時間、filesystem operation、open handle、coordinator容量について、product固有の数値validation limitを定義してはならない（MUST NOT）。利用可能な容量はNode.js、選択したparser、OS、filesystem、browser、実行環境から継承しなければならない（MUST）。Applicationは予期しないfailureのcapacity、resource、operational原因を、再分類・retry・recoveryのために検査してはならない（MUST NOT）。1つのfileに限定されないfailureは影響attemptをfailさせ、そのattemptはitem、Source、recognitionまたはderived result、scan-result body、success response、generationを一切commitしない。Closedな原因checkは単一file read境界に1つだけ存在する: resource-exhaustion errno集合 — `EMFILE`、`ENFILE`、`ENOMEM` — は読んでいるfileではなくmachineの状態を識別するため、そのfailureはそのfileのper-file outcomeになるのではなくattempt abortへ送られる（Constitution § Quality and Safety Standards）。これ以外のerrno・message・原因の検査は存在しない。Requestがdisable、shutdown、supersession、failureによってpublication authorityを失った後に完了したworkは、その遅延resultをすべて破棄しなければならない（MUST）。EngineまたはOSによる終了はenvironment limitationであり、product guaranteeとして表現してはならない（MUST NOT）。
- **FR-030**: ユーザーはactiveなsourceを明示的に再scanできなければならない（MUST）。Admit済みの各scan commandは不透明な`scanRequestId`を受け取らなければならず（MUST）、queued、active、complete、partial、failedの各statusはそのrequestを識別し、成功したgenerationはcommitしたrequestを記録しなければならない（MUST）。以前のstatus、snapshot、generationが新しいrequestの完了を満たしてはならない（MUST NOT）。Publicな`partial`はClosedなScan Publication Outcome表のfile-confinedな`partial` outcomeだけを意味しなければならず（MUST）、他のincomplete stateをpublishしてはならない。RepositoryとGlobalの調査は、lifecycleが独立しているため独立したgeneration sequenceを保持する。Repository sequenceはbootstrap generation 0から始まり、Global sequenceはそれを作るenable commitからdisableが破棄するまでだけ存在する。Scan resultは所属するsequenceの1つのgeneration snapshotとしてatomicにcommitしなければならない（MUST）。成功したcompleteまたはpartialのcommitはそのsequenceの以前のsnapshotを置換し、そのsequenceの以前のgenerationに属する`FileDetail`、comparison selection/view、editor-model stateを無効化しなければならず（MUST）、stale responseからそれらを復元してはならない（MUST NOT）。Commitはもう一方のsequenceのstateを変更も無効化もしてはならない（MUST NOT）。FileのidentityはそのSourceとSource-relative Pathであり、generationを跨いで安定している — 世代ごとのfile IDは存在しない — ため、detail requestはそのidentityを現在のcommit済みsnapshotに対して解決する: 現在のgenerationがそのpathに保持するfileを提供するか、保持していないことを報告し、以前のgenerationのrecordを提供することは決してない。Rescanがcommit前に致命的に失敗した場合、Inspectorはそのscanの未commit resultをpartial resultを含めてすべて破棄し、最後に正常commitされたsnapshotを維持し、そのSourceについてrescan失敗を理由にstaleとしてmarkし、失敗requestのerrorを表示しなければならない（MUST）。All-rejectedなGlobal attemptはcommitを生成してはならない（MUST NOT）。
- **FR-031**: 調査結果は既定でsession内に限定し、初期リリースではprofile、cache、repository fileとして永続化してはならない（MUST NOT）。
- **FR-032**: Inventory、detail、comparison、Global control、diagnostic、API response、CLI output、documentationの全surfaceで、初期リリースはvalidator、linter、natural-language semantic analyzerまたはranker、synchronizer、converter、formatter、auto-fixer、policy engine、remediation adviserとして動作してはならない（MUST NOT）。構文だけのparsing、認識したkindが公開する宣言についてparserが解決した値の読み取り、その解決済み宣言のdetailおよびcomparison surfaceが描画するdocumentへのserialization（FR-007、FR-011、FR-012）、確定済みdocumented field/ruleに対する分類、文書化済みorder、scope、condition、selection、reference relationshipの投影だけを行ってよい（MAY）。これらの操作は、確定済みcatalogを超える正しさ、有効性、compliance、quality、supportを判定または示唆してはならず（MUST NOT）、parse diagnosticはvalidation findingではなくdescriptive failureのままにしなければならない（MUST）。
- **FR-033**: カスタマイズファイルのsource textと宣言名は不活性なtextまたはdataとして表示しなければならない（MUST）。埋め込まれたmarkup、image、link、URI handler、control sequence、その他のcontentを、カスタマイズファイルの表示だけで実行、load、navigateしてはならない（MUST NOT）。
- **FR-034**: `AGENTS.md`というfilenameだけを理由にClaude Code recognitionを付与したり、`.claude/hooks`内の参照されていないscriptをhookと推測したり、単独の`.claude/prompts` directoryをサポート対象Claude Codeカスタマイズファイル種別として扱ったりしてはならない（MUST NOT）。
- **FR-035**: Codex instructionについて、Inspectorは各directoryで空でないinstruction fileを最大1つ選ぶ文書化されたrule—該当するoverrideを最初に、それ以外は通常fileと設定済みfallback name—および、Globalからrepositoryを経由してruntime working directoryへ向かう広いscopeから狭いscopeへのorderを表さなければならない（MUST）。Globalの`AGENTS.override.md`/`AGENTS.md` branchでは、decodeしたnon-binary stringについて、任意の先頭UTF-8 BOMを1つ除去した後に`String.prototype.trim().length === 0`となる場合をemptyとしなければならない（MUST）。Whitespace-only textはemptyであり、保持した`U+FFFD`はnon-whitespaceである。Fallbackはoverrideがabsentまたはemptyの場合に適用する。Binaryまたはunreadableなoverrideは、そのdiagnosticとともにbranchを終了し、fallbackしない。Working directoryまたはconfigurationが不明な場合、そのchainはconditionalのままにしなければならない（MUST）。
- **FR-036**: Claude instructionについて、文書化されたlayering — 広いscopeから狭いscopeへのorderと、同じlevelでlocal instructionが通常instructionに続くこと — はvendor自身のruntime挙動であり、そのvendorの維持されるcomposition strategyへ記録しなければならない（MUST）。Product surfaceへ投影してはならない（MUST NOT）。具体的なsessionがどのlayerを読み込むかはruntime working directoryとそのsessionが読むfileに依存し、inspectorはそのどちらも観測しないからである（FR-009）。
- **FR-037**: 複数のCopilot instruction sourceが同時に適用され得る場合、またはprecedenceがproduct surfaceによって変わる場合、各recognitionを維持し、一般的なsemantic上の勝者を作り出してはならない（MUST NOT）。
- **FR-038**: 初期リリースの実装とpackageに含む実行可能なapplication codeは、すべてJavaScript/TypeScriptでなければならない（MUST）。CLI、local host、調査対象sourceのfilesystem layerはNode.jsの公開JavaScript API上で動作し、browser logicはJavaScript/TypeScript sourceから生成しなければならない（MUST）。Declarativeな生成済みHTML/CSS、strict JSON manifest、documentation、license fileはpackageへ含めてよい（MAY）。ProductにRust code、Node-APIその他のnative addon、prebuilt native binary、package lifecycleでのcompile、package lifecycleまたはruntimeでのartifact downloadを含めてはならない（MUST NOT）。Production dependency closureで許可する唯一のnon-JavaScript executableは`open` packageのvendoredなPOSIX shell `xdg-open`であり、そのpackage自身の選択policyは、Linux hostではvendored copyが実行可能である限りそれを使い、そうでないときにsystemの`xdg-open`へfallbackする。他のclosure payloadは実行可能なnon-JavaScript codeを持ち込んではならない。Closureに関する保証はcommit済みlockfileがpinしたresolutionに対して確立し、releaseで監査する。公開packageはlockfileを持たないため、consumerのinstallはcaret rangeをfreshに解決し、監査済みresolution外のdependency versionは、後続のあらゆるinstallの認証済み性質ではなく、記録済みのresidual limitationである（research.md § 3）。
- **FR-042**: Global disableはscoped client-model disposalではなく、recover可能なcoordinator barrierでなければならない（MUST）。Bundled browserはdisable request送信前にFR-027のfull client-data purgeを実行しなければならない（MUST）。Non-no-op barrierの最初のaccept時、serverは`globalContentEpoch`をatomicにincrementし、non-null `globalDisableInProgress`をinstallし、publication authorityをrevokeし、全inspection-data routeを固定conflictでfenceしなければならない（MUST）。Fence中のsession routeはcontrol/errorだけの`GlobalFenceRecoverySnapshot`だけを返さなければならない（MUST）。各inspection-data successはcaptureしたepochへbindし、responseの最終publish時にepoch不変かつfence nullを再checkしなければならない（MUST）。いずれかのresponseでより大きいepochまたはnon-null fenceを観測したclientはrender前にpurgeし、control-only recoveryへ入らなければならない（MUST）。独立したliveness probeは定義しない——transportは問い合わせずともhost喪失を報告し、単一browser sessionがsupportされる使い方だからである。Accept後failureではfence、失敗requestのerror、retry/join controlを保持し、process livenessを維持し、purge済みcontentをrestoreしてはならない（MUST NOT）。Cleanupを確認できない場合はprocess restartをfallbackとする。Accept前failureまたはtrue no-opではfenceをnullのままにし、purge済みclientがfresh full snapshotを直ちに取得可能にしなければならない（MUST）。PublicなGlobal consent、control、Source stateのいずれかが存在する場合、`remove-active-state`成功ではGlobal generation sequence全体とそのSourceを破棄しなければならない（MUST）。Repository sequence、そのgeneration、そのIDは影響を受けない。未公開のoperation-local initial-enable stateだけのcleanupでのみ`cleanup-only`を使用してよく、そのsuccessはcommit済みstateを一切変えずにfenceをremoveしなければならない（MUST）。
- **FR-043**: 公開packageは、browser bundleがcodeを含むすべてのthird-party packageのcopyright noticeとpermission noticeを同梱しなければならない（MUST）。Build済みSPAはそれらのcodeをinlineするため、利用者はlicense fileを伴うinstall済みpackageとして受け取らないからである。Notice listは手作業で維持せず、build時に完成したbundleからderiveしなければならない（MUST）。そうすればdependency変更によってbundle済みpackageが漏れることがない。またpackage testが、packaged notice fileの存在とbundle済みeditorのcopyright holder名の記載を証明しなければならない（MUST）。掲載対象はmanifestのどのsectionが宣言したかではなくbundleされたかで決めなければならない（MUST）: client側がbundleされるdependencyはそのfileに含まれ、package managerが利用者のためにinstallしrun timeにloadされるものを重複記載してはならない（MUST NOT）。後者は自身のlicense fileを伴って届き、2つ目の写しは古くなるだけである。Bundle済みpackageのlicense textが見つからない場合は、license識別子だけで公開せずbuildを失敗させなければならない（MUST）。
- **FR-044**: 同梱browserは読み手自身のmachineに、この applicationをどう提示してほしいか、そのmachineがfileをどう扱うかについてのpreference — detail surfaceがfileを開くapplicationと、pageを描く配色 — だけを保持してよい（MAY）。それ以外は保持しない。こうしたpreferenceはinspectedな値を一切保持してはならない（MUST NOT）: Source root、Source-relative Path、authoredな名前、fileの内容、session identityのいずれも持たないため、FR-027のpurgeとFR-031のsession scopeがclientの保持するinspectedな値すべてを引き続き記述する。読み手が配色を選ぶまでpageはoperating system自身の設定で描かれなければならず（MUST）、pageが開いている間その設定に追従し続けなければならない（MUST）。したがって最初の訪問が何かを固定することはない。選ばれた配色はsessionをまたいで永続しなければならず（MUST）、source viewerを含むすべてのsurfaceに適用されなければならない（MUST）。この永続はbrowser自身のsame-origin storageによるものであり、同じaddressから提供される以後のすべてのsessionに届く。異なるportにbindされたhostはbrowserにとって別のsiteであり、そのsessionは最初の訪問と同様にoperating systemの設定から始まる。選択肢は製品が描く2つの配色であり、systemの設定を表す第3の値は持たない。未選択のpageが既にそれに追従しているからである。Browserがstorageを拒否した場合、および製品が認識しない保存値の場合も、inspectionは完全に使用可能でなければならず（MUST）、faultを報告するのではなくoperating systemの設定へfallbackしなければならない（MUST）。配色を設定するcontrolはkeyboardで操作できなければならず（MUST）、その目的と現在の状態を支援技術へ伝えなければならない（MUST）。読み手のplatformがそのcontrolの視覚的なaffordanceを抑制する場合は、その名前と状態が目的と状態を伝えるものになる。
- **FR-045**: 共有agent homeのGlobal memberは、`node:path.join(capturedHomedir, '.agents')`配下 — この境界を移動させる文書化済み設定は存在しない — の、次のpathだけを調査しなければならない（MUST）: skill名を直下1階層とする`skills/<skill-name>/SKILL.md`。これはCodexとCopilotの両方がpersonal skillの場所として文書化している。そして、Codexが文書化するpersonal plugin marketplaceである`plugins/marketplace.json`。このmemberはFR-013の4つのpreview entryの1つであり、tool homeとまったく同じようにconsentとadmissionを経る。admitされたfileは、文書がそのmatched pathを名指すすべてのtoolのrecognitionを持つ。

### 初期リリースでサポートするカスタマイズファイル

計画phaseでは、実装前にこれらのカスタマイズファイル種別と調査対象パスをその時点の公式仕様と照合し、正確な調査対象パス一覧を確定しなければならない（MUST）。再確認によって曖昧なfilename patternを狭めてよいが、仕様変更なしに別productを追加したり、Global memberをFR-015からFR-018およびFR-045より広げたりしてはならない（MUST NOT）。確定後のrevalidationはrecurringかつmaintainer所有とする。Maintainerは、すべてのfrozen release candidate前と、supported surfaceへのmaterialなupstream変更を認知した時点で、controlledなofficial-source drift reviewを実行しなければならない（MUST）。Reviewは、reverse-indexされた全affected recordと両言語版のreview、paraphrased assertionとfingerprintの明示更新、`reviewedOn`の前進をもってのみ完了する。

| ツール         | Repositoryの調査対象パスとカスタマイズファイル種別                                                                                                                                                                                                                                                                                                                      | 明示的な対象外またはconditionalな動作                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| -------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| GitHub Copilot | Repository全体およびpath-specific instruction、認識対象`AGENTS.md`、rootの`CLAUDE.md`と`GEMINI.md`、custom agent、`.github/skills`、`.agents/skills`、`.claude/skills`配下のskill、promptとCopilot CLI互換command、hook宣言、exactなVS Code `.vscode/mcp.json`とVS Code 1.118以降のexact workspace root `.mcp.json`を含むMCP宣言、サポート対象settingsとplugin metadata | Surfaceに依存するsupportと文書化されていないprecedenceはvendorの維持管理contractに記録され、どのsurfaceもprojectしない。1.118 root `.mcp.json` release assertionはcurrent guideの網羅的location listとconflictし、そのVS Code schemaとtotal same-name orderはunknownのままとするため、このprovenanceはpath/surface-onlyとし、同じphysical fileの独立CLI extractionを分離して保持する。instruction fileに書かれたCLI `@path` targetはsource textであり、targetを開かない。Hosted personalまたはorganization configuration、`COPILOT_CUSTOM_INSTRUCTIONS_DIRS`または`COPILOT_SKILLS_DIRS`で指定する追加directoryは初期リリース対象外。Local originを持たない文書化済みCloud/runtime behaviorはスコープ外とし、hosted stateとconfigurationは調査しない |
| Claude Code    | `CLAUDE.md`、`.claude/CLAUDE.md`、`CLAUDE.local.md`、nested instruction file、`.claude/rules`、skill、legacy command、subagent、project/local settings、宣言済みhook、root MCP configuration、output style、pluginは manifest・marketplace catalog entry・その両方のいずれかで宣言される                                                                                | 記述された`@path` importはsource textであり、targetを開かない。`AGENTS.md`をfilenameだけでは認識しない。参照されていないscriptをhookと推測しない。単独の`.claude/prompts` directory、managed settings、managed instructions、無関係なuser stateはRepository source対象外                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| OpenAI Codex   | `AGENTS.md`と`AGENTS.override.md`、`.agents/skills`、custom agent定義、project configuration、hook宣言、MCP宣言、rule、pluginはmarketplace catalog entryで宣言され、そのentryのlocal sourceが名指すplugin rootのfileを伴う                                                                                                                                              | instruction fileに書かれた参照はsource textであり、targetを開かない。Project trustまたはworking directoryに依存することはvendorの維持管理contractに記録される。非推奨のuser custom promptとuser-level skillはRepository source対象外                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |

### 主要Entity

- **Inspection Session**: FR-001で定義した選択済みRepository rootからfilesystem I/Oを行わずbootstrap時に作成したRepository Sourceを正確に1つ、0から4つのmember Global Source、現在のscan result、comparison selection、diagnosticを含む一時的なユーザー活動。
- **Scan Request**: 不透明な`scanRequestId` 1つで識別する、1つのadmission済みRepository initial scan/明示的rescan、またはnonemptyなinitial-enable/retry `GlobalBatchScan`。決定的なall-rejected Global initial attempt/retryは`active-no-job`でありScan Requestを作らない。Global batchはSourceごとではなく全admit済みtool rootに1 requestを使う。以前のsnapshotまたはstatusが新しいrequestを満たさないよう、すべてのScan Requestのstatusと正常commitしたgenerationをrequestに対応付けたままにする。
- **Source**: 種別（`Repository`または`Global`）、正確に1つのroot location、enabled state、scan statusを持つ、明示的なsource identity。Repository Sourceはstableな`sourceId`とescaped root labelを備えてsession bootstrap時から存在する。Global Sourceはconsent後のatomic commitによってのみ作成し、さらに固定4 member集合 — サポート対象の3つのtoolとshared agent home — の正確に1 memberで識別する。そのroot内にある異なる種別のcustomization fileは別々のinventory itemとして扱う。
- **Source-relative Path**: カスタマイズファイルを所有するSourceの1つのrootを基準にした、display、filtering、lookup、selection用path — exactなraw entry nameを`/`でjoinしたもの。Repository Sourceの場合に限り選択済みRepository rootからのrepository-relative pathとなり、各Global Sourceは自身のrootとnamespaceを使用する。
- **カスタマイズファイル**: Source-relative Pathで識別される、Source内で発見された1つのfile。Encoding（導出されるread stateもこれが決める）、readableな場合の完全なsource text、recognition、relationship、diagnosticを持つ。
- **Tool Recognition**: カスタマイズファイルに付与するtool-specific interpretation。Tool、file type、宣言名、そのextraction outcomeを含む。
- **Evidence Assessment**: 1つのexactなbehavior、rule、strategy subjectに対するatomicなevidence state — closedなdocumentation completenessとordered lifecycle qualifierであり、分離したまま単一scalarやqualifier unionへ縮約しない。それはmaintenance factとして採点対象のregistry record上に生き、response、provenance、relationshipのいずれも運ばない。製品が報告するのは見つけたものであって、vendorが自身のruleをどこまで文書化しているかではないからである。
- **Relationship**: カスタマイズファイルから別pathまたは宣言済みcomponentへの、実行されない参照。Import contentを展開せず、boundaryとresolution statusを含む。
- **Diagnostic**: カスタマイズsource valueを複製しない、session内だけの情報であり、空結果、決定的なmalformedまたはbinary content outcome、不確実性、stale file、cycle、boundary violationを実行可能に説明し、正確に1つのlocation scopeを持つ。File-scoped Diagnosticはcoherentな`sourceId`と`sourceRelativePath`のpairを必須とし（MUST）、fileは特定したSourceに属し、pathはそのSource内での当該fileのpathでなければならない（MUST）。Source-scoped Diagnosticは`sourceId`だけを必須とし（MUST）、`sourceRelativePath`を禁止しなければならない（MUST）。Path-lessなscopeは存在せず、すべてのDiagnosticはSourceに属さなければならない（MUST）。このlocation scopeは、Diagnosticがcommit済みgenerationまたはsession lifecycleのどちらに属するかとは独立する。1つのfileに限定されない予期しないfailureは通常のerrorとして表面化し、Diagnosticにはならない。

## 品質要件 _(必須)_

### 保守性とコードの明確さ

- **QR-001**: 調査対象パスの定義、source boundary、recognition、precedence ruleは、無関係なtoolを変更せずに1つのtoolを更新できる凝集したownershipと明示的なinvariantを持たなければならない（MUST）。自明でないsecurityまたはcompatibility判断には理由を文書化し、抽象化は実際に共通することが示された振る舞いだけに限定しなければならない（MUST）。

### テストと検証

- **QR-002**: 自動検証は、各toolの調査対象パス一覧に含まれるpathと含まれないpath、multi-tool recognition、source separation、決定的なorderとfallback、すべてのuncertainty state、comparison、opt-inとdisable flow、不正なfile、encoding、回復可能な環境/runtime failure、symlinkの透過的なread、最後にcommitされたsnapshotへの致命的な再scanのrollback、リテラルcredentialの正確な表示、環境変数参照の非解決、ならびに実行、source mutation、MCP connection、禁止対象のカスタマイズファイル起因network accessがゼロであることを示すregression testを扱わなければならない（MUST）。Network verificationは、loopback host自身のstatic assetとlocal session API以外のproduct発行requestが0件であることを要求しなければならない（MUST）。さらに現在のrequestに対する客観的なqualifying-status assertionも検証しなければならない（MUST）。SC-003、SC-004、SC-005、SC-007のcheck-in済みrelease-evidence fixture manifestについて、schema、version、再現可能なmanifest digest、一意でstableなcase ID、criterionとrequired-classのmembership、fixtureまたは決定的builderへのreference、客観的expected outcome、参照fixtureのcontent digestを検証しなければならない（MUST）。すべてのrequired classは空でなく、参照された全caseが存在して実行され、measurement recordは正確なmanifest version、manifest digest、実行済みcase IDを識別しなければならない（MUST）。すべてのerror caseには客観的な期待結果が必要であり、end-to-end browser testは4つのuser storyすべてを扱わなければならない（MUST）。Diagnosticの検証はclosedなfile/source scope unionを扱わなければならない（MUST）。File scopeはcoherentな`sourceId`/`sourceRelativePath` pairを必須とし、source scopeは`sourceId`だけを必須として`sourceRelativePath`を禁止する。これらのDiagnostic entity invariantに違反する、欠落、余分、Source間で不整合、または捏造されたlocation tupleをすべて拒否しなければならず（MUST）、commit済みgenerationとsession-lifecycleのどちらに属するかは直交するlifetime上の問題として扱わなければならない（MUST）。
  検証はproductのfilesystem requestを計測して、mutation可能なoperationが0件であること、および観測可能な範囲でcontent、length、identity、link state、mode、modification/change time、extended attributeまたはACLが変化しないことを証明し、OSだけによるaccess-time変更を別に記録しなければならない（MUST）。Node.jsには安定したextended-attribute/ACL APIが存在しないため、それらの属性については直接のbefore/after snapshotではなく変更時刻（`ctime`）の観測が文書化された間接signalとなる。製品がfile size、item件数、parser構造、request/response size、queue、時間、concurrencyのvalidation上限を定義しないこと、失敗したrequestがsessionを利用可能なまま残すこと、適用時にpublication authorityが取り消されること、破棄された遅延resultがsnapshotへ決して入らないことを検証しなければならない（MUST）。Cross-surface negative contract/browser/CLI-output/documentation testは、Inventory、Detail、Comparison、Global control、Diagnostic、API response、CLI output、documentationがvalidationまたはlint、natural-languageの意味解釈またはrank付け、正しさ・有効性・compliance・qualityの判定、contentのsynchronization、conversion、formatting、fixing、policy engineとしての動作、remediation adviceを行わないことを証明しなければならない（MUST）。

  Evidence検証は、behavior、rule、strategy subjectごとにassessmentを正確に1件要求し、closed enum外のdocumentation-status value、重複または順序違反のlifecycle qualifier、`documentation-conflict`のdocumentation statusとしての使用をすべてrejectし、documentation status・lifecycle qualifier・evidence assessmentをどのresponseも運ばないこと — これらはregistry自身のmaintenance recordである（QR-005） — を証明しなければならない（MUST）。

### セキュリティとプライバシー

- **QR-003**: Viewing sessionは起動元machineからのみ到達可能でなければならない（MUST）。Hostはloopbackだけにbindし、その外へ公開してはならない（MUST NOT）。配信contentにはユーザー自身のsecretが含まれ得るからである。Session hostはそのbindingの内側で認証なしに動作し、他のlocal processおよびDNS rebinding経由の悪意あるweb pageがInspector実行中にsessionへ到達し得る残存limitationは、host contract（`contracts/http-api.md` § Host requirements）が明記しなければならない（MUST）。readmeはこの製品の利用者向け文書であり、読者がツールを起動して読むために必要なことを載せるものなので、この記述の置き場所ではない。調査対象sourceのI/Oは単一のinspection moduleにとどめ、read-onlyとする。完全なauthored contentはloopback session APIだけから返してよく（MAY）、bundled browserは1 requestにつき1 fileまたは1 comparisonだけをrequestする。Contentはinertかつsession内だけに保ち、永続化、別machineまたはremote serviceへの送信、logまたはtelemetryへの複製をしてはならない（MUST NOT）。

### ドキュメントと参加しやすさ

- **QR-004**: 英語・日本語のユーザー文書とContributor文書は意味的に同等でなければならない（MUST）。readmeと、そこからリンクする配下のページがユーザー文書であり、読者がツールを起動し、表示されたものを読むために必要なこと — launchとsetup、既定の`process.cwd()` Repository rootと任意の`--root <path>`選択semantics、source boundaryとsession-wide all-tools Global consent、完全なsourceの直接表示、diagnostic — を説明しなければならない（MUST）。正確な調査対象パス一覧、今回のreleaseが対象外とした文書化済みsurface、conditional interpretation、環境変数参照を解決しないこと、環境由来のresource behaviorは維持管理するvendor contractが所有し、それぞれruleごとに根拠と並べて述べる。readmeでそれらを繰り返せば、readmeの読者が誰も開かない表の2つ目の写しになる。主要なdiscovery、inspection、comparison、consent workflowはkeyboardで操作でき、意味のあるlabelとfocus stateを提供し、ローカルbrowser interfaceに適用されるWCAG 2.2 AA基準を満たさなければならない（MUST）。Maintainerは、WCAG 2.2のLevel AおよびAA success criterionをすべて列挙する、意味的に同等な英語・日本語のWCAG 2.2 AA applicability/acceptance matrixを維持しなければならない（MUST）。各criterion rowはapplicableかどうかを示し、非適用のrowはcriterion固有の理由を示さなければならない（MUST）。Applicableな各rowは、必要なautomated check、manual checkまたはその両方をstable IDで名称付きにし、expected resultと記録済みevidenceを示さなければならない（MUST）。Matrixは`MANUAL-*` checkが定義されるmanual execution matrixの形を述べ、そうした各checkをこのreleaseでは未実行として記録しなければならない（MUST）。実行はSC-008の外であり、manual cellはいずれも結果を持たない。Error messageは問題と実用的な次の手順の両方を示さなければならない（MUST）。
- **QR-005**: 保守するすべてのvendor behavior、Inspector rule、runtime-composition strategyは、canonicalな第一者documentation URL、正確なreview済みsection、review dateへ解決する1つ以上のstable source IDを参照しなければならない（MUST）。Atomicなbehavior、rule、strategy assertionはそれぞれ、正確な`documentationStatus`と`lifecycleQualifiers`を所有しなければならない（MUST）。引用した各sectionが、それを引用した対象のassertionをどこまで確立しているかを保守者が判別できるようにするためである。これらは保守用のrecordとする: read allowlistの記録された根拠であり、revalidationが突き合わせる材料であって、responseもsurfaceも運ばない。製品は見つけたものを報告するのであって、その背後のdocumentationを説明するのではないからである。`documentationStatus`はclosed enum `documented | partially-documented | unknown | conflict`とする。`documented`は引用したofficial sectionが保守対象assertionを完全に確立すること、`partially-documented`は一部だけを確立すること、`unknown`はそのassertionについて決定を確立しないこと、`conflict`は保持したofficial assertionに互換性がないことを表す。`lifecycleQualifiers`は`preview`、`experimental`、`deprecated`の固定順で重複のないarrayとする。Empty arrayはlifecycle claimがないことだけを表し、`stable`として表示してはならない（MUST NOT）。保守するpath、schema、またはprecedence assertionがそのsurfaceの全review済みversionで有効ではない場合、所有rowはversion gateを明示しなければならず（MUST）、upstreamがversion tokenを公開する場合は正確なtoken（例: `VS Code 1.118+`）で示し、公開しない場合は未解決の`engine-version` condition factを名指しし、そのgateを確立するversion付きrelease noteまたはchangelog sectionを引用しなければならない（MUST）。そのgateのeffective dateは、引用したreleaseが公開する場合にだけ存在する。日付なしまたはrollingなpageでversion gateを確立してはならず（MUST NOT）、引用したassertionを超えるrollout stateは推測せず`documentationStatus`と`lifecycleQualifiers`に保持しなければならない（MUST）。`documentation-conflict`はdocumentation statusではない。この語彙で互換性のない場合の綴りは`conflict`である。Vendor lookup behavior、Inspector matcher、runtime compositionはownershipを分離し、各製品は独自のbehavior文書を持ち、Repository behaviorとUser/Global behaviorは別表を使用し、GitHub CopilotのVS Code、CLI、Cloud behaviorは別表を使用しなければならない（MUST）。すべてのRepository matcherは、selectorをauthorしたtyped segment array program — selected Repository rootを基点とするliteral、regex、非隣接recursive-directory segment — として示さなければならず（MUST）、globのように見えるrendered string形式を持たない。自動documentation checkは、closed assessment enumと順序、record単位のsubject identity、英日parity、identifier uniqueness、相互参照、controlled official-source driftを検証し、behavior、rule、strategyを自動変更してはならない（MUST NOT）。

## 成功基準 _(必須)_

### 測定可能な成果

**初回利用者評価のガバナンス**

20セッションによる評価はinitial-release candidateについて1回実施する。自動checkとprojectに詳しい
contributorだけでは、project contextを持たない初回利用でのdiscoverabilityと正しい解釈を立証できない
からである。固定分母20は観測された19/20と18/20という閾値を明示するためのものであり、母集団に対する
統計的主張ではない。同じ20セッションをSC-001とSC-006で再利用しなければならない（MUST）。

セッションは人ではなく自律agentである。初見のparticipant 20名を集めることはこのprojectには不可能で
あり、したがってこの評価はrecruitment、compensation、moderation、consent、retention、reviewer
processのいずれも伴わず、どのartifactもそれらを要求してはならない。Evidenceはmaintainer teamが所有
する。Pull requestごとの義務ではなく、通常のcontributorに実施を求めてはならない（MUST NOT）。

評価はrelease candidateのbuildに対して実行しなければならず（MUST）、記録はそのbuildと、採点に用いた
task材料 — `tests/usability/sc001-sc006-study-inputs/`配下のguidance、4つの定型task prompt、
response form、ground truth、scoring rubric — を明示しなければならない（MUST）。Primary workflow
またはそれら材料にmaterialな変更があった場合は再実施しなければならない（MUST）。この評価はagentが
実施するため、記録はそれを明記しなければならず（MUST）、結果を人によるものとして提示してはならない
（MUST NOT）。

**Release-Evidence Fixtureのガバナンス**

Release candidateごとに、SC-003、SC-004、SC-005、SC-007は測定前にfreezeしたcheck-in済みでversion付きのrelease-evidence fixture manifestを1つ使用しなければならない（MUST）。Manifestの各caseは、一意でstableなcase ID、criterionとrequired-classのmembership、fixtureまたは決定的builderへのreference、客観的expected outcome、参照するすべてのfixture byteのdigestを持たなければならない（MUST）。各evidence recordはmanifest versionと再現可能なdigestを識別し、実行した正確なcase IDを記録し、該当criterionが指定するrequired classごとに0件ではない件数を示さなければならない（MUST）。Caseのremove/reclassify、required-class定義の変更、expected outcomeの変更は、manifest versionをincrementし、明示的なreviewを受け、新しい直接比較不能なmeasurement setを開始しなければならない（MUST）。参照fixture byteだけを変更する場合は、影響する全fixture digestとcanonical manifest digestを更新し、その場合も新しい直接比較不能なmeasurement setを開始しなければならない（MUST）。Digestの変更だけでdenominator semanticsの変更を認可してはならない（MUST NOT）。Manifestの欠落、空、read不能、digest不一致、caseの欠落または重複、required classの空集合、参照fixtureの欠落、result未記録、manifest記載caseの省略は、影響するすべてのcriterionをfailureにしなければならず（MUST）、release denominatorを黙って減らしてはならない（MUST NOT）。

実際にdenominator semanticsを変更する場合、release validationは変更前後のmanifest version、変更したcase ID、required-class定義またはexpected outcome、明示的なreviewer decisionまたはreview referenceを記録しなければならない（MUST）。Automated contract testはtable-drivenなprevious/current manifest revision pairに対してversion/digest transition ruleを検証しなければならないが（MUST）、そのtestがhuman reviewを立証すると主張してはならない（MUST NOT）。初回manifest作成にはprior revisionがないことを明記しなければならない（MUST）。

- **SC-001**: 20件の独立したfirst-useセッションによる評価において、19件以上が、提供されたproduct guidanceだけを用いて、対象Repository rootからinspectorを起動し、発見されたcustomization file 1件を2分以内に開く。1セッションは、そのguidanceと定型taskプロンプトだけを与えられた自律agentが駆動する。Productが自ら印字・描画するもの以外に、command、navigation、interface操作のヒントは与えない。**これが測るのはproduct自身のguidanceでfileに到達できるかであって、人がどう感じるかではない**。human-subjectのusability studyではなく、そのように報告してはならない（MUST NOT）。この評価のためにparticipantをrecruit・moderate・consentすることはなく、その予定も無い。初見のparticipant 20名を集めることはこのprojectには不可能であり、だからこそこのcriterionは、何もassertしないのではなく自動runが立証できるものを測る。2分のtimerは定型taskプロンプトの提示時点で開始し（MUST）、発見されたcustomization file 1件のsource/details viewが実際に開いて操作可能になった時点で停止する（MUST）。したがって計測区間にはInspectorの起動と、印字されたURLでそこへ到達することが含まれる。Rootの選択（そこへ移動する、または`--root`を与える）は自動化されたUser Story 1 testが検証するproduct capabilityであり、この評価で計測する操作ではない。完了を妨げるまたは中断するenvironment/product failureは、task timer開始前のものも含め、そのcriterionの不成功として数えなければならず（MUST）、セッションを除外・置換してはならない（MUST NOT）。同じ20セッションを同じ評価runのSC-006にも使用し（MUST）、SC-001を先に実施する。
- **SC-003**: SC-003 manifestは、サポート対象のexactな`(tool, customization file type, admitted source form)` rowごとにpositive fixtureを1件以上、freezeしたinspection-path selector familyごとにrejected near-miss fixtureを1件以上、文書化済みmulti-tool attribution combinationごとにshared-physical-file fixtureを1件以上含めなければならない（MUST）。Manifest記載fixture全体で、調査対象パス一覧に含まれるサポート対象カスタマイズファイルの認識率100%、一覧外のファイルを解釈する件数0、共有物理ファイルに対するmulti-tool attributionの正解率100%を達成する。
- **SC-004**: SC-004 manifestは、サポート対象toolごと、禁止対象effect class（カスタマイズファイル由来のcommandまたはcode execution、child process、MCP connection、FR-022で定義したdirect product-issued outbound request、product起因の調査対象source mutation）ごと、およびRepositoryとGlobalの各source kindについてboundary外selectorを1件以上含めなければならない（MUST）。Manifest記載fixtureの100%において、inspectionはカスタマイズファイル由来のcommandまたはcode execution、child process、MCP connection、direct product-issued outbound request、product起因の調査対象source mutationを0件とし、有効なsource boundary外として拒否されたselectorに対する意図的なread requestを0件とする。Direct-request assertionは対象product network/URL/MCP surfaceをinstrumentし、exactな2つのFR-022 authorized internal loopback class、すなわちpackaged UI assetへのstatic/SPA `GET`/`HEAD`と、発行済み`localhost` authorityにおけるlocal session API channelを別々に分類・検証し、それ以外のinstrument済みsurfaceで禁止対象request 0件を要求しなければならない（MUST）。全fixture rootがlocalであることも記録しなければならない（MUST）。Lexicalに識別不能なpre-mounted/mapped network filesystemはこのassertionの対象外としてFR-022のplatform/environment limitationとして文書化したまま残し、explicit UNC/server-share/device vectorはfilesystem、DNS、SMB call 0件を証明しなければならない（MUST）。Mutation assertionはproductのfilesystem operationをinstrumentし、platformが公開する範囲でcontent、length、identity、link state、mode、modification/change time、extended attributeまたはACLが変化しないことを観測しなければならない（MUST）。OSのread semanticsだけに起因するaccess-time変更は別に記録し、このcriterionをfailにせず、product起因mutationの証拠にしてはならない（MUST NOT）。
- **SC-005**: SC-005 manifestは、サポート対象のexactな`(tool, customization file type, admitted source form)` rowごとに読み取り可能なexact-display fixtureを1件以上含み、source/comparison両surface、literal-credential/environment-variable-reference両class、set-sentinel/unset referenced-variable両stateについて0件ではないcaseを持たなければならない（MUST）。Manifest記載fixtureの100%で、リテラルcredential値と環境変数参照textがsource viewとcomparison viewにmaskされず変更なしで表示され、参照先のprocess environment値が表示contentへ混入せず、maskまたはreveal controlも表示されない。
- **SC-006**: SC-001を実施した後、同じ20セッションがSC-006を実施する。SC-001の結果にかかわらず、全セッションが同じ指定customization fileを開いた状態でSC-006を開始しなければならない（MUST）。2分のtimerはその状態が整い定型taskプロンプトを提示した時点で開始する（MUST）。各セッションはfileのsource、recognizing tools、file typeの必須項目を持つ定型response formに回答を記録しなければならない（MUST）。成功は3項目すべてを2分以内に提出し、各項目が指定fileの事前定義ground truthと一致することを要する。欠落または誤りのある項目は不成功として数える（MUST）。18セッション以上が、SC-001が定めるノーヒント方針の下で提供guidanceだけを用いて成功しなければならない（MUST）。**SC-001と同様、これが測るのはproductがfileについて何を述べるかであって、人がそれをどう読むかではない。** その計時付き回答の提出後、20セッションすべてが定型のcomparisonタスクとGlobal-consentタスクを同じノーヒント方針で実施しなければならない（MUST）。SC-001のdiscovery観測と計時付きSC-006のinspection観測と合わせ、これらのタスクは4つのprimary workflowすべてを覆わなければならない（MUST）。登録された全セッションと全environment/product outcomeは、除外も置換もせず、4つの客観的workflow完了outcomeと事前定義safety event項目とともに記録し続けなければならない（MUST）。**このcriterionのsafetyの側こそ、自動runが最もよく測るものである**。意図しないexecution、被検査sourceのmutation、FR-022が定める禁止対象のdirect product-issued outbound requestまたはMCP connection、exactな2つのauthorized internal loopback class外のrequest、被検査contentの他マシンへの露出は、いずれも自動的にcriticalである。セッションはそれを、自身のbrowserが発したrequestと被検査treeの状態から観測する。同じ性質は自動化されたFR-022およびUser Story testが各自のgateで担保しており、そのための独立したstudy instrumentation、proxy、reviewer processは存在せず、必要でもない。Zero-critical-issue gateは、20セッションすべてが4つのworkflow outcomeを記録し、criticalなissueが観測されなかったときにのみ通過しなければならない（MUST）。

- **SC-007**: SC-007 manifestは、file-confined outcome classであるmalformed content、binary content、invalid non-NUL UTF-8 replacement decoding、unreadableなfile（壊れたsymbolic linkを含む）の各fixtureと、failure classであるjob accept前にrejectされたsession-API request、accept済みsession-API job後のfailure、startup failureの各fixtureを1件以上含めなければならない（MUST）。Manifest記載fixtureの100%で、invalidなnon-NUL UTF-8はreadableな`utf-8-replaced` textとして処理され、それ自体でscanをpartialにしない。他のfile-confined outcomeはそのfileの実行可能なdiagnosticと許可されたcompleteまたはpartial commitを生成し、他のfileへ影響しない。失敗したrequest-owned attemptはnew/partial resultを一切commitせず、失敗requestのerrorとともに最後の正常commit済みsnapshotだけを利用可能に保つ。Startup failureは実行可能なmessageとともにlaunchを終了する。Accept済みの明示rescan failureは最後の正常commit済みsnapshotを維持してstaleとしてmarkし、accept前rejectionは`scanRequestId`を作成せず、失敗したinitial Global enableはGlobal Sourceまたはgenerationを作成せず既存snapshot stateを維持する。Manifestはさらにaccept後のGlobal-disable failureを扱わなければならない（MUST）。この場合、REST processは全inspection dataをfenceし、retry/join controlをretainし、restart next stepを示して生存しなければならない（MUST）。
- **SC-008**: 4つのprimary workflowすべてがkeyboardだけで完了でき、維持管理される二言語applicability matrixが名指す自動WCAG 2.2 Level A/AA checkが、3つの認証browserすべてで通過する。Matrixは、どのLevel A/AA success criterionが適用対象であり、非適用のものはなぜ非適用かを記録し続ける。Criterion rowの欠落、必要なrationaleの欠落、check mappingの欠落はgateをfailさせ、英日のmatrixは意味的に等価であり続けなければならない（MUST）。適用対象のcriterionは、そのrowが名指す安定IDの自動checkすべてに合格結果が記録されているときに通過する。**支援技術に対する手動実行はこのcriterionの一部ではない。** かつて要求していた閉じたmanual matrixは、3つのoperating systemと3つのscreen readerなしには実行できない。したがってSC-008が主張するのは自動層とkeyboard workflowであり、manual checkを名指すrowはそのcheckを未実行の残余として記録する。SC-008は、適用対象の自動checkの失敗が0件のときにのみ通過する。欠陥のseverityはこの合否規則を変えない。

## 前提

- 初期リリースはローカルのsingle-user inspection sessionである。Remote hosting、collaboration、account、durable profileは対象外とする。
- 初期リリースの実行可能なapplication codeはすべてJavaScript/TypeScriptで実装する。Browserは生成済みclient logicとdeclarative assetを実行し、それ以外のproduct codeはすべてNode.js上で実行する。Strict manifest、documentation、license fileはnon-executable package dataのままとする。FR-038のとおり、公開packageのuserはRust toolchain、native compiler、native addon、platform別prebuilt binary、package lifecycle/runtimeでのartifact downloadを必要としない。公開package payloadとproduct runtimeはこれらを一切含まず、要求もしない。記録済みの唯一のclosure例外は、production dependency closure内の`open` packageのvendoredなPOSIX shell `xdg-open`であり、そのpackage自身の選択policyは、Linux hostではvendored copyが実行可能である限りそれを使い、そうでないときにsystemの`xdg-open`へfallbackする（FR-038）。開発専用toolingは公開package payloadの外にあり、この保証の対象外である。Contributor toolchainは、prebuiltなplatform別componentやpin済みcertification-browser downloadを含み得る、個別にpin/audit済みのthird-party development dependencyを使用するが、これらを公開payloadまたはproduct runtimeへ持ち込んではならない。
- 調査対象のRepository rootとopt-in済みGlobal rootは、起動したuserが管理する、ユーザーが既に信頼しているworkspace内の通常のlocal pathである。通常の同時editはper-file diagnosticまたはfailed scanとして現れ得るので、ユーザーが再scanする。Workspaceを信頼しないユーザーはそこでこのツールを実行すべきではないため、productは敵対的なlocal processに対する防御を行わない。
- Product起因source mutationは、OSのaccess-time policyではなく、mutation可能なfilesystem requestと観測可能なsource propertyによって測定する。Inspectorはaccess-time変更を要求しない。OSだけによるread-side access-time更新は別に記録し、product mutationの成立根拠にしない。
- 選択済みRepository rootは調査boundaryであり、いずれかのcoding agentが使用する実効working directoryの証明ではない。起動時`cwd`または`--root`でsubdirectoryを選択した場合、Repository Sourceはそのsubtreeに限定される。より広いscopeを調査するには、意図するrootからcommandを再実行するか、そのrootを`--root`で指定する。
- 公式のカスタマイズ形式は変化し得る。正確な調査対象パス、filename、extensionは計画時に再確認して確定し、公開したうえでconformance fixtureによって検証する。Version付きrelease noteがcurrent general guideの網羅的listから省略されたbehaviorを直接追加する場合、そのspecific additionはscopeに残し、互換性のないevidenceを`conflict`として保持する。記載されないschemaまたはorderingを推測しない。
- 公式pageは日付なしまたはrolling更新であり得て、upstream featureはpreview、rollback、supersedeされ得る。Evidenceの鮮度はpage dateではなくrecordごとの`reviewedOn`とsection fingerprintで固定し、日付なしまたはrollingなpageはversion gateを確立しない。Preview中のfeatureはclosedな`preview`/`experimental` lifecycle qualifierを保持する。Rollbackまたはsupersedeされたassertionはdrift時に再reviewし、直接矛盾が残る場合は削除するか`conflict`として保持する。いずれのstateも`stable`として表示したり推測で解消したりしない。
- Global調査は、FR-015からFR-017およびFR-045が名指す文書化されたuser-levelカスタマイズfile — instruction、rule、skill、commandとprompt、agent、settings、hook、MCP宣言、output style、personal plugin marketplace — を、それぞれのconsent済みmember root配下で対象とする。それらのroot外、または製品のclosed kind set外にある文書化されたuser surface — VS Code profileのfile、LSP configuration、extensions、workflow script、managed/remote configuration — には、別の同意と将来の仕様作業が必要である。
- Source text、表示対象の宣言名、authored relationship target、comparison contentは、記述された差分を確認できるようcredential maskingなしで表示する。調査対象content内の環境変数参照はリテラルのままとし、解決しない。Productはreveal workflowを持たない。Loopback-boundなlocal hostがAPI access boundaryであり、それが境界のすべてである。authored contentの前に確認stepは立たず、その隣に注意書きも置かない。Bundled browserはfileをmemory内だけに保持し、client-data purgeで破棄する。
- Inspectorは、構文だけのparsing、認識したkindが公開する宣言についてparserが解決した値の読み取り、frozen catalogに対する分類、文書化済みstructural order、scope、condition、selection、reference relationshipの投影を行ってよい。Natural-languageの意味を解釈せず、正しさ、有効性、compliance、qualityを判定せず、contentをrank付けせず、remediationを助言しない。Parse diagnosticはvalidation resultではなくdescriptive failureである。
- File、collection、parser、transport、queue、時間、concurrencyの容量は、Node.js、parser、OS、filesystem、browser、実行環境から継承する。Inspectorは数値capacity上限を定義せず、thrownまたはrejected operationの原因を調べず、customization contentをvalidまたはinvalidに分類しない。
- Node.js公開APIのfilesystem promiseは強制cancelできない場合がある。Disable、shutdown、supersessionはpublication authorityを取り消し、遅延resultを破棄する。Productはkernel I/Oの物理的cancelや、uncaughtなstartup error後のprocess survivalを保証しない。
- 初期リリースで一度に比較できるのは2つのdistinctなカスタマイズファイルに限定し、contentのmergeやeditは行わない。
- プロダクト文書とサポート対象一覧は公式vendor documentationを規範となる外部dependencyとして使用し、文書化されていない動作は明示的にuncertainなまま扱う。
