---
'agent-customization-inspector': minor
---

Claude Code is now named as a reader of every `AGENTS.md` in the repository — at the root, where it joins GitHub Copilot, OpenAI Codex, and Antigravity CLI, and in any subdirectory, where the file is listed under that directory's range. Claude Code 2.1.277 and later read `AGENTS.md` where they read `CLAUDE.md`. Whether a session reads it instead of the `CLAUDE.md` files or beside them depends on the files on its own path and on a user-level setting, so no row states which.
