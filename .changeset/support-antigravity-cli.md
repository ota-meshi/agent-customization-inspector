---
'agent-customization-inspector': minor
---

Add Antigravity CLI as a fourth supported tool. The inventory now lists the files Antigravity CLI reads — the root `GEMINI.md` and `AGENTS.md`, the skills, custom agents, and MCP servers under `.agents/`, and, after consent, the context file, MCP servers, custom agents, skills, settings, permissions, and hooks of the `~/.gemini` home — and names Antigravity CLI as a reader of the files it shares with the other tools.
