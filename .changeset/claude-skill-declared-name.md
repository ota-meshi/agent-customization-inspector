---
'agent-customization-inspector': minor
---

A Claude Code skill at the selected root is now listed under the `name` its frontmatter declares — the name Claude Code's own slash menu and the desktop app list it under — with the skill directory as the fallback, so a root `SKILL.md` that Claude Code and GitHub Copilot both read is one row rather than two. A nested skill keeps its directory-qualified command, such as `apps/web:deploy`.
