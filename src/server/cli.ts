#!/usr/bin/env node
// Inert CLI entry scaffold. The Gunshi-based command definition, the anchored
// `--cwd` selection contract, and session bootstrap arrive with the minimal
// secure foundation tasks; importing or running this module has no effect yet.
// This file is the direct `package.json.bin` target: tsdown preserves the
// shebang in the bundled `dist/cli.mjs`, and the package manager makes the
// linked bin executable at install time.
export {};
